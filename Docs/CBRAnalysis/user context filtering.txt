// Source: 2.2 Update 4 (21UPDATE4)_CBR1.5_1735.cs
// CheckForMatchingConsignee(CustomerOrderObject) // returns boolean indicating whether the customer order consignee matches the current shipment consignee
      CheckForMatchingConsignee: function (CustomerOrderObject)
      {
        try
        {
          var con = $('name-address[nameaddress="vm.currentShipment.PackageDefaults.Consignee"]');

          let a = {
            //conCompany: $(con).find('input[name="company"]').val().trim().toUpperCase().replaceAll(' ',''),
            conAddress1: $(con).find('input[name="address1"]').val().trim().toUpperCase(),
            conCity: $(con).find('input[name="city"]').val().trim().toUpperCase(),
            conState: $(con).find('input[name="stateProvince"]').val().trim().toUpperCase(),
            conPostalCode: $(con).find('input[name="postalCode"]').val().trim().substring(0, 5)
          };

          let b = {
            //conCompany: CustomerOrderObject.shipAddress1.CONSIGNEE_COMPANY.trim().toUpperCase().replaceAll(' ', ''),
            conAddress1: CustomerOrderObject.shipAddress1.trim().toUpperCase(),
            conCity: CustomerOrderObject.shipCity.trim().toUpperCase(),
            conState: CustomerOrderObject.shipState.trim().toUpperCase(),
            conPostalCode: CustomerOrderObject.shipZipCode.trim().substring(0, 5)
          };

          /* Remove all the spaces in the strings so we get a good comparison, not one that's thrown off by an extra space. */
          let aa = JSON.stringify(a).replaceAll(' ', '');
          let bb = JSON.stringify(b).replaceAll(' ', '');

          /* Quick comparison to see if the strings match. */
          if (aa != bb)
          {
            Logger.Log({ Source: 'CheckForMatchingConsignee()', Message: 'Could not add order ' + CustomerOrderObject.orderNumber + ' (' + CustomerOrderObject.orderId + '). Primary order data to follow.', Data: a });
            Logger.Log({ Source: 'CheckForMatchingConsignee()', Message: 'Could not add order ' + CustomerOrderObject.orderNumber + ' (' + CustomerOrderObject.orderId + '). Subsequent order data to follow.', Data: b });
          }

          return (aa == bb);
        }
        catch (error)
        {
          Logger.Log({ Source: 'CheckForMatchingConsignee()', Error: error });
        }
      },

// Source: ABB OPTICAL GROUP (ABBOptical)_ABB Optical Group Reference Rule_22.cs
// PreSearchHistory(searchCriteria) // returns undefined
this.PreSearchHistory = function(searchCriteria) {
        var userContext = this.GetUserContext();
        searchCriteria.WhereClauses.push({ FieldName: 'UserId', FieldValue: userContext.UserId, Operator: 0 });
    }

// GetUserContext() // returns user context object or undefined
this.GetUserContext = function () {
        if (this.vm.userContext) return this.vm.userContext;
        if (this.vm.UserInformation) return this.vm.UserInformation;
    }

// Source: Alight Solutions (ALISOL)_AlightSolutions Client Rules_1842.cs
// GetBatches(context) // returns batches filtered by the provided user context's CompanyId
this.GetBatches = function (context) {        
        var batches;
        var data = { SearchCriteria: null, CompanyId: context.CompanyId };
        this.thinClientAPIRequest('GetBatches', data, false).done(function (response) {
            batches = response.Batches;
            console.log(batches);
        });
        
        return batches;
    };

// Source: AlstonBird (ALBI)_ClientBusinessRules_1866.cs
// PreSearchHistory(searchCriteria) // returns void
    this.PreSearchHistory = function (searchCriteria) {
        this.vm.shipping ??= client.setMailroomOrTraveler(this.vm.profile.Name);
        if (this.vm.shipping.IsTraveler) searchCriteria.WhereClauses.push({ FieldName: 'MiscReference20', FieldValue: client.userName, Operator: 5 });
    }

// setMailroomOrTraveler(profileName) // returns an object with IsMailroom, IsTraveler, LoadRadioButtonsIsHidden
        setMailroomOrTraveler: function(profileName) {
            return {
                IsMailroom: profileName != 'Traveler Profile',
                IsTraveler: profileName == 'Traveler Profile',
                LoadRadioButtonsIsHidden: true
            }
        }

// Source: AMERICAN INTERNATIONAL GROUP (AIG)_Shipper Ref = Custom1_25.cs
// this.PreSearchHistory(searchCriteria) // returns void
    this.PreSearchHistory = function (searchCriteria) {
        searchCriteria.WhereClauses.push({ FieldName: 'UserId', FieldValue: userContext.UserId, Operator: 0 });
    }; 

// Source: Amplifon (AMPN)_Amplifon CBR_1917.cs
// function LoadCostCenter() // returns void
    function LoadCostCenter()
    {
        var currentUserData = vm?.profile?.UserInformation?.Address.CustomData;
        var costCenter = GetUserData(currentUserData, "CostCenter");
        if ($.isEmptyObject(costCenter) === false)
        {
             console.log ("CostCenter detected: " + JSON.stringify(costCenter));
             vm.currentShipment.Packages[vm.packageIndex].ShipperReference = costCenter.Value;
             $('[ng-model="vm.currentShipment.Packages[vm.packageIndex].ShipperReference"]').prop( "disabled", true );
        }
        else
        {
            console.log ("Error: CostCenter not found");
        }
    }

// Source: Applied Material Inc. (AMATDesktop)_new_1769.cs
// userDataOnlyHistoryView(searchCriteria) // returns void
function userDataOnlyHistoryView(searchCriteria)
{
    //var userContext;
    //userContext = GetUserContext(userContext);	
    
    searchCriteria.WhereClauses.push({ FieldName: 'UserId', FieldValue: client.userContext.UserId, Operator: 0 });
}

// this.PreSearchHistory(searchCriteria) // returns void
this.PreSearchHistory = function (searchCriteria) {
	debugger;
    userDataOnlyHistoryView(searchCriteria);
};

// Source: APPNEXUS (APPNEXUS)_Appnexus Business Rule_73.cs
// get UserRef1() // returns the user's Custom1 value from OriginAddress custom data
   get UserRef1() { return this.GetCustom("Custom1", "User"); }

// GetCustom(fieldName, customDataType) // returns a matching custom field value from User or To custom data
   GetCustom(fieldName, customDataType) {
      var returnValue = "";
      var customData = null;
      if (customDataType === "User") customData = this.ShipmentRequest.PackageDefaults.OriginAddress.CustomData;
      if (customDataType === "To") customData = this.ShipmentRequest.PackageDefaults.Consignee.CustomData;

      if (customData) {
         customData.forEach(function (customField) {
            if (fieldName === customField.Key) returnValue = customField.Value;
         })
      }
      return returnValue;
   }

// Source: ASEA (ASEA_DOMESTIC)_ASEA CBR_1861.cs
// AssignCustomEvents() // returns void
this.AssignCustomEvents = function(){
        $("#btnCustomBatchProcessing").on( "click", function() {
        debugger;
            var userMethodParams = {"ActionMessage": "CustomBatchProcessing"};
            var data = { Data: JSON.stringify(userMethodParams), UserContext: client.userContext };
            var shipExecServiceUrl = client.config.ShipExecServiceUrl;
            var authorizationToken = client.getAuthorizationToken();
            $.post({ url: shipExecServiceUrl + '/' + 'UserMethod', data : data, async:false, headers: authorizationToken });
        });     
    }

// getUserContext() // returns void
getUserContext: function () {
            var userContextUrl = this.config.ShipExecServiceUrl.replace('ShippingService', 'usercontext/GET');
            var authorizationToken = this.getAuthorizationToken();
            this.userContext = $.get({ url: userContextUrl, headers: authorizationToken, async: false }).responseJSON;
        }

// Source: Avery Dennison (Avery)_Avery Reference Rule_28.cs
// GetShipment(shipmentRequest) // returns a Shipment helper object with user/to custom-data accessors
function GetShipment(shipmentRequest) {

        class Shipment {
           constructor(shipmentRequest) {
              this.ShipmentRequest = shipmentRequest;
           }

           get UserRef1() { return this.GetCustom("Custom1", "User"); }
           get UserRef2() { return this.GetCustom("Custom2", "User"); }
           get UserRef3() { return this.GetCustom("Custom3", "User"); }
           get UserRef4() { return this.GetCustom("Custom4", "User"); }
           get UserRef5() { return this.GetCustom("Custom5", "User"); }
           get UserRef6() { return this.GetCustom("Custom6", "User"); }
           get UserRef7() { return this.GetCustom("Custom7", "User"); }
           get UserRef8() { return this.GetCustom("Custom8", "User"); }
           get UserRef9() { return this.GetCustom("Custom9", "User"); }
           get UserRef10() { return this.GetCustom("Custom10", "User"); }

           get ToRef1() { return this.GetCustom("Custom1", "To"); }
           get ToRef2() { return this.GetCustom("Custom2", "To"); }
           get ToRef3() { return this.GetCustom("Custom3", "To"); }
           get ToRef4() { return this.GetCustom("Custom4", "To"); }
           get ToRef5() { return this.GetCustom("Custom5", "To"); }
           get ToRef6() { return this.GetCustom("Custom6", "To"); }
           get ToRef7() { return this.GetCustom("Custom7", "To"); }
           get ToRef8() { return this.GetCustom("Custom8", "To"); }
           get ToRef9() { return this.GetCustom("Custom9", "To"); }
           get ToRef10() { return this.GetCustom("Custom10", "To"); }

           GetCustom(fieldName, customDataType) {
              var returnValue = "";
              var customData = null;
              if (customDataType === "User") customData = this.ShipmentRequest.PackageDefaults.OriginAddress.CustomData;
              if (customDataType === "To") customData = this.ShipmentRequest.PackageDefaults.Consignee.CustomData;

              if (customData) {
                 customData.forEach(function (customField) {
                    if (fieldName === customField.Key) returnValue = customField.Value;
                 })
              }
              return returnValue;
           }
        }

        return new Shipment(shipmentRequest);
     }

// Source: Barnes & Noble Education, Inc. (BNEDUCAT)_BarnesAndNoble Client BR_1588.cs
// this.PreSearchHistory(searchCriteria) // returns void
    this.PreSearchHistory = function(searchCriteria) {
        searchCriteria.WhereClauses.push({ FieldName: 'UserId', FieldValue: userContext.UserId, Operator: 5 });
    }

// function getUserContext() // returns void
    function getUserContext() {
        if (userContext == undefined) {
            $.get({ url: "api/usercontext/GET", async: false }).done(function (data) {
                userContext = data;
            });
        }
    }

// Source: BealCo (BEALCO)_Default Demo CBR_2435.cs
// SetClientContext() // returns void
SetClientContext: () =>
      {
        $.get(URL_USER_CONTEXT, (data) => 
        {
          AJAX_SE23.CompanyId = data.CompanyId;
          AJAX_SE23.UserId = data.UserId;
        })
      },

// GetClientContext() // returns { CompanyId, UserId }
GetClientContext: () =>
      {
        return { CompanyId: AJAX_SE23.CompanyId, UserId: AJAX_SE23.UserId };
      },

// MakeUserMethodRequest(RequestObject) // returns Promise
MakeUserMethodRequest: (RequestObject) =>
      {
        const getConfigObject = () =>
        {
          return $.get('config.json').then((jsonConfig) =>
          {
            const urlService = jsonConfig.ShipExecServiceUrl;
            const tokenAuth = { 'Authorization': `Bearer ${JSON.parse(window.localStorage.getItem("TCToken")).access_token}`};

            let m = {};
            m.UserContext = AJAX_SE23.GetClientContext();
            m.Data = JSON.stringify(RequestObject);

            return { url: urlService + '/UserMethod', headers: tokenAuth, data: m };
          });
        }

        return getConfigObject().then((jsonData) => 
        {
          return $.post(jsonData).done((postResult) => postResult);
        });
      }

// Source: BHARTIYA HANDICRAFTS (BHARTIYAHCFT)_ISMEA_FTP_1584.cs
// getUserContext() // returns void
function getUserContext() {
        debugger;
        if (userContext === undefined) {
            $.get({ url: "api/usercontext/GET", async:false })
            .done(function (data) {
               userContext = data;
            })
        }
    }

// Source: Boston Scientific (BOSTON)_Boston Coyol Client Business Rules_2474.cs
// GetShipFromAddresses() // returns filtered ShipFromAddressBook.AddressBookEntries based on sf_AddressType
this.GetShipFromAddresses = function(){
    var payloadShipFromAddress = {
        "Skip": 0,
        "Take": 0,
        "WhereClauses": [{
           "FieldName": "Code",
                    "FieldValue": sf_AddressType,   //'ze-'
                    "Operator": 6
                }]
    };
 
    var sfdata = {
        CompanyId: client.userContext.CompanyId,
        addressBookIds: shipFromAddressBookId,
        SearchCriteria: payloadShipFromAddress
    };
   
    ShipFromAddressBook = client.thinClientAPIRequest('GetAddressBookEntries_AddressBook', sfdata, false).responseJSON;
    
     //debugger;
    
    for(let i = 0; i < ShipFromAddressBook.AddressBookEntries.length; i++){
    
        ShipFromAddressBook.AddressBookEntries[i].Code = ShipFromAddressBook.AddressBookEntries[i].Code.replace("&amp;",'&');
        ShipFromAddressBook.AddressBookEntries[i].Company = ShipFromAddressBook.AddressBookEntries[i].Company.replace("&amp;",'&');
   }


    console.log(ShipFromAddressBook.AddressBookEntries);
    
    return ShipFromAddressBook.AddressBookEntries;
};

// Source: Boston Scientific (BOSTON)_Boston Heredia Client Business Rules_2475.cs
// this.GetShipFromAddresses() // returns the filtered ship-from address book entries for the current user context
this.GetShipFromAddresses = function(){
    var payloadShipFromAddress = {
        "Skip": 0,
        "Take": 0,
        "WhereClauses": [{
           "FieldName": "Code",
                    "FieldValue": sf_AddressType,   //'ze-'
                    "Operator": 6
                }]
    };
 
    var sfdata = {
        CompanyId: client.userContext.CompanyId,
        addressBookIds: shipFromAddressBookId,
        SearchCriteria: payloadShipFromAddress
    };
   
    ShipFromAddressBook = client.thinClientAPIRequest('GetAddressBookEntries_AddressBook', sfdata, false).responseJSON;
    
     //debugger;
    
    for(let i = 0; i < ShipFromAddressBook.AddressBookEntries.length; i++){
    
        ShipFromAddressBook.AddressBookEntries[i].Code = ShipFromAddressBook.AddressBookEntries[i].Code.replace("&amp;",'&');
        ShipFromAddressBook.AddressBookEntries[i].Company = ShipFromAddressBook.AddressBookEntries[i].Company.replace("&amp;",'&');
   }


    console.log(ShipFromAddressBook.AddressBookEntries);
    
    return ShipFromAddressBook.AddressBookEntries;
};

// Source: Boston Scientific (BOSTON)_Boston_MULTI_2485.cs
// this.GetShipFromAddresses() // returns the filtered ship-from address book entries for the user context
this.GetShipFromAddresses = function(){
    var payloadShipFromAddress = {
        "Skip": 0,
        "Take": 0,
        "WhereClauses": [{
           "FieldName": "Code",
                    "FieldValue": sf_AddressType,   //'ze-'
                    "Operator": 6
                }]
    };
 
    var sfdata = {
        CompanyId: client.userContext.CompanyId,
        addressBookIds: shipFromAddressBookId,
        SearchCriteria: payloadShipFromAddress
    };
   
    ShipFromAddressBook = client.thinClientAPIRequest('GetAddressBookEntries_AddressBook', sfdata, false).responseJSON;
    
     //debugger;
    
    for(let i = 0; i < ShipFromAddressBook.AddressBookEntries.length; i++){
    
        ShipFromAddressBook.AddressBookEntries[i].Code = ShipFromAddressBook.AddressBookEntries[i].Code.replace("&amp;",'&');
        ShipFromAddressBook.AddressBookEntries[i].Company = ShipFromAddressBook.AddressBookEntries[i].Company.replace("&amp;",'&');
   }


    console.log(ShipFromAddressBook.AddressBookEntries);
    
    return ShipFromAddressBook.AddressBookEntries;
};

// this.GetShipToAddresses() // returns the ship-to address book entries for the user context
this.GetShipToAddresses = function(){
    var payloadShipToAddress = {
        "Skip": 0,
        "Take": 10,
        "WhereClauses": [{
            "FieldName": "Group",
            "FieldValue": "3",
            "Operator": 0
        }]
    };
    
    var data = {
        CompanyId: client.userContext.CompanyId,
        addressBookIds: shipToAddressBookId,
        SearchCriteria: payloadShipToAddress
    };
    //console.log(data);
  
    ShipToAddressBook = client.thinClientAPIRequest('GetAddressBookEntries_AddressBook', data, false).responseJSON;

    console.log(ShipToAddressBook.AddressBookEntries);
    
    return ShipToAddressBook.AddressBookEntries;
            
};

// function serviceOptions(profile) // returns the filtered list of services allowed by user custom data
    function serviceOptions(profile) {
        //debugger;
        var filterServices = [];
        var customData3 = uPS_Service.split(',');
        for (var item of customData3) {
            var symbol = getServiceSymbol(item.toLowerCase());
            for (var service of profile.Services) {
                if (service.Symbol === symbol) {
                    service.Name = service.Name.replace('(UPS Adapter)','').trim();
                    if (service.Name.includes("Expedited")){
                        service.Name = service.Name + " (MAIN SERVICE)";
                    }
                    if (service.Name.includes("Saver")){
                        service.Name = service.Name + " (To Heredia or Only with Boston's Authorization)";
                    }
                    filterServices.push(service);
                    break;
                }
            }
        }
        return filterServices;
        function getServiceSymbol(code) {
            if (code == '2dn') return 'UPSAPI.UPS.2DA'
            else if (code == '3dd') return 'UPSAPI.UPS.3DS'
            else if (code == 'exp') return 'UPSAPI.UPS.EXPEDITED'
            else if (code == 'ess') return 'UPSAPI.UPS.EXPRESS'
            else if (code == 'nda') return 'UPSAPI.UPS.NDA'
            else if (code == 'dae') return 'UPSAPI.UPS.NDAEAM'
            else if (code == 'grn') return 'UPSAPI.UPS.GND'
            else if (code == 'uwe') return 'UPSAPI.UPS.EXPEDITED'
            else if (code == 'wep') return 'UPSAPI.UPS.EXPRESSSP'
            else if (code == 'swe') return 'UPSAPI.UPS.EXPRESS'
            else if (code == 'uws') return 'UPSAPI.UPS.SAVER'
            else if (code == 'std') return 'UPSAPI.UPS.STANDARD'
        }
        
    }

// Source: BULGARI (BULGARI)_Bulgari Reference Rules 2_1576.cs
// GetUserContext() // returns the current user context object
    this.GetUserContext = function () {
        if (this.vm.userContext) return this.vm.userContext;
        if (this.vm.UserInformation) return this.vm.UserInformation;
    }

// GetAddressBookEntries(addressBookName, context) // returns an array of address book entries
    this.GetAddressBookEntries = function (addressBookName, context) {
        var searchCriteria = { WhereClauses: [{ FieldName: "Name", FieldValue: addressBookName, Operator: 0 }] };
        var data = { SearchCriteria: searchCriteria, CompanyId: context.CompanyId, SiteId: context.SiteId, UserId: context.UserId };
        var response = this.thinClientAPIRequest('GetAddressBooks', data, false).responseJSON;
        if (response.TotalRecords == 0) {
            var addressBookEntry = { Code: 'Error: AddressBook "' + addressBookName + '" Not Found' };
            return [addressBookEntry];
        } else {
            var addressBookId = response.AddressBooks[0].Id;
            searchCriteria = { OrderByClauses: [{ FieldName: "Code", Direction: "asc", FieldType: 3 }] };
            data = { AddressBookIds: [addressBookId], SearchCriteria: searchCriteria, CompanyId: context.CompanyId, SiteId: context.SiteId, UserId: context.UserId };
            response = this.thinClientAPIRequest('GetAddressBookEntries', data, false).responseJSON;
            if (response.TotalRecords > 0) {
                return response.AddressBookEntries;
            }
        }
    }

// Source: Bulgari MX (BULGARIMX)_BulgariMx Reference Rules_1778.cs
// GetUserContext() // returns the current user context object from vm.userContext or vm.UserInformation
    this.GetUserContext = function () {
        if (this.vm.userContext) return this.vm.userContext;
        if (this.vm.UserInformation) return this.vm.UserInformation;
    }

// GetAddressBookEntries(addressBookName, context) // returns address book entries filtered by CompanyId, SiteId, and UserId
    this.GetAddressBookEntries = function (addressBookName, context) {
        var searchCriteria = { WhereClauses: [{ FieldName: "Name", FieldValue: addressBookName, Operator: 0 }] };
        var data = { SearchCriteria: searchCriteria, CompanyId: context.CompanyId, SiteId: context.SiteId, UserId: context.UserId };
        var response = this.thinClientAPIRequest('GetAddressBooks', data, false).responseJSON;
        if (response.TotalRecords == 0) {
            var addressBookEntry = { Code: 'Error: AddressBook "' + addressBookName + '" Not Found' };
            return [addressBookEntry];
        } else {
            var addressBookId = response.AddressBooks[0].Id;
            searchCriteria = { OrderByClauses: [{ FieldName: "Code", Direction: "asc", FieldType: 3 }] };
            data = { AddressBookIds: [addressBookId], SearchCriteria: searchCriteria, CompanyId: context.CompanyId, SiteId: context.SiteId, UserId: context.UserId };
            response = this.thinClientAPIRequest('GetAddressBookEntries', data, false).responseJSON;
            if (response.TotalRecords > 0) {
                return response.AddressBookEntries;
            }
        }
    }

// Source: Carrier Misroute (CMR) (CMR)_Client Business Rules_2487.cs
// getShipperCustomers(shippers) // returns an array of customer options filtered/mapped from shipper custom data
            function getShipperCustomers(shippers) {
                var shipper = shippers.find(shipper => shipper.Symbol == vm.shipping.slic);
                if (shipper == undefined)
                     return [{ name: '--- No Shipper Configured ---', disabled: true }];

                var customData = shipper.CustomData.filter(c => c.Key.toUpperCase() == "CUSTOMER");
                var customers = customData.map(function(c) {
                    var customer = c.Value.toUpperCase();

                    if (customer == 'AMAZON')
                        return { name: "Amazon", value: customer, hasLoad: true, hasReturn: true, autoShip: true };
                    else if (customer == 'UPS')
                        return { name: "UPS", value: customer, hasCameraImage: true, hasFileImage: true, autoShip: false };
                    else if (customer == 'DEMO')
                        return { name: "Demo", value: customer, hasLoad: false, hasReturn: true, hasCameraImage: true, hasFileImage: false, autoShip: false };
                    else if (customer == 'FANATICS')
                        return { name: "Fanatics", value: customer, hasReturn: true, hasCameraImage: true, autoShip: false };
                    else
                        return { name: `--- ${customer} ---`, disabled: true };
                });

                customers.sort(function(a, b) {
                    if (a.disabled && !b.disabled) return 1;
                    if (!a.disabled && b.disabled) return -1;
                    return a.name.localeCompare(b.name);
                });
                return [{ name: '--- Select Customer Option ---', selector: 'select[ng-model="vm.shipping.customer"]', disabled: true }].concat(customers);
            }

// activate() // returns void
            function activate() {
                if (vm.profile.Name != vm.thinClient.adminProfileName) {
                    vm.hideButtonExport = true;

                    let reports = [ 'Default', 'Detailed Report', 'Void Report', 'Summary Report' ];
                    vm.reports = vm.reports.filter(function(report) {
                        report.TemplateHtml = report.TemplateHtml.replace(/<span.*glyphicon-search.*<\/span>/g, '');
                        return reports.includes(report.Name);
                    });
                }
            }

// selectReportChange(event) // returns void
            function selectReportChange(event) {
                if (vm.report?.Name == 'Summary Report') {
                    vm.hideButtonExport = true;
                    vm.history.carrierAcceptTableContainer = {
                        'height': $('.panel-body').height() + 'px',
                        'overflow-y': 'auto'
                    };

                    let payload = {
                        Action: 'summaryReport',
                        Data: {
                            startDate: vm.dtstart.toLocaleDateString('en-CA'),
                            endDate: vm.dtend.toLocaleDateString('en-CA'),
                            sites: getSites()
                        }
                    };
                    let data = { UserContext: undefined, Data: JSON.stringify(payload) };
                    thinClient.httpClient('UserMethod', data).then(function (ret) {
                        debugger;
                        vm.history.carrierAcceptReport = JSON.parse(atob(ret.Data));
                        vm.thinClient.loadData.show();
                    });
                } else {
                    vm.hideButtonExport = false;
                }

                function getSites() {
                    let sites = vm.UserInformation.SiteId ? vm.sites.filter(function(site) { return site.Id == vm.UserInformation.SiteId;}) : vm.sites;
                    return sites.map(function(site) { return { name: site.Name }; });
                }
            }

// Source: Childrens Creative Learning Centers (KLC)_KLC_Business_Rules_68.cs
// GetShipment(shipmentRequest) // returns a Shipment object
      function GetShipment(shipmentRequest) {

         class Shipment {
            constructor(shipmentRequest) {
               this.ShipmentRequest = shipmentRequest;
            }

            get UserRef1() { return this.GetCustom("Custom1", "User"); }
            get UserRef2() { return this.GetCustom("Custom2", "User"); }
            get UserRef3() { return this.GetCustom("Custom3", "User"); }
            get UserRef4() { return this.GetCustom("Custom4", "User"); }
            get UserRef5() { return this.GetCustom("Custom5", "User"); }
            get UserRef6() { return this.GetCustom("Custom6", "User"); }
            get UserRef7() { return this.GetCustom("Custom7", "User"); }
            get UserRef8() { return this.GetCustom("Custom8", "User"); }
            get UserRef9() { return this.GetCustom("Custom9", "User"); }
            get UserRef10() { return this.GetCustom("Custom10", "User"); }

            get ToRef1() { return this.GetCustom("Custom1", "To"); }
            get ToRef2() { return this.GetCustom("Custom2", "To"); }
            get ToRef3() { return this.GetCustom("Custom3", "To"); }
            get ToRef4() { return this.GetCustom("Custom4", "To"); }
            get ToRef5() { return this.GetCustom("Custom5", "To"); }
            get ToRef6() { return this.GetCustom("Custom6", "To"); }
            get ToRef7() { return this.GetCustom("Custom7", "To"); }
            get ToRef8() { return this.GetCustom("Custom8", "To"); }
            get ToRef9() { return this.GetCustom("Custom9", "To"); }
            get ToRef10() { return this.GetCustom("Custom10", "To"); }

            GetCustom(fieldName, customDataType) {
               var returnValue = "";
               var customData = null;
               if (customDataType === "User") customData = this.ShipmentRequest.PackageDefaults.OriginAddress.CustomData;
               if (customDataType === "To") customData = this.ShipmentRequest.PackageDefaults.Consignee.CustomData;

               if (customData) {
                  customData.forEach(function (customField) {
                     if (fieldName === customField.Key) returnValue = customField.Value;
                  })
               }
               return returnValue;
            }
         }
         return new Shipment(shipmentRequest);
      }

// GetShipment(shipmentRequest) // returns a Shipment object
     function GetShipment(shipmentRequest) {

        class Shipment {
           constructor(shipmentRequest) {
              this.ShipmentRequest = shipmentRequest;
           }

           get UserRef1() { return this.GetCustom("Custom1", "User"); }
           get UserRef2() { return this.GetCustom("Custom2", "User"); }
           get UserRef3() { return this.GetCustom("Custom3", "User"); }
           get UserRef4() { return this.GetCustom("Custom4", "User"); }
           get UserRef5() { return this.GetCustom("Custom5", "User"); }
           get UserRef6() { return this.GetCustom("Custom6", "User"); }
           get UserRef7() { return this.GetCustom("Custom7", "User"); }
           get UserRef8() { return this.GetCustom("Custom8", "User"); }
           get UserRef9() { return this.GetCustom("Custom9", "User"); }
           get UserRef10() { return this.GetCustom("Custom10", "User"); }

           get ToRef1() { return this.GetCustom("Custom1", "To"); }
           get ToRef2() { return this.GetCustom("Custom2", "To"); }
           get ToRef3() { return this.GetCustom("Custom3", "To"); }
           get ToRef4() { return this.GetCustom("Custom4", "To"); }
           get ToRef5() { return this.GetCustom("Custom5", "To"); }
           get ToRef6() { return this.GetCustom("Custom6", "To"); }
           get ToRef7() { return this.GetCustom("Custom7", "To"); }
           get ToRef8() { return this.GetCustom("Custom8", "To"); }
           get ToRef9() { return this.GetCustom("Custom9", "To"); }
           get ToRef10() { return this.GetCustom("Custom10", "To"); }

           GetCustom(fieldName, customDataType) {
              var returnValue = "";
              var customData = null;
              if (customDataType === "User") customData = this.ShipmentRequest.PackageDefaults.OriginAddress.CustomData;
              if (customDataType === "To") customData = this.ShipmentRequest.PackageDefaults.Consignee.CustomData;
              if (customData) {
                 customData.forEach(function (customField) {
                    if (fieldName === customField.Key) returnValue = customField.Value;
                 })
              }
             // alert("before return inside this add pachage" + returnValue);
              return returnValue;
           }
        }
        return new Shipment(shipmentRequest);
     }

// Source: Chrysler (Chrysler)_Chrysler Reference Rule_26.cs
// userDataOnlyHistoryView(searchCriteria) // returns void
function userDataOnlyHistoryView(searchCriteria)
    {
    //    var userContext;
    //    userContext = GetUserContext(userContext);
    //    searchCriteria.WhereClauses.push({ FieldName: 'UserId', FieldValue: userContext.UserId, Operator: 0 });
    }

// Source: Clarios (CLRS)_Clarios_Rule_1705.cs
// function userContext() // returns void
     function userContext() {
         
         var UserContextmethod,GetClientProfilemethod,authorizationToken,getCompanyId,methodParams,getCarriersList,options;
             UserContextmethod = 'api/usercontext/GET';
             GetClientProfilemethod='api/ShippingService/GetClientProfile';
             authorizationToken = clientService.authorizationToken();
             getCompanyId = $.get({url: clientService.config.ApiUrl + '/' + UserContextmethod, headers: authorizationToken, async: false}).responseJSON;
             userContextDetails=getCompanyId;
             methodParams = {"CompanyId":getCompanyId.CompanyId};
             getCarriersList =  $.post({url: clientService.config.ApiUrl + '/' + GetClientProfilemethod, data: methodParams, async: false, headers: authorizationToken }).responseJSON;
             options = '<option  class="ng-binding" selected="selected">Select Carrier...</option>';
     
     
             AllCarriersList=getCarriersList.Profile.Carriers;
             AllShipperList=getCarriersList.Profile.Shippers;
             
            for (var i = 0; i < getCarriersList.Profile.Carriers.length; i++) {
               
                options += '<option label="' + getCarriersList.Profile.Carriers[i].Name + '"value="' + getCarriersList.Profile.Carriers[i].Id + '" >' + getCarriersList.Profile.Carriers[i].Name + '</option>';
            }
            $('#CarriersList').html(options);
          
                    if($('#CarriersList :selected').text()=='Select Carrier...')
                    {
                        $('#btnCloseAllShippers').prop('disabled', true);
                    }else
                    {
                        $('#btnCloseAllShippers').prop('disabled', false);
                    }
          
        }

// Source: Clarios (CLRS)_Clarios_Rule_Paperless_2440.cs
// function userContext() // returns void
function userContext() {

        var UserContextmethod, GetClientProfilemethod, authorizationToken, getCompanyId, methodParams, getCarriersList, options;
        UserContextmethod = 'api/usercontext/GET';
        GetClientProfilemethod = 'api/ShippingService/GetClientProfile';
        authorizationToken = clientService.authorizationToken();
        getCompanyId = $.get({ url: clientService.config.ApiUrl + '/' + UserContextmethod, headers: authorizationToken, async: false }).responseJSON;
        userContextDetails = getCompanyId;
        methodParams = { "CompanyId": getCompanyId.CompanyId };
        getCarriersList = $.post({ url: clientService.config.ApiUrl + '/' + GetClientProfilemethod, data: methodParams, async: false, headers: authorizationToken }).responseJSON;
        options = '<option  class="ng-binding" selected="selected">Select Carrier...</option>';


        AllCarriersList = getCarriersList.Profile.Carriers;
        AllShipperList = getCarriersList.Profile.Shippers;

        for (var i = 0; i < getCarriersList.Profile.Carriers.length; i++) {

            options += '<option label="' + getCarriersList.Profile.Carriers[i].Name + '"value="' + getCarriersList.Profile.Carriers[i].Id + '" >' + getCarriersList.Profile.Carriers[i].Name + '</option>';
        }
        $('#CarriersList').html(options);

        if ($('#CarriersList :selected').text() == 'Select Carrier...') {
            $('#btnCloseAllShippers').prop('disabled', true);
        } else {
            $('#btnCloseAllShippers').prop('disabled', false);
        }

    }

// getUserContext() // returns void
getUserContext: function () {
            var userContextUrl = this.config.ShipExecServiceUrl.replace('ShippingService', 'usercontext/GET');
            var authorizationToken = this.getAuthorizationToken();
            this.userContext = $.get({
                url: userContextUrl,
                headers: authorizationToken,
                async: false
            }).responseJSON;
        }

// Source: Conduent (CONDUENT)_Conduent_CBR_1659.cs
// getUserContext() // returns the current user context data
function getUserContext() {
        var result;
        $.get({ url: 'api/usercontext/GET', async: false }).done(function (data) { result = data; });
        return result;
    }

// Source: Consumer Health (CONSUMERHEALTH)_Glasko Smith BR_1711.cs
// PreSearchHistory(searchCriteria) // returns void
this.PreSearchHistory = function(searchCriteria) {
        var whereClauses = searchCriteria.WhereClauses;
        whereClauses.push({ FieldName: 'UserId', FieldValue: userContext.UserId, Operator: 0 });
    }

// getUserContext() // returns void
function getUserContext() {
        if (userContext == undefined) {
            userContext = ajaxGet('api/usercontext/GET');
        }
    }

// ajaxGet(url) // returns result
function ajaxGet(url) {
        var result;
        $.ajax({
            type: 'GET',
            url: url,
            async: false,
            success: function(response) {
                result = response;
            }
        });
        return result;
    }

// Source: COREBRIDGE (CORE)_Shipper Ref = Custom1_1815.cs
// function userDataOnlyHistoryView(searchCriteria) // returns void
function userDataOnlyHistoryView(searchCriteria) {
        var data_;
        data_ = GetUserContext_(data_);
        searchCriteria.WhereClauses.push({ FieldName: 'UserId', FieldValue: data_.UserId, Operator: 0 });
    }

// this.PreSearchHistory = function (searchCriteria) // returns void
this.PreSearchHistory = function (searchCriteria) {
        userDataOnlyHistoryView(searchCriteria);
    };

// Source: Edward Jones  (EdwardJones)_Edward Jones Reference Rules 2.0_2476.cs
// PreSearchHistory(searchCriteria) // returns undefined; mutates searchCriteria to add a user-context-based filter
    this.PreSearchHistory = function(searchCriteria) {
//        debugger;
        //var branchNo = getKeyValue('Custom3', userCustomData)
		var branchNo = client.getValueByKey('Custom3', this.vm.profile.UserInformation.Address.CustomData);
        if (branchNo) {
            searchCriteria.WhereClauses.push({ FieldName: 'MiscReference20', FieldValue: branchNo, Operator: 5 });
        }
    }

// Source: Edward Jones  (EdwardJones)_Edward Jones Reference Rules_4.cs
// PreSearchHistory(searchCriteria) // returns undefined
    this.PreSearchHistory = function(searchCriteria) {
//        debugger;
        //var branchNo = getKeyValue('Custom3', userCustomData)
		var branchNo = client.getValueByKey('Custom3', this.vm.profile.UserInformation.Address.CustomData);
        if (branchNo) {
            searchCriteria.WhereClauses.push({ FieldName: 'MiscReference20', FieldValue: branchNo, Operator: 5 });
        }
    }

// Source: Epiq Bankruptcy Services (EPIQ)_Epiq CBR with UserContext Fix 2.0_1805.cs
// GetUserContext() // returns a Promise resolving to the current user context data
    async GetUserContext()
    {
      let that = this;
      return $.ajax({
        method: 'GET',
        url: that.UserContextUrl,
        dataType: 'json',
        headers: that.GetRequestHeaders(),
        done: function(data)
        {
          that.UserContext = data;
          return data;
        }
      });
    }

// MakeUserMethodRequest(MethodName, MethodData, callback) // returns a promise for the AJAX request result
      MakeUserMethodRequest: async function(MethodName, MethodData, callback)
      {
        let defjam = $.Deferred();

        let m = {};
        m.ServerMethod = MethodName;
        m.MethodData = MethodData || null;

        let u = new User();
        //all my userMethod calls take two params, a ServerMethod: string, and MethodData: any

        var ajaxObject = {
          UserContext: u.UserContext || await u.GetUserContext(),
          Data: JSON.stringify(m)
        };

        $.ajax(
          {
            url: u.UserMethodUrl,
            method: 'POST',
            dataType: 'json',
            data: ajaxObject,
            headers: u.GetRequestHeaders()
          }).done(function(data)
          {

            if (data.ErrorCode == 0) { defjam.resolve(true); }
            //let t = BatchClass.ParseEncodedData(data).then(function(result)
            //{
            //  defjam.resolve(result);
            //});

          }).fail(function(xhr)
          {
            defjam.reject('Error in ajax call');
            console.log(xhr);
            console.log('Method name: ', MethodName);
            console.log('Method data: ', MethodData);
          });

        return defjam.promise();
      }

// Start(callback) // returns void/undefined after initializing user context and service URLs
    async Start(callback)
    {
      let that = this;
      if (this.UserInformation)
      {
        let u = {};
        u.CompanyId = this.UserInformation.CompanyId || null;
        u.UserId = this.UserInformation.UserId || null;
        u.SiteId = this.UserInformation.SitdId || null;
        this.UserContext = u;
      }

      //use localStorage for UserMethodUrl and UserContextUrl
      if (window.localStorage.getItem('cbrValues'))
      {
        let cbr = $.parseJSON(window.localStorage.getItem('cbrValues'));
        this.UserContextUrl = decodeURIComponent(cbr.UserContextUrl) || null;
        this.UserMethodUrl = decodeURIComponent(cbr.UserMethodUrl) || null;
      }

      //check for the two items that we need.  if they exist, no need to go further.
      if (this.UserContext && this.UserContext.CompanyId && this.UserMethodUrl)
      {
        if ($.isFunction(callback)) callback(true);
      }

      //nothing found in localStorage, add stuff.
      that.GetJsonConfig().then(function(data)
      {
        let _rootUrl = new URL(data.ShipExecServiceUrl).origin;

        that.UserContextUrl = _rootUrl + '/api/usercontext/GET';
        that.UserMethodUrl = _rootUrl + '/api/ShippingService/UserMethod';

        //write the values to local storage
        let cbr = {};
        cbr.UserContextUrl = encodeURIComponent(that.UserContextUrl);
        cbr.UserMethodUrl = encodeURIComponent(that.UserMethodUrl);
        window.localStorage.setItem('cbrValues', JSON.stringify(cbr));

        //this is actually a set/get for the class var
        that.GetUserContext().then(function() { if ($.isFunction(callback)) callback(true); });
      });
    }

// Source: Epiq Bankruptcy Services (EPIQ)_Epiq CBR with UserContext Fix_1792.cs
// MakeUserMethodRequest(MethodName, MethodData, callback) // returns promise
      MakeUserMethodRequest: async function(MethodName, MethodData, callback)
      {
        let defjam = $.Deferred();

        let m = {};
        m.ServerMethod = MethodName;
        m.MethodData = MethodData || null;

        let u = new User();
        //all my userMethod calls take two params, a ServerMethod: string, and MethodData: any

        var ajaxObject = {
          UserContext: u.UserContext || await u.GetUserContext(),
          Data: JSON.stringify(m)
        };

        $.ajax(
          {
            url: u.UserMethodUrl,
            method: 'POST',
            dataType: 'json',
            data: ajaxObject,
            headers: u.GetRequestHeaders()
          }).done(function(data)
          {

            let t = Tools.ParseEncodedData(data).then(function(result)
            {
              defjam.resolve(result);
            });

          }).fail(function(xhr)
          {
            defjam.reject('Error in ajax call');
            console.log(xhr);
            console.log('Method name: ', MethodName);
            console.log('Method data: ', MethodData);
          });

        return defjam.promise();
      }

// constructor(ViewModelUserInformation, callback) // returns instance initialized with optional user information
    constructor(ViewModelUserInformation, callback)
    {
      if (ViewModelUserInformation) this.UserInformation = ViewModelUserInformation;
      
      this.Start(callback);
    }

// Start(callback) // returns promise/async initialization result
    async Start(callback)
    {
      debugger;
      let that = this;
      if (this.UserInformation)
      {
        let u = {};
        u.CompanyId = this.UserInformation.CompanyId || null;
        u.UserId = this.UserInformation.UserId || null;
        u.SiteId = this.UserInformation.SitdId || null;
        this.UserContext = u;
      }

      //use localStorage for UserMethodUrl and UserContextUrl
      if (window.localStorage.getItem('cbrValues'))
      {
        let cbr = $.parseJSON(window.localStorage.getItem('cbrValues'));
        this.UserContextUrl = decodeURIComponent(cbr.UserContextUrl) || null;
        this.UserMethodUrl = decodeURIComponent(cbr.UserMethodUrl) || null;
      }

      //check for the two items that we need.  if they exist, no need to go further.
      if (this.UserContext && this.UserContext.CompanyId && this.UserMethodUrl)
      {
        if ($.isFunction(callback)) callback(true);
      }

      //nothing found in localStorage, add stuff.
      that.GetJsonConfig().then(function(data)
      {
        let _rootUrl = new URL(data.ShipExecServiceUrl).origin;

        that.UserContextUrl = _rootUrl + '/api/usercontext/GET';
        that.UserMethodUrl = _rootUrl + '/api/ShippingService/UserMethod';

        //write the values to local storage
        let cbr = {};
        cbr.UserContextUrl = encodeURIComponent(that.UserContextUrl);
        cbr.UserMethodUrl = encodeURIComponent(that.UserMethodUrl);
        window.localStorage.setItem('cbrValues', JSON.stringify(cbr));

        //this is actually a set/get for the class var
        that.GetUserContext().then(function() { if ($.isFunction(callback)) callback(true); });
      });
    }

// GetUserContext() // returns ajax promise resolving user context data
    async GetUserContext()
    {
      let that = this;
      return $.ajax({
        method: 'GET',
        url: that.UserContextUrl,
        dataType: 'json',
        headers: that.GetRequestHeaders(),
        done: function(data)
        {
          that.UserContext = data;
          return data;
        }
      });
    }

// Source: F Schumacher (FSCHUMAC)_Product Area (8942X4) CBR with PV_1816.cs
// SetCurrentUserContext(UserInformation) // returns void
function function ClientBusinessRules ()
{
  this.PageLoaded = function (location)
  {
    //console.log(this.vm.UserInformation);
    SessionData.SetCurrentUserContext(this.vm.UserInformation);

    if (window.location.hash == "#!/shipping")
    {
      var checkExist = setInterval(function ()
      {

        if (($) && ($('select[ng-model="vm.currentShipment.PackageDefaults.Shipper"]').find('option')))
        {
          /* Stop the timer. */
          clearInterval(checkExist);

        //   SessionData.SetServerDebugMode(true);
        //   Logger.Log({Source: 'PageLoad()', Message: 'Test of CBR page load'});

        }

      }, 100);
    }
  }

  this.PreShip = function (shipmentRequest, shipmentResponse)
  {
    /* Implicitly set the MiscReference7 here as sometimes the view model won't update in time. */
    shipmentRequest.Packages[0].MiscReference7 = $('input[type=text][ng-model="vm.currentShipment.Packages[vm.packageIndex].MiscReference7"]').val();
    PackVerify.ClosePackVerifyDialog();
  }

  this.PostLoad = function (loadValue, shipmentRequest)
  {
      
      console.log(shipmentRequest);

    /* If there is nothing on the scale, this won't return a value. */
    $('[ng-click="vm.getWeight()"]').trigger('click').promise().done(function ()
    {

      var checkExist = setInterval(function ()
      {

        /* Wait until the load comes back and populates the Country field. */
        if ($('[nameaddress="vm.currentShipment.PackageDefaults.Consignee"]').find('select[ng-model="nameaddress.Country"]').prop('selectedIndex') > 1)
        {

          /* Stop the timer. */
          clearInterval(checkExist);

          /* Don Beal (dbeal@ups.com) - 2021-12-09 */
          /* Commented as the attribute list is not needed with PackVerify. */

          // /* Deal with consolidated orders if we have them.  The function that creates this attr on the load input is in the Shipping template. */
          // /* I use the attribute as a lot of the form values reset once the load is completed. */
          // /* Set the Misc Ref 7 to the orderList that was passed from the calling function. */
          // if ($('input[type=text][ng-model="vm.loadValue"]').attr('orderList') && $('input[type=text][ng-model="vm.loadValue"]').attr('orderList').length > 0)
          // {
          //   $('input[type=text][ng-model="vm.currentShipment.Packages[vm.packageIndex].MiscReference7"]').val($('input[type=text][ng-model="vm.loadValue"]').attr('orderList'));
          //   $('input[type=text][ng-model="vm.currentShipment.Packages[vm.packageIndex].ShipperReference"]').val($('input[type=text][ng-model="vm.loadValue"]').attr('orderList'));

          //   /* Get rid of the attributes now. */
          //   $('input[type=text][ng-model="vm.loadValue"]').attr('orderList', '');
          // }

          if (shipmentRequest.PackageDefaults.Shipper == 'FSCO7F4175')
          {
            /* Set the default box type to 'BAG 1'.  The multiple props and attr are for older browsers. */
            $('select[ng-model="vm.currentShipment.Packages[vm.packageIndex].BoxType"] > option[label="SAMPLE BAG 1"]').prop({ 'selected': 'true', 'selected': true }).attr('selected', 'selected').trigger('change');
          }

          /* Don Beal (dbeal@ups.com) - 2021-12-09 */
          /* Commented as this was not enabled in production. */

          // /* Validate the address, this is only needed for 7F4175 and 8942X4 */
          // var CountryCodeString = $('[nameaddress="vm.currentShipment.PackageDefaults.Consignee"]').find('select[ng-model="nameaddress.Country"]').val();
          // if (shipmentRequest.PackageDefaults.Shipper == 'FSCO7F4175' || shipmentRequest.PackageDefaults.Shipper == 'FSCO8942X4' && CountryCodeString == "string:US")
          // {
          //   //$('[nameaddress="vm.currentShipment.PackageDefaults.Consignee"]').find('[ng-click="validate(nameaddress)"]').trigger('click');
          // }

          /* Change the service to UPS Ground if the package is over 1lb and using USPS FC */
          CheckWeightAndService($('input[type=text][ng-model="vm.currentShipment.Packages[vm.packageIndex].Weight.Amount"]').val());

          /* After loading an order show the Pack Verify dialog and fill out the columns. */
          HideLoader();
          PackVerify.ShowPackVerifyDialog(JSON.parse(shipmentRequest.PackageDefaults.UserData5), shipmentRequest.Packages[0].ShipperReference);

        }

      }, 150);
    });

  }

  this.NewShipment = function (shipmentRequest)
  {
      console.log(this.vm);
    SessionData.SetCurrentViewModel(this.vm);
    $('input[type=text][ng-model="vm.loadValue"]').focus();
  }

  // /* This will throw an exception, make sure to remove from PROD code. */
  // /* Use this to test the logging of client data to the server by assigning it to a button click or some other event. */
  // function TestServerLogging ()
  // {
  //   var i = 1;
  //   try
  //   {
  //     i.replace("-", "");
  //   }
  //   catch (error)
  //   {
  //     Logger.Log({ Source: GetCallingFunctionName(), Error: error });
  //   }
  // }

  function CheckWeightAndService (weightVal)
  {
    // /* Once the button is clicked, start a timer and wait for the weight to populate. */
    // /* If the weight is over 1 pound, switch the service from USPS FC to UPS Ground. */
    if ((weightVal > 1) && ($('select[ng-model="vm.selectedServices"]').find('option:selected').val() == "CONNECTSHIP_ENDICIA.USPS.FIRST"))
    {

      /* Deselect any currently selected options. */
      $('select[ng-model="vm.selectedServices"]').val([]).scrollTop(10);

      /* Select the ground option. */
      $('select[ng-model="vm.selectedServices"] > option[value="CONNECTSHIP_UPS.UPS.GND"]')
        .prop({ 'selected': 'true', 'selected': true })
        .attr('selected', 'selected')
        .trigger('change').promise().done(function ()
        {

        });
    }
  }

  /* This function is used to get the name of a function during error handling when option strict is true. */
  function GetCallingFunctionName ()
  {
    try
    {
      throw new Error();
    }
    catch (e)
    {
      try
      {
        return e.stack.split('at ')[3].split(' ')[0] + '()';
      }
      catch (e)
      {
        return '';
      }
    }
  }

  function ShowLoader ()
  {
    $('div.loading').removeClass('ng-hide').addClass('ng-show');
  }

  function HideLoader ()
  {
    $('div.loading').removeClass('ng-show').addClass('ng-hide');
  }

  function ShowOverlay ()
  {
    $('div.overlay').show();
  }

  function HideOverlay ()
  {
    $('div.overlay').hide();
  }

  /* Don Beal (dbeal@ups.com) 2021-11-03 */
  /* This needs to be at the bottom of the CSBR so it runs after everything else. */
  /* All event delegates should be set within the $(document).ready section */
  $(document).ready(function ()
  {
    try
    {
      /* All global CBR declarations should go here using $.globalEval(). */

      /* I moved all global vars into the classes using the 'let' construct so they are only exposed in that class and not leaked during user sessions. */
      /* If the SessionData vars are needed during debugging, add the following to the this.PageLoaded function. */
      /* this.vm.SessionData = SessionData; */
      /* sessionData = this.vm.SessionData; */
      /* AND */
      /* Add the following line to $(document).ready */
      /* $.globalEval('var sessionData;'); */

      /* Global ajax event handlers. */
      $.ajaxSetup({ global: true });

      $(document).ajaxStart(function (event, jqXHR, ajaxSettings)
      {
        ShowLoader();
      });

      $(document).ajaxStop(function ()
      {
        HideLoader();
      });

      $(document).ajaxComplete(function ()
      {
        HideLoader();
      });
      /* End global ajax event handlers. */

      /* Hide any error alerts when another element has focus. */
      $('body').delegate('*', 'focus', function (e) { $('div.alert').hide(); });

      $(document).delegate('body', 'keydown', function (e)
      {
        /* If the user presses F12, close the dialog and ship the package. */
        var keycode = (e.keyCode ? e.keyCode : e.which);
        if (keycode == 123 && $('#divModalPackVerify').is(':visible'))
        {
          PackVerify.ClosePackVerifyDialog();
        }
      });

      $('body').delegate('#inputScanCutTicket', 'keydown', function (e)
      {
        var keycode = (e.keyCode ? e.keyCode : e.which);
        if (keycode == 13)
        {
          PackVerify.ParseCutTicket($(this).val());
          /* Clear out the value of the text box. */
          $(this).val([]);
        }
      });

      $('body').delegate('#textOrderNumber', 'keyup', function (e)
      {
        var keycode = (e.keyCode ? e.keyCode : e.which);
        if (keycode == 13 | keycode == 9)
        {
          PackVerify.AddOrderNumbersToList();
        }
      });

      /* The two following delegates do the same thing, one reacts to the event when the 'Enter' key is pressed while the load value is populated, the other intercepts the 'Load' button click. */
      /* The loader will not show as part of the global ajax handlers as this event called from within the Angular controller. */
      $('body').delegate('#inputLoadValue', 'keydown', function (e)
      {
        var keycode = (e.keyCode ? e.keyCode : e.which);
        if (keycode == 13)
        {
          ShowLoader();
        }
      });

      $('body').delegate('input[type=button][ng-click="vm.load()"]', 'click', function (e)
      {
        ShowLoader();
      });

      /* If the user clicks on an alert, hide the loader and the alert. */
      $('body').delegate('div.alert', 'click mousedown', function ()
      {
        HideLoader();
        $(this).hide();
      });

      /* Remove .hasCustomError classes from any controls. */
      $('body').delegate('.form-control', 'keydown mousedown', function (e)
      {
        $(this).removeClass('hasCustomError');
      });

      $('body').delegate('#buttonVerifyPacking', 'click', function (e)
      {
        PackVerify.ClosePackVerifyDialog();
      });

      $('body').delegate('#buttonAddOrderNumbers', 'click', function (e)
      {
        PackVerify.AddOrderNumbersToList();
      });

      $('body').delegate('#divCurrentContainer span.glyphicon-minus', 'click', function (e)
      {
        PackVerify.RemoveCurrentBoxFromShipment();
      });

      $('body').delegate('#divCurrentContainer span.glyphicon-plus', 'click', function (e)
      {
        PackVerify.AddNewBoxToCurrentShipment();
      });

      $('body').delegate('div.table-box-contents table.table-custom span.glyphicon-minus', 'click', function (e)
      {
        PackVerify.RemoveThisTableRow(this);
      });

      $('body').delegate('span.glyphicon.get-weight', 'click', function (e)
      {
        PackVerify.GetCurrentWeightFromScale();
      });

      $('body').delegate('#divConsolidatedOrderList span.glyphicon-duplicate', 'click', function (e)
      {
        PackVerify.ShowConsolidatedOrderDialog();
      });

      $('body').delegate('#buttonSaveConsolidatedOrders', 'click', function (e)
      {
        PackVerify.SaveConsolidatedOrderNumbers();
      });

      $('body').delegate('#buttonShipAfterVerify', 'click', function (e)
      {
        //$('#divModalPackVerify').hide();
        PackVerify.ClosePackVerifyDialog();
        $('input[type=button][ng-click="vm.checkShip()"]').first().click();
      });

      $('body').delegate('#buttonPackVerifyNextContainer', 'click', function (e)
      {
        e.stopImmediatePropagation();
        e.preventDefault();
        e.stopPropagation();
        PackVerify.ShowContainerTable('next');

        /* Check button status. */
        $(this).attr('disabled', $('button[type=button][ng-click="vm.nextPackage();"]').attr('disabled'));
        $('#buttonPackVerifyPreviousContainer').attr('disabled', $('button[type=button][ng-click="vm.previousPackage();"]').attr('disabled'));
      });

      $('body').delegate('#buttonPackVerifyPreviousContainer', 'click', function (e)
      {
        e.stopImmediatePropagation();
        e.preventDefault();
        e.stopPropagation();
        PackVerify.ShowContainerTable('prev');

        /* Check button status. */
        $(this).attr('disabled', $('button[type=button][ng-click="vm.nextPackage();"]').attr('disabled'));
        $('#buttonPackVerifyNextContainer').attr('disabled', $('button[type=button][ng-click="vm.nextPackage();"]').attr('disabled'));
      });

      $('body').delegate('span.add-box-item', 'click', function (e)
      {
        try
        {
          var descString = $(this).closest('tr').find('td:eq(0)').text();
          var qtyInt = $(this).closest('tr').find('td:eq(3)').text();
          var singleArray = descString.split(" ", 1);

          var parseString = singleArray[0] + ' ' + qtyInt;

          PackVerify.ParseCutTicket(parseString);
        }
        catch (error)
        {
          Logger.Log({ Source: GetCallingFunctionName(), Error: error });
        }
      });

      $('body').delegate('#selectClonedBoxTypes', 'change', function (e)
      {
        /* Reset the border color, in case it was red from a previous error. */
        $(this).css('border-color', '#ccc');

        if ($(this).prop('selectedIndex') === 0)
        {
          $('span.dim-length').text('0');
          $('span.dim-height').text('0');
          $('span.dim-width').text('0');
          $('span.dim-weight').text('0');
          return false;
        }

        var currentSel = $(this).prop('selectedIndex');
        $('[ng-model="vm.currentShipment.Packages[vm.packageIndex].BoxType"]').prop('selectedIndex', currentSel).trigger('click').trigger('change');

        /* A new box has been selected, get the weight from the scale. */
        PackVerify.GetCurrentWeightFromScale();

        /* Create the data object of the visible container to hold the select value. */
        var containerBoxData = {};
        containerBoxData.SelectedIndex = $(this).find('option:selected').index();
        containerBoxData.SelectedText = $(this).find('option:selected').text();

        var currentContainer = $('div.table-box-contents table.table-custom:visible');
        $(currentContainer).data(containerBoxData);

        /* The vals could be the same, maybe clear out the val first, wait until it automatically changes, then run our function. */
        var checkExist = setInterval(function ()
        {
          if ($('input[type="text"][name="length"]').first().val().length > 0)
          {
            /* Stop the timer. */
            clearInterval(checkExist);
            var l = $('input[type="text"][name="length"]').first().val();
            var h = $('input[type="text"][name="height"]').first().val();
            var w = $('input[type="text"][name="width"]').first().val();

            if ($('#divModalPackVerify').is(':visible'))
            {
              $('span.dim-length').text(l);
              $('span.dim-height').text(h);
              $('span.dim-width').text(w);
            }
          }

        }, 100);

        PackVerify.CheckForCompletedPacking();

        $('#inputScanCutTicket').focus();
      });

      /*
        The service select box will present in non-alphabetical order so sort it accordingly.
        Need to wait until the services are populated to do this so add an interval.
        Make sure this stays at the BOTTOM of the $(document).ready function.

        Once the sort is completed the callback will focus the cursor on the 'Load' input and the default shipper will be selected.
      */
      var checkExist = setInterval(function ()
      {
        if ($('select[ng-model="vm.selectedServices"]').find('option').length > 0)
        {
          /* Stop the timer. */
          clearInterval(checkExist);

          var selServiceOptions = $('select[ng-model="vm.selectedServices"]').find('option');

          selServiceOptions.detach().sort(function (a, b)
          {
            return ($(a).text() > $(b).text()) ? 1 : (($(a).text() < $(b).text()) ? -1 : 0);
          });

          selServiceOptions.appendTo('select[ng-model="vm.selectedServices"]').promise().done(function (e)
          {
            /* The sorting is done, now select the default shipper and set the focus to the Load input box. */
            $('select[ng-model="vm.currentShipment.PackageDefaults.Shipper"]').find('option:eq(1)').prop(
              {
                'selected': 'selected',
                'selected': true
              }).change();

            /* Now set the focus to the 'Load' input box and select the first shipper in the list. */
            $('input[type=text][ng-model="vm.loadValue"]').focus();
            $('select[ng-model="vm.currentShipment.PackageDefaults.Shipper"]').find('option:eq(1)').prop({ 'selected': 'selected', 'selected': true }).change();
          });
        }

      }, 100);

    }
    catch (error)
    {
      Logger.Log({ Source: GetCallingFunctionName(), Error: error });
    }
  }); /* end $(document).ready(function () */

  var PackVerify = (function ()
  {
    /* Private methods and variables, accessible only to the PackVerify class. */
    /* Using 'let var = value;' here will be available to all functions in the class, as will 'var v = value;'. */
    /* NOTE:  If the functions have return values, they MUST be called with the class identifier, ie PackVerify.FunctionName */

    function UpdateItemCounts ()
    {
      var itemCount = ($('div.table-box-contents table.table-custom:visible').find('tr:visible').not('.empty-table').length - 1);
      $('span.box-pkg-count').text(itemCount);

      var unpackedCount = ($('#tablePackVerifyOrderDetails').find('tr.not-packed, tr.not-complete').length);
      $('span.unpacked-item-count').text(unpackedCount);

      $('#inputScanCutTicket').focus();
    }

    function UpdateContainerCounts ()
    {
      var totalContainers = $('div.table-box-contents table.table-custom').length;
      $('span.total-container-count').text(totalContainers);

      /* Get the index of the visible table.table-custom. */
      var currentContainer = ($('div.table-box-contents table.table-custom:visible').index() + 1);
      $('span.current-container-index').text(currentContainer);

      UpdateItemCounts();
    }

    function UpdateCommodities (PackageIndex)
    {
      /* Click the active tab, just to refresh the commodities.  This will only work on the first commodity added. */
      $('div.ui-tab-container > div.ng-isolate-scope > ul > li.active > a').click();

      if ($('#goods').find('div.ng-table-counts.btn-group:eq(' + PackageIndex + ')').length > 0)
      {
        /* Clicking the button below refreshes the commodities list in the UI. */
        /* Find the first button that does not have the .active class. */
        $('#goods').find('div.ng-table-counts.btn-group').find('button:not(.active):first').click();
      }
    }

    function ResetDivElements (CloseDialog)
    {
      /* Remove the items from the incoming orders table. */
      $('#tablePackVerifyOrderDetails').find('tr:gt(0)').remove();

      $('#tablePackVerifyCutTicketScanResults').find('tr:gt(1)').remove();
      $('#tablePackVerifyCutTicketScanResults').find('tr.empty-table').show();

      $('span.order-number-list').text('');
      $('span.pack-verify-instructions-detail').text('');

      $('#inputScanCutTicket').val([]);

      $('#divPackVerifyButtonGroup').find('input[type=button]').prop('disabled', 'disabled');

      UpdateContainerCounts();

      if (CloseDialog) { $('#divModalPackVerify').hide(); }
    }

    function GetRandomNumber (DigitCount)
    {
      return Math.floor(Math.pow(10, (DigitCount - 1)) + Math.random() * 9 * Math.pow(10, (DigitCount - 1)));
    }

    function GetTranslatedUnitOfMeasurement (sVal)
    {
      switch (sVal.toUpperCase())
      {
        case "EACH":
          return "EA";
        case "YARDS":
        case "YARD":
          return "YD";
        case "METER":
          return "M";
        case "SF":
          return "SFT";
        case "PAIR":
          return "PR";
        case "SR":
          return "ROL";
        default:
          return sVal;
      }
    }

    function CheckForBasicErrors ()
    {
      var i = 0;
      /* Sometimes the weight OR service won't come across from the api, check for valid values and paint the invalid inputs red. */
      var weightInput = $('input[ng-model="vm.currentShipment.Packages[vm.packageIndex].Weight.Amount"]');
      if (weightInput.val() == "") { $(weightInput).addClass('hasCustomError'); i += 1; }

      return i;
    }

    /* Public methods (accessible to entire CBR, prefixed with PackVerify.function). */
    /* The opening brace MUST be on the same line as the return statement. */
    /* With the exception of the last function in the return section, all functions need to have a comma ',' after the closing brace. */
    /* NOTE:  If the functions have returns, they MUST be called with the class identifier, ie PackVerify.FunctionName */
    return {

      ShowErrorAlert: function (ErrorMessage)
      {
        $('#divCustomAlert div.alert-message').text(ErrorMessage).closest('div.alert').show();
      },

      AddCommoditiesToPackage: function (packageIndex, commodityContent)
      {
        /* The commodityContent object has a number of items that can't be pushed to the shipmentRequest array. */
        /* Create a new object just with the items needed in the shipmentRequest.Packages[packageIndex].CommodityContents array. */
        try
        {
          var shipmentRequest = SessionData.GetCurrentViewModel().currentShipment;

          if (!shipmentRequest.Packages[packageIndex].CommodityContents)
          {
            shipmentRequest.Packages[packageIndex].CommodityContents = [];
          }
          shipmentRequest.Packages[packageIndex].CommodityContents.push(commodityContent);

          UpdateCommodities(packageIndex);

          /* If there are no commodities yet, the buttons will not exist to click. */
          /* As a workaround, trigger the .change event for the selectClonedBoxTypes select box.  This will trigger an update of the view model, and the commodities list. */
          /* Need to find a way to trigger the same view model update on the nth package in the shipment. */



          // if ($('#goods').find('div.ng-table-counts.btn-group:eq(' + packageIndex + ')').length === 0)
          // {
          //   console.log(packageIndex);
          //   console.log('No button group found.  No commodites loaded.');
          //   $('#selectClonedBoxTypes').trigger('change');
          // }
          // else
          // {
          //   /* Clicking the button below refreshes the commodities list in the UI. */
          //   /* Find the first button that does not have the .active class. */
          //   console.log('Button group found.  Commodities loaded');
          //   $('#goods').find('div.ng-table-counts.btn-group').find('button:not(.active):first').click();
          // }
        }
        catch (error)
        {
          Logger.Log({ Source: 'AddCommoditiesToPackage()', Error: error});
        }
      }, /* end AddCommoditiesToPackage */

      RemoveCommoditiesFromPackage: function (packageIndex, commodityContent)
      {
        try
        {
          var shipmentRequest = SessionData.GetCurrentViewModel().currentShipment;

          if (!shipmentRequest.Packages[packageIndex].CommodityContents)
          {
            shipmentRequest.Packages[packageIndex].CommodityContents = [];
          }
          shipmentRequest.Packages[packageIndex].CommodityContents.pop(commodityContent);

          /* Update the commodities by clicking on the active tab. */
          //$('div.ui-tab-container > div.ng-isolate-scope > ul > li.active > a').click();
          UpdateCommodities(packageIndex);
        }
        catch (error)
        {
          Logger.Log({ Source: 'RemoveCommoditiesFromPackage()', Error: error });
        }
      },

      ShowPackVerifyDialog: function (CommodityContents, ShipperReference)
      {
        /* Delete and re-create the individual container tables.
          <table class="table-custom" id="tablePackVerifyCutTicketScanResults">
            <tbody>
              <tr>
                <th>Order #</th>
                <th>Item #</th>
                <th>Qty</th>
                <th>Total</th>
                <th style="width: 32px;"></th>
              </tr>
              <tr class="empty-table">
                <td colspan="6" style="text-align: center;">No items placed in container.</td>
              </tr>
            </tbody>
          </table>
        */
        try
        {
          /* Get rid of any previous container tables, then create a new empty one. */
          /* This is more efficient than looking for possible existing table(s) and dealing with them. */
          $('div.table-box-contents').find('table').remove().promise().done(function (e)
          {
            var tableResults = $('<table />').addClass('table-custom').attr('id', 'tablePackVerifyCutTicketScanResults');

            $(tableResults).append($('<tr />')
              .append($('<th />').text('Order #'))
              .append($('<th />').text('Item #'))
              .append($('<th />').text('Qty'))
              .append($('<th />').text('Total'))
              .append($('<th />').css('width', '32px')));

            $(tableResults).append($('<tr />').addClass('empty-table')
              .append($('<td />')
                .attr('colspan', '6')
                .css('text-align', 'center')
                .text('No items placed in container.')));

            $('div.table-box-contents').append($(tableResults));

          });

          $('span.total-container-count').text('1');
          $('span.current-container-index').text('1');
          $('span.box-pkg-count').text('0');

          $('#divModalPackVerify').fadeIn(200).promise().done(function ()
          {
            // $('body').delegate('select[ng-model="vm.currentShipment.Packages[vm.packageIndex].BoxType"]', 'change', function (e)
            // {
            //   $('#selectClonedBoxTypes').first().prop('selectedIndex', $(this).prop('selectedIndex'));
            //   $('#selectClonedBoxTypes').first().trigger('change').trigger('click');
            // });

            var i = $('select[ng-model="vm.currentShipment.Packages[vm.packageIndex].BoxType"]').prop('selectedIndex');

            $('#selectClonedBoxTypes').prop('selectedIndex', i).trigger('change');
            $('span.order-number-list').text(ShipperReference);

            ShowOverlay();

            PackVerify.AddCommoditiesToTable(CommodityContents);

            $('#inputScanCutTicket').focus();

          });
        }
        catch (error)
        {
          Logger.Log({ Source: GetCallingFunctionName(), Error: error, Data: CommodityContents });
          HideOverlay();
        }
      },

      /* This is called when the user clicks the 'Done' button on the Pack Verify dialog. */
      ClosePackVerifyDialog: function ()
      {
        /* Need to reset all the elements on the page. */
        ResetDivElements(true);
        HideOverlay();
      },

      AddAllItemsToCurrentBox: function ()
      {
        var tableRows = $('#tablePackVerifyOrderDetails').find('tr.not-packed');

        $(tableRows).each(function (e)
        {
          AddRowItemToCurrentBox($(this));
        });
      },

      AddNewBoxToCurrentShipment: function ()
      {
        try
        {
          /* No box type selected, return out. */
          if ($('#selectClonedBoxTypes').prop('selectedIndex') === 0)
          {
            $('#selectClonedBoxTypes').css('border-color', 'crimson');
            return false;
          }

          /* Trigger the button on the shipping screen. */
          $('#buttonAddPackageToShipment').first().click();

          //need to clone the tablePackVerifyCutTicketScanResults object, fill it with details, append it to table-box-contents
          var newTable = $('<table />').addClass('table-custom').css('display', 'none');

          $(newTable).append($('<tr />')
            .append($('<th />').text('Order #'))
            .append($('<th />').text('Item #'))
            .append($('<th />').text('Qty'))
            .append($('<th />').text('Total'))
            .append($('<th />').css('width', '32px')));

          $(newTable).append($('<tr />').addClass('empty-table')
            .append($('<td />')
              .attr('colspan', '6')
              .css('text-align', 'center')
              .text('No items placed in container.')));

          /* Hide any visible tables, then show the new one. */
          $('div.table-box-contents').find('table.table-custom').hide();
          $('div.table-box-contents').append($(newTable).fadeIn().show());

          /* Reset the box type select element. */
          $('#selectClonedBoxTypes').prop('selectedIndex', 0).trigger('change');

          UpdateContainerCounts();
        }
        catch (error)
        {
          Logger.Log({ Source: GetCallingFunctionName(), Error: error });
        }
      },

      ParseCutTicket: function (inVal)
      {
        var splitArray = inVal.split(" ", 2);
        var itemNumber = splitArray[0];
        var itemQuantity = splitArray[1];
        var remainingQuantity, shippedQuantity;
        var matchingArray;

        try
        {
          /* Now find the table row that corresponds to the item number passed into the function. */
          var rowsArray = $('#tablePackVerifyOrderDetails').find('tr.not-packed, tr.not-complete');

          matchingArray = $.grep(rowsArray, function (pVal, pIndex)
          {
            remainingQuantity = parseFloat($(pVal).find('td:eq(3)').text());
            shippedQuantity = parseFloat($(pVal).find('td:eq(2)').text());
            return (($(pVal).text().split(" ")[0] == itemNumber) && (itemQuantity == remainingQuantity));
          })[0];

          /* The matchingArray will contain rows that match the item needed with the quantity needed. */
          /* If the array is empty, find rows that have enough material to use. */
          if ($.isEmptyObject(matchingArray))
          {
            matchingArray = $.grep(rowsArray, function (pVal, pIndex)
            {
              remainingQuantity = parseFloat($(pVal).find('td:eq(3)').text());
              shippedQuantity = parseFloat($(pVal).find('td:eq(2)').text());
              return (($(pVal).text().split(" ")[0] == itemNumber) && (itemQuantity < remainingQuantity));
            })[0];
          }

          /* If the matchingArray is still empty, then the user tried to scan something into the box that wasn't part of the order.  Raise an error. */
          if ($.isEmptyObject(matchingArray))
          {
            this.ShowErrorAlert('ERROR: Item ' + itemNumber + ' not found within currently loaded order(s).');
            return false;
          }

          $(matchingArray).data().ScanQuantity = itemQuantity;
          $(matchingArray).data().ItemNumber = itemNumber;

          this.AddRowItemToCurrentBox(matchingArray);
        }
        catch (error)
        {
          Logger.Log({ Source: 'ParseCutTicket()', Error: error });
        }
      },

      GetOrderInformationFromCustomer: function (OrderList, CallBack)
      {
        var dataObject = {
          ServerMethod: 'GetOrderInformation',
          OrderList: OrderList
        };

        var ajaxObject = {
          UserContext: SessionData.GetCurrentUserContext(),
          Data: JSON.stringify(dataObject)
        };

        var ajaxRequest = $.ajax(
        {
          url: 'api/ShippingService/UserMethod',
          method: 'POST',
          contentType: 'application/x-www-form-urlencoded; charset=UTF-8',
          dataType: 'json',
          data: ajaxObject
        });

        ajaxRequest.done(function (data)
        {
          var jsonData = JSON.parse(data.Data);

          for (var x = 0; x < jsonData.length; x++)
          {
            if (PackVerify.CheckForMatchingConsignee(jsonData[x]) === true)
            {
              PackVerify.AddCommoditiesToTable(jsonData[x].Details);
              PackVerify.UpdateReferencesWithOrderNumbers(jsonData[x].Header.Order_Number);
            }
            else
            {
              /* Add a note to the footer of the dialog. */
              $('#spanPackVerifyFooter').text('Could not add commodities for order ' + jsonData[x].Header.Order_Number);
            }
          }
        });

        ajaxRequest.fail(function (jqXHR, textStatus)
        {
          Logger.Log({ Source: GetCallingFunctionName(), Error: { name: 'AjaxError', message: jqXHR.ResponseText } });
        });

        ajaxRequest.always(function (jqXHR, textStatus, jqXHRError)
        {
          if (CallBack && typeof (CallBack) == "function")
          {
            CallBack();
          }
        });
      },

      AddCommoditiesToTable: function (CommodityContents)
      {
        /* Incoming CommodityContents Structure - ARRAY. */
        /*
          "COMMODITY_CONTENTS_QUANTITY": 2.0,
          "COMMODITY_CONTENTS_QUANTITY_UNIT_MEASURE": "EACH",
          "COMMODITY_CONTENTS_UNIT_VALUE": 0.01,
          "COMMODITY_CONTENTS_PRODUCT_CODE": "MS66184",
          "COMMODITY_CONTENTS_ORIGIN_COUNTRY": "US",
          "Order_Number": "50980336.4",
          "COMMODITY_CONTENTS_DESCRIPTION": "DONT FRET",
          "COMMODITY_CONTENTS_COMMENTS": null,
          "COMMODITY_CONTENTS_TARRIFFCODE1": null,
          "COMMODITY_CONTENTS_TARRIFCODE2": "",
          "COMMODITY_CONTENTS_AVERAGEWEIGHT": 0.0
        */
        try
        {
          for (var x = 0; x < CommodityContents.length; x++)
          {
            /* Map the CommodityContent object here that will be used on the server.  By matching the structure here, hoping to push it straight to the viewmodel without any translation. */
            var CommodityObject = {
              UnitWeight: { Amount: "1" },
              Quantity: CommodityContents[x].COMMODITY_CONTENTS_QUANTITY,
              ProductCode: CommodityContents[x].COMMODITY_CONTENTS_PRODUCT_CODE,
              UnitValue: { Amount: CommodityContents[x].COMMODITY_CONTENTS_UNIT_VALUE, Currency: 'USD' },
              QuantityUnitMeasure: GetTranslatedUnitOfMeasurement(CommodityContents[x].COMMODITY_CONTENTS_QUANTITY_UNIT_MEASURE),
              Description: CommodityContents[x].COMMODITY_CONTENTS_DESCRIPTION,
              OriginCountry: (CommodityContents[x].COMMODITY_CONTENTS_ORIGIN_COUNTRY.length > 0 ? CommodityContents[x].COMMODITY_CONTENTS_ORIGIN_COUNTRY : 'US'),
              ConsigneePostalCode: (CommodityContents[x].CONSIGNEE_POSTALCODE ? CommodityContents[x].CONSIGNEE_POSTALCODE.substring(0, 5) : null),
              EntryId: GetRandomNumber(8),
              OrderNumber: CommodityContents[x].Order_Number,
              HarmonizedCode: CommodityContents[x].COMMODITY_CONTENTS_TARRIFFCODE1,
              HarmonizedCode2: CommodityContents[x].COMMODITY_CONTENTS_TARRIFCODE2,
              AverageWeight: CommodityContents[x].COMMODITY_CONTENTS_AVERAGEWEIGHT,
              UserData1: CommodityContents[x].COMMODITY_CONTENTS_COMMENTS
            };

            $('#tablePackVerifyOrderDetails')
              .append($('<tr />').addClass('not-packed').attr('title', 'Order Number: ' + CommodityObject.OrderNumber)
                .append($('<td />').text(CommodityContents[x].COMMODITY_CONTENTS_PRODUCT_CODE + ' ' + CommodityContents[x].COMMODITY_CONTENTS_DESCRIPTION))
                .append($('<td />').text(CommodityContents[x].COMMODITY_CONTENTS_QUANTITY))
                .append($('<td />').text(0))
                .append($('<td />').text(CommodityContents[x].COMMODITY_CONTENTS_QUANTITY))
                .append($('<td />').append('<span class="glyphicon glyphicon-plus trigger-icon add-box-item" title="Add this item to the current box." style="color: green;"></span>'))
                .data(CommodityObject))
              .promise().done(function ()
              {
                UpdateItemCounts();
                HideLoader();
              })
          }
        }
        catch (error)
        {
          Logger.Log({ Source: 'AddCommoditiesToTable()', Error: error, Data: CommodityContents });
        }
      },

      CheckForMatchingConsignee: function (CustomerOrderObject)
      {
        try
        {
          var con = $('name-address[nameaddress="vm.currentShipment.PackageDefaults.Consignee"]');
          var a = {
            conCompany: $(con).find('input[name="company"]').val().trim().toUpperCase(),
            conAddress1: $(con).find('input[name="address1"]').val().trim().toUpperCase(),
            conCity: $(con).find('input[name="city"]').val().trim().toUpperCase(),
            conState: $(con).find('input[name="stateProvince"]').val().trim().toUpperCase(),
            conPostalCode: $(con).find('input[name="postalCode"]').val().trim().substring(0, 5).toUpperCase()
          };

          var hdr = CustomerOrderObject.Header;
          var b = {
            conCompany: hdr.CONSIGNEE_COMPANY.trim().toUpperCase(),
            conAddress1: hdr.CONSIGNEE_ADDRESS_LINE_1.trim().toUpperCase(),
            conCity: hdr.CONSIGNEE_CITY.trim().toUpperCase(),
            conState: hdr.CONSIGNEE_STATEPROVINCE.trim().toUpperCase(),
            conPostalCode: hdr.CONSIGNEE_POSTALCODE.trim().substring(0, 5).toUpperCase()
          };

          if (JSON.stringify(a) != JSON.stringify(b))
          {
            Logger.Log({ Source: 'CheckForMatchingConsignee()', Message: 'Could not add order ' + hdr.SHIPPER_REFERENCE + '. Primary order data to follow.', Data: a });
            Logger.Log({ Source: 'CheckForMatchingConsignee()', Message: 'Could not add order ' + hdr.SHIPPER_REFERENCE + '. Subsequent order data to follow.', Data: b });
          }

          /* Quick comparison to see if the strings match. */
          return (JSON.stringify(a) == JSON.stringify(b));
        }
        catch (error)
        {
          Logger.Log({ Source: 'CheckForMatchingConsignee()', Error: error });
        }
      },

      CheckForCompletedPacking: function ()
      {
        /* Enable the 'Done' button if the order is completely packed. */
        if ($('#selectClonedBoxTypes').prop('selectedIndex') === 0)
        {
          $('#selectClonedBoxTypes').css('border-color', 'crimson');
        }

        if (($('#tablePackVerifyOrderDetails').find('tr.not-packed, tr.not-complete').length == 0) && ($('#selectClonedBoxTypes').prop('selectedIndex') > 0))
        {
          $('#divPackVerifyButtonGroup').find('input[type=button]').prop('disabled', false);
        }
      },

      GetCurrentWeightFromScale: function ()
      {
        var w;

        if ($('input[type=button][ng-click="vm.getWeight()"]').attr('disabled') === 'disabled')
        {
          w = $('[ng-model="vm.currentShipment.Packages[vm.packageIndex].Weight.Amount"]').val();
          $('span.dim-val.dim-weight').text(w);
        }
        else
        {
          $('input[type=button][ng-click="vm.getWeight()"]').click(function ()
          {
            w = $('[ng-model="vm.currentShipment.Packages[vm.packageIndex].Weight.Amount"]').val();
            $('span.dim-val.dim-weight').text(w);
          });
        }
      },

      /* Triggered when the user clicks on the '+' icon within the order list.  Add all items in the row to the current box. */
      AddRowItemToCurrentBox: function (triggerElement)
      {
        try
        {
          var tableRow = $(triggerElement).closest('tr');
          var commodityContent = $(tableRow).data();

          /* No box type selected, paint the border red. */
          // if ($('#selectClonedBoxTypes').prop('selectedIndex') === 0)
          // {
          //   $('#selectClonedBoxTypes').css('border-color', 'crimson');
          // }

          /* This adds the commodities to the 'Goods' tab. */
          var packageIndex = $('span.current-container-index').text();
          packageIndex -= 1;
          this.AddCommoditiesToPackage(packageIndex, commodityContent);

          var newRow = $('<tr />').css('display', 'none');
          var itemDescText = $(tableRow).find('td:eq(0)').text();
          var itemNumber = itemDescText.split(' ')[0];
          var tableCurrent = $('div.table-box-contents table.table-custom:visible');

          var trData = {};
          trData.OrderNumber = commodityContent.OrderNumber;
          trData.ItemNumber = itemNumber;
          trData.ItemDescription = itemDescText;
          trData.StartVal = parseFloat($(tableRow).find('td:eq(1)').text());
          trData.ShippedVal = parseFloat(commodityContent.ScanQuantity);
          trData.RemainingVal = parseFloat(trData.StartVal - trData.ShippedVal);
          trData.TotalVal = trData.ShippedVal;

          /* For the total value, find each entry with the same item# and total the values and add the shipped value. */
          $(tableCurrent).find('tr:gt(0)').find('td:eq(1)').each(function (e)
          {
            if ($(this).text() == trData.ItemNumber)
            {
              trData.TotalVal += parseFloat($(this).closest('tr').find('td:eq(2)').text());
            }
          });

          /* Both html tables will have data objects on the tr's. */
          /* Make the .EntryId element inside each tr data() object equal so it serves as a key between the two tables. */
          trData.EntryId = $(tableRow).data().EntryId;

          $(newRow).append($('<td />').text(commodityContent.OrderNumber))
            .append($('<td />').text(itemNumber))
            .append($('<td />').text(trData.ShippedVal))
            .append($('<td />').text(trData.TotalVal))
            .append($('<td />').append('<span class="glyphicon glyphicon-minus trigger-icon" title="Remove this item from the current box." style="color: crimson;"></span>'))
            .data(trData);

          /* If the destination table (container) is empty, get rid of the single '.empty-table' row. */
          // if ($('tr.empty-table').is(':visible'))
          // {
            $('tr.empty-table').fadeOut().promise().done(function ()
            {
              $(tableCurrent).append($(newRow).fadeIn().show()).promise().done(function (e) { UpdateItemCounts(); });
            });
          // }
          // else
          // {
          //   $(tableCurrent).append($(newRow).fadeIn().show());
          // }

          /* Now update the classes and amounts from the incoming order data (table on left side of screen). */
          /* We can link the source and destination order by using the .EntryKey data() element. */

          /* Update the shipped quantity. */
          var previouslyShipped = parseFloat($(tableRow).find('td:eq(2)').text());

          var a, b;
          a = parseFloat(commodityContent.ScanQuantity) + parseFloat(previouslyShipped);
          b = parseFloat(trData.StartVal) - a;

          $(tableRow).find('td:eq(2)').text(a);
          $(tableRow).find('td:eq(3)').text(b);

          var trCount = ($('#tablePackVerifyOrderDetails').find('tr').length - 1);

          /* Update the shipped amount, change the color to green. */
          /* Set Shipped value to the value of the 2nd td. */
          if ($(tableRow).find('td:eq(3)').text() > 0)
          {
            $(tableRow).removeClass('not-packed').addClass('not-complete');

            // /* If there is only a single row then change the class and return. */
            // if (trCount > 1)
            // {
            //   var insertTargetRow = $(tableRow).closest('table.table-custom').find('tr.packed').first();
            //   if (insertTargetRow.length === 0)
            //   {
            //     insertTargetRow = $(tableRow).closest('table.table-custom').find('tr.not-packed').last();
            //     //$(tableRow).detach().insertAfter(insertTargetRow);
            //   }
            //   else
            //   {
            //     //$(tableRow).detach().insertBefore(insertTargetRow);
            //   }
            // }
          }
          else
          {
            $(tableRow).removeClass('not-complete not-packed').addClass('packed').promise().done(function () { UpdateItemCounts(); });
            /* Change the icon, don't allow a click. */
            $(tableRow).find('td:eq(4) > span').removeClass('glyphicon-plus add-box-item').addClass('glyphicon-ban-circle').css('color', 'crimson').attr({ 'title': '' });

            // if (trCount > 1)
            // {
            //   /* Now move the row we just added to the box to the end of the list. */
            //   var lastRow = $(tableRow).closest('table.table-custom').find('tr').last();
            //   //$(tableRow).detach().insertAfter(lastRow);
            // }
          }

          this.CheckForCompletedPacking();

          /* If the user clicked on the last row of the table, don't need to detach and append it.  Exit here. */
          //if ($(tableRow).index() == ($(tableRow).closest('table.table-custom').find('tr').length - 1)) { return; }

          // if ($(tableRow).closest('table.table-custom').find('tr').length > 2)
          // {
          //   /* Now move the row we just added to the box to the end of the list. */
          //   var lastRow = $(tableRow).closest('table.table-custom').find('tr').last();
          //   $(tableRow).detach().insertAfter(lastRow);
          // }
        }
        catch (error)
        {
          Logger.Log({ Source: GetCallingFunctionName(), Error: error});
        }
      },

      RemoveCurrentBoxFromShipment: function ()
      {
        /* If there is only a single box in this shipment, return out. */
        var tableCount = $('div.table-box-contents table.table-custom').length;
        if (tableCount <= 1) { UpdateItemCounts(); return false; }

        /* Click the button on the main page to remove the box. */
        $('#buttonRemovePackageFromShipment').first().trigger('click');

        /* Send the items from the current box back to the list. */
        var tableItemObjects = $('div.table-box-contents table.table-custom:visible tr:gt(1)');

        $(tableItemObjects).each(function (e)
        {
          PackVerify.RemoveThisTableRow(this);
        }).promise().done(function (e)
        {
          var prevContainerIndex = ($('div.table-box-contents table.table-custom:visible').index() - 1);

          $('div.table-box-contents table.table-custom:visible').fadeOut().remove().promise().done(function ()
          {
            //$('div.table-box-contents table.table-custom:eq(' + prevContainerIndex + ')').fadeIn().show();
            PackVerify.ShowContainerTable(prevContainerIndex);
            UpdateContainerCounts();
          });
        });
      },

      /* This takes the order number from the input box and adds it to the select box. */
      AddOrderNumbersToList: function ()
      {
        var inVal = $('#textOrderNumber').val();
        var bExists;

        /* Do not add duplicate entries to the select box. */
        $('#selectConsolidatedOrders').find('option').each(function ()
        {
          if (this.text == inVal)
          {
            bExists = true;
            /* Break out of the .each loop. */
            return false;
          }
        });

        if (bExists == true)
        {
          $('#textOrderNumber').val([]).focus();
          return false;
        }

        if (inVal.length > 0)
        {
          $('#selectConsolidatedOrders').append($('<option />')
            .val(inVal)
            .text(inVal)
            .trigger('change'))
            .promise().done(function ()
            {
              $('#textOrderNumber').val([]).focus();
            });
        }
      },

      ShowConsolidatedOrderDialog: function ()
      {
        /* Add the first order at the top of the list. */
        /* The first order is already set by the user, then the 'Consolidate Orders' button on the Pack Verify dialog triggers this function. */
        $('#divModalConsolidateShipments').fadeIn(400)
          .promise().done(function ()
          {
            /* Clear the input box, then focus on it. */
            $('#textOrderNumber').val([]).focus();
          });
      },

      SaveConsolidatedOrderNumbers: function ()
      {
        try
        {
          /* Append the shipper reference with each item in the select list in csv format. */
          /* Once this is done, change the SSBR on post load to loop through each option in the csv list. */
          var inputShipperReference = $('input[type=text][ng-model="vm.currentShipment.Packages[vm.packageIndex].ShipperReference"]');
          var shipperReferenceVal = $(inputShipperReference).val();

          /* If the user closes the dialog box, then opens it back up, we don't want to check the same orders more than once so only process what was entered into the dialog box this time. */
          var ordersToQuery = '';

          /* Loop through all the orders in the select box and determine if they are already in the list. */
          $('#selectConsolidatedOrders').find('option').each(function (i, v)
          {
            /* Remove the spaces before the commas for comparison. */
            var arrayOrders = shipperReferenceVal.split(",").map(function (item)
            {
              return item.trim();
            });

            /* If the current option value is NOT in the list within the Shipper Reference field, add it. */
            if ($.inArray(v.value.trim(), arrayOrders) == -1)
            {
              ordersToQuery += v.value.trim() + ',';
              //shipperReferenceVal += ',' + v.value;
            }

            /* Remove the option once it is added to the input box. */
            $(this).remove();

          }).promise().done(function ()
          {
            /* Done creating the list of orders to get from the server which is now held in the ordersToQuery variable. */

            /* Hide the div. */
            $('#divModalConsolidateShipments').hide();

            /* Make sure there are orders in the list. */
            if (ordersToQuery.length > 0)
            {
              PackVerify.GetOrderInformationFromCustomer(ordersToQuery);
            }
            else
            {
              PackVerify.ShowErrorAlert('ERROR: No unique orders to pull from the server.  All consolidated orders have already been added to this shipment.');
            }
          });
        }
        catch (error)
        {
          Logger.Log({ Source: 'SaveConsolidatedOrderNumbers()', Error: error });
        }
      },

      UpdateReferencesWithOrderNumbers: function (OrderNumber)
      {
        var inputShipperReference = $('input[type=text][ng-model="vm.currentShipment.Packages[vm.packageIndex].ShipperReference"]');
        var inputMiscReference07 = $('input[type=text][ng-model="vm.currentShipment.Packages[vm.packageIndex].MiscReference7"]');
        var shipperReferenceVal = $(inputShipperReference).val() + ',' + OrderNumber;
        //shipperReferenceVal += ',' + OrderNumber;

        /* Set these input values.  ShipperReference is used by the customer, MiscReference7 is used by the SBR to update each record post-ship. */
        /* ShipperReference ONLY keeps the first order number, before the comma, MiscReference7 keeps the entire string. */
        /* On the server-side, if MiscReference7 contains a comma, each item in the csv list gets posted back.  If MiscReference7 is empty, the ShipperReference is used instead for a single post-back. */
        $(inputShipperReference).val(shipperReferenceVal);
        $(inputMiscReference07).val(shipperReferenceVal);
        $('span.order-number-list').text(shipperReferenceVal);
      },

      /* Remove table rows from the container/box list. */
      /* Take the row from the container, then append it to the item list, adding the not-packed class. */
      RemoveThisTableRow: function (objElement)
      {
        var thisRow = $(objElement).closest('tr');
        var tableObj = $(objElement).closest('table');
        var tableRows = $(tableObj).find('tr').length;
        var trData = $(thisRow).data();

        var removedQuantity = parseFloat($(thisRow).find('td:eq(2)').text());

        try
        {
          $(thisRow).fadeOut().promise().done(function ()
          {
            /* Now remove it from this table and add it to the item list table. */
            $(this).remove();

            var firstRow = $('#tablePackVerifyOrderDetails').find('tr').first();

            $('#tablePackVerifyOrderDetails tr').filter(function (index)
            {
              return $(this).data().EntryId == trData.EntryId;

            }).fadeOut('fast').promise().done(function ()
            {
              /* Need to remove commodities from current view model. */
              var currentContainerIndex = (parseInt($('span.current-container-index').text()) - 1);
              PackVerify.RemoveCommoditiesFromPackage(currentContainerIndex, $(this).data());

              trData.ShippedVal -= removedQuantity;

              $(this).find('td:eq(1)').text(trData.StartVal);
              $(this).find('td:eq(2)').text(trData.ShippedVal);
              $(this).find('td:eq(3)').text(trData.StartVal);

              $(this).find('td:eq(4) > span').removeClass('glyphicon-ban-circle').addClass('glyphicon-plus add-box-item').attr({ 'title': 'Add this item to the current box.' });

              $(this).removeClass('packed not-complete')
                .addClass('not-packed')
                .detach()
                .insertAfter(firstRow)
                .fadeIn('fast').promise().done(function (e) { UpdateItemCounts(); });
            });

            /* Show the 'Empty Table' text. */
            if (tableRows <= 3)
            {
              $('#tablePackVerifyCutTicketScanResults').find($('tr.empty-table').fadeIn());
            }

          });
        }
        catch (error)
        {
          Logger.Log({ Source: GetCallingFunctionName(), Error: error });
        }
      },

      /* tableIndex could be either 'prev', 'next', or an integer. */
      ShowContainerTable: function (tableIndex)
      {
        try
        {
          var currentContainer = $('div.table-box-contents table.table-custom:visible');
          var targetContainer;

          /* fallbackContainer is used when the last table in the container is reached.  Go back to the beginning on 'next', to the end on 'prev'. */
          //var fallbackContainer;
          var uiNavButton;

          //console.log($('span.current-container-index.container-val').text());
          var currentContainerIndex = parseInt($('span.current-container-index.container-val').text());

          switch (tableIndex)
          {
            case 'prev':
              // targetContainer = $('div.table-box-contents table.table-custom:eq(' + ($(currentContainer).index() - 1) + ')');
              targetContainer = $('div.table-box-contents table.table-custom:eq(' + (currentContainerIndex - 2) + ')');


              //fallbackContainer = $('div.table-box-contents table.table-custom').last();
              uiNavButton = $('button[type=button][ng-click="vm.previousPackage();"]').first();
              break;

            case 'next':
              targetContainer = $('div.table-box-contents table.table-custom:eq(' + ($(currentContainer).index() + 1) + ')');
              //fallbackContainer = $('div.table-box-contents table.table-custom').first();
              uiNavButton = $('button[type=button][ng-click="vm.nextPackage();"]').first();
              break;

            default:
              targetContainer = $('div.table-box-contents table.table-custom:eq(' + tableIndex + ')');
              break;
          }

          // if (targetContainer.length === 0) {
          //   targetContainer = fallbackContainer;
          // }

          $(uiNavButton).click();

          $(currentContainer).fadeOut(200).hide().promise().done(function ()
          {
            var rows = $(targetContainer).find('tr').length;
            if (rows > 2)
            {
              $(targetContainer).find('tr.empty-table').hide();
            }
            $(targetContainer).fadeIn(200).show().promise().done(function () { UpdateItemCounts(); });
          });

          /* Update the text to show 'Container X of Y'.  .index() + 1 as the index is 0 based. */
          $('span.current-container-index').text($(targetContainer).index() + 1);

          /* Now get the box associated with the table and change the select option. */
          var selIndex = $(targetContainer).data().SelectedIndex;

          $('#selectClonedBoxTypes').prop('selectedIndex', selIndex);

        }
        catch (error)
        {
          Logger.Log({ Source: GetCallingFunctionName(), Error: error });
        }
      },

      ShowErrorAlert: function (ErrorMessage)
      {
        $('#divCustomAlert div.alert-message').text(ErrorMessage).closest('div.alert').show();
      }

    }; /* End return { */

  }()); /* end var PackVerify = (function () */

  var SessionData = (function ()
  {
    /* Private variables and functions. */
    let currentViewModel;
    let currentShipmentRequest;
    let currentUserContext = {};
    let serverLogging;

    return {

      SetCurrentUserContext: function (UserInformation)
      {
        /* If we only need the CompanyId, we can get that from the vmProfile. */
        /* If we need the UserId, we'll need to use a $.get call. */

        /* Updated 11/29/2021 */
        /* No need for the GET now as the data is stored in the vm.UserInformation node. */
        var localContext = {
          CompanyId: UserInformation.CompanyId,
          UserId: UserInformation.UserId,
          Machine: UserInformation.Machine
        };

        // if (!currentUserContext.UserId && bIncludeUser === true)
        // {
        //   $.ajax(
        //   {
        //     url: 'api/usercontext/GET',
        //     method: 'GET',
        //     async: false
        //   })
        //   .done(function (data)
        //   {
        //     localContext = data;
        //   })
        //   .fail(function (jqXHR, textStatus)
        //   {
        //     Logger.Log({ Source: GetCallingFunctionName(), Error: { name: 'AjaxError', message: jqXHR.ResponseText } });
        //   });
        // }
        // else if (!currentUserContext.CompanyId)
        // {
        //   /* Just need the CompanyId, get it from the profile, but don't null out the UserId if it is present. */
        //   localContext.CompanyId = vmProfile.CompanyId;
        //   localContext.UserId = currentUserContext.UserId;
        // }

        currentUserContext = localContext;

      }, /* end GetUserContext: function (vmProfile, bIncludeUser) */

      GetCurrentUserContext: function ()
      {
        return currentUserContext;
      },

      SetCurrentShipmentRequest: function (shipmentRequest)
      {
        currentShipmentRequest = shipmentRequest;
      },

      GetCurrentShipmentRequest: function ()
      {
        return currentShipmentRequest;
      },

      SetCurrentViewModel: function (viewModel)
      {
        currentViewModel = viewModel;
      },

      GetCurrentViewModel: function ()
      {
        return currentViewModel;
      },

      /* The debug mode will always write to the console.  Set the var to true to write to the server as well. */
      /* In future versions, add a hidden div to the page which will show errors as part of the UI. */
      SetServerDebugMode: function (writeToServer)
      {
        serverLogging = writeToServer;
      },

      GetServerDebugMode: function ()
      {
        return serverLogging;
      }

    }; /* end return { */

  }()); /* end var SessionData = (function () */

  /* If the below is placed within the Logger class it will need to be accessed using Logger.LogLevel.Trace */
  /* By placing it here, it is accessed using LogLevel.Trace.  This keeps the same nomenclature as the SBR logging class. */
  var LogLevel = {
    Trace: 0,
    Debug: 1,
    Info: 2,
    Warning: 3,
    Error: 4,
    Fatal: 5
  };

  var Logger = (function ()
  {
    let LogObject = {
      Source: null,
      LogLevel: null,
      Message: null,
      Data: null,
      Error: null
    }

    return {

      Log: function (LogObject)
      {
        try
        {
          /*
          Usage:
            Errors:
              try {
                ...
              } catch (error) {
                Logger.Log({ Source: 'FunctionName()', Error: error });
                OR
                Logger.Log({ Source: 'FunctionName()', Error: { name: 'Error name', message: 'Error message' } });
                OR
                Logger.Log({ Source: 'FunctionName()', Error: { name: 'Error name', message: 'Error message' }, Data: 'Simple or complex data object.' });
              }

            Info (default, no 'Type' needed):
              Logger.Log({ Source: 'FunctionName()', Message: 'Test of server info logging', Data: 'Simple or complex data object.' });

            All others:
              Logger.Log({ Source: 'FunctionName()', Type: 'Trace', Message: 'Test of server info logging with a complex data object.', Data: SessionData.GetCurrentUserContext() });
              Logger.Log({ Source: 'FunctionName()', Type: 'Fatal', Message: 'Test of server info logging with a simple data string.', Data: 'Simple data string literal.' });
              Logger.Log({ Source: 'FunctionName()', Type: 'Debug', Message: 'Test of server debug logging with no data object.' });
          */

          /* If the LogObject has an 'error' object, set the LogLevel here.  This makes the LogLevel variable optional for errors. */
          if (LogObject.Error)
          {
            LogObject.LogLevel = LogLevel.Error;

            /* Can't pass the error container to the server without preparing it inside a new object first. */
            var e = {};
            e.name = LogObject.Error.name;
            e.message = LogObject.Error.message;
            LogObject.Error = e;
          }
          else if (!LogObject.LogLevel)
          {
            /* If the LogObject does NOT have an 'error' object AND there is no LogLevel set, use .Info.  This makes the LogLevel variable optional for info logging. */
            LogObject.LogLevel = LogLevel.Info;
          }

          /* All logging will go to the console, output here. */
          switch (LogObject.LogLevel)
          {
            case LogLevel.Error:

              console.error('Exception encountered in', LogObject.Source);
              console.error(LogObject.Error.name);
              console.error(LogObject.Error.message);

              /* Use .trace if needed but within the Angular sandbox the .trace object can be huge and convoluted. */
              /* Be careful if passing .trace data on to the server as it will contain various data types and directives. */
              //console.error(LogObject.Error.trace);

              /* Don't use console.error for the data object, it causes readability issues. */
              if (LogObject.Data) { console.log(LogObject.Data); }
              break;

            /* Using the same nomenclature used by SBR logging. */
            case LogLevel.Info:
            case LogLevel.Debug:
            case LogLevel.Trace:
            case LogLevel.Fatal:
            default:

              console.log('Output from', LogObject.Source);
              console.log(LogObject.Message);
              if (LogObject.Data) { console.log(LogObject.Data); }
              break;
          }

          if (SessionData.GetServerDebugMode() == true)
          {
            var dataObject = {
              ServerMethod: 'AddClientEntry',
              MessageObject: LogObject
            };

            var ajaxObject = {
              UserContext: SessionData.GetCurrentUserContext(),
              Data: JSON.stringify(dataObject)
            };

            /* async doesn't necessarily need to be set to false here but I think it helps with data collisions and getting the error messages to appear in chronological order on the server. */
            var ajaxRequest = $.ajax({
              url: 'api/ShippingService/UserMethod',
              method: 'POST',
              contentType: 'application/x-www-form-urlencoded; charset=UTF-8',
              dataType: 'json',
              data: ajaxObject,
              async: false
            });

            ajaxRequest.done(function (json)
            {
              /* There is no return for this call, if the POST does come back with data, it's a server message/error like 'UserContext cannot be null' or more likely '{"Data":"","ErrorCode":0}' */
              //console.log(JSON.parse(json.Data));
            });

            ajaxRequest.fail(function (jqXHR, textStatus)
            {
              console.log('Unable to log message to server.');
              console.log(jqXHR);
              console.log(textStatus);
            });

            ajaxRequest.always(function (jqXHR, textStatus, jqXHRError)
            {

            });
          }

          /* Need to hide the loader outside the decision trees. */
          HideLoader();

        }
        catch (error)
        {
          console.log(error.message);
        }
      }

    }; /* end return { */

  }()); /* end var Logger = (function () */

} /* end function ClientBusinessRules() */

// Source: F Schumacher (FSCHUMAC)_Samples Area (7F4175) PROD CBR_2449.cs
// SetCurrentUserContext(viewModel, bIncludeUser) // returns undefined
SetCurrentUserContext: function (viewModel, bIncludeUser)
      {
        /* If we only need the CompanyId, we can get that from the vmProfile. */
        /* If we need the UserId, we'll need to use a $.get call. */
        var localContext = {};

        try
        {
          /* The .UserInformation is not always populated in time and will throw an error so wrap it in a try/catch. */
          /* If this info is available, assign it to the local object and return out of the function. */
          if (viewModel.UserInformation.CompanyId)
          {
            localContext.CompanyId = viewModel.UserInformation.CompanyId;
            localContext.UserId = viewModel.UserInformation.UserId;
            currentUserContext = localContext;
            return false;
          }

          if (!currentUserContext.UserId && bIncludeUser === true)
          {
            $.ajax(
              {
                url: 'api/usercontext/GET',
                method: 'GET',
                async: false
              })
              .done(function (data)
              {
                localContext = data;
              })
              .fail(function (jqXHR, textStatus)
              {
                Logger.Log({ Source: 'SetCurrentUserContext()', Error: { name: 'AjaxError', message: jqXHR.ResponseText } });
              });
          }
          else if (!currentUserContext.CompanyId)
          {
            /* Just need the CompanyId, get it from the profile, but don't null out the UserId if it is present. */
            localContext.CompanyId = viewModel.profile.CompanyId;
            localContext.UserId = currentUserContext.UserId;
          }
        }
        catch (error)
        {
          Logger.Log({ Source: 'SetCurrentUserContext()', Error: error });
        }

        currentUserContext = localContext;

      }

// GetCurrentUserContext() // returns the current user context object
GetCurrentUserContext: function ()
      {
        return currentUserContext;
      }

// SetCurrentViewModel(viewModel, bSetUserContext, bIncludeUser, callback) // returns undefined
SetCurrentViewModel: function (viewModel, bSetUserContext, bIncludeUser, callback)
      {
        if (bSetUserContext === true) { this.SetCurrentUserContext(viewModel, bIncludeUser); }

        currentViewModel = viewModel;

        if ($.isFunction(callback)) { callback(); }
      }

// MakeUserMethodRequest(requestMethod, requestData, bAsync = true, callback) // returns the immediate retJson value (or undefined before async completion)
MakeUserMethodRequest: function (requestMethod, requestData, bAsync = true, callback)
      {
        Tools.ShowLoader();

        var retJson;

        var dataObject = {
          ServerMethod: requestMethod,
          MethodData: requestData
        };

        var ajaxObject = {
          UserContext: Tools.GetCurrentUserContext(),
          Data: JSON.stringify(dataObject)
        };

        var ajaxRequest = $.ajax(
          {
            url: 'api/ShippingService/UserMethod',
            method: 'POST',
            contentType: 'application/x-www-form-urlencoded; charset=UTF-8',
            dataType: 'json',
            async: bAsync,
            data: ajaxObject
          });

        ajaxRequest.done(function (json)
        {
          /* Returning out of here only returns out of the ajax request, not out of the function. */
          retJson = Tools.ParseReturnData(json.Data);
        });

        ajaxRequest.fail(function (jqXHR, textStatus)
        {
          Logger.Log({ Source: 'MakeUserMethodRequest()', Error: { name: 'AjaxError', message: jqXHR.ResponseText } });
          retJson = jqXHRError;
        });

        ajaxRequest.always(function (jqXHR, textStatus, jqXHRError) 
        {
          Tools.HideLoader();
          if ($.isFunction(callback)) { callback(retJson); }
        });
        
        return retJson;
      }

// Source: FIS Global (FIS)_FIS Global Reference Rules_96.cs
// this.PreSearchHistory(searchCriteria) // returns void
    this.PreSearchHistory = function (searchCriteria) {
        searchCriteria.WhereClauses.push({ FieldName: 'MiscReference19', FieldValue: userContext.UserId, Operator: 5 });
    };

// Source: Fluid Analysis Laboratory (POLARIS)_Polaris_1684.cs
// GetUserContext() // returns the fetched user context object
function GetUserContext() {
        if (userContext === undefined) {
            userContext = ajaxGet('api/usercontext/GET');
        }
    }

// Source: FMC (FMC)_FMC Reference Rule_2486.cs
// function GetUserContext(userContext) // returns the userContext object, fetched from api if undefined
function GetUserContext(userContext) {
    if (userContext === undefined) {
        userContext = ajaxGet('api/usercontext/GET');
    }
    return userContext;
}

// function userDataOnlyHistoryView(searchCriteria) // returns void
function userDataOnlyHistoryView(searchCriteria)
{
    var userContext;
    userContext = GetUserContext(userContext);
    searchCriteria.WhereClauses.push({ FieldName: 'UserId', FieldValue: userContext.UserId, Operator: 0 });
}

// Source: FNA (FENDI)_Revision 3_2482.cs
// function getAddressBookEntries(addressBookName, context) // returns address book entries array or undefined
function getAddressBookEntries(addressBookName, context) {
        var searchCriteria = { WhereClauses: [{ FieldName: "Name", FieldValue: addressBookName, Operator: 0 }] };
        var data = { SearchCriteria: searchCriteria, CompanyId: context.CompanyId, SiteId: context.SiteId, UserId: context.UserId };
        debugger;
        //var response = ajaxPost('GetAddressBooks', data);
        var response = thinClientAPIRequest('GetAddressBooks_Shared', data, false);
        debugger;
        if (response.TotalRecords == 0) {
            var addressBookEntry = { Code: 'Error: AddressBook "' + addressBookName + '" Not Found' };
            return [addressBookEntry];
        } else {
            var addressBookId = response.AddressBooks[0].Id;

            searchCriteria = { OrderByClauses: [{ FieldName: "Code", Direction: "asc", FieldType: 3 }] };
            data = { AddressBookIds: [addressBookId], SearchCriteria: searchCriteria, CompanyId: context.CompanyId, SiteId: context.SiteId, UserId: context.UserId };
            //response = ajaxPost('GetAddressBookEntries', data);
            response = thinClientAPIRequest('GetAddressBookEntries_Shared', data, false);
            debugger;
            if (response.TotalRecords > 0) {
                return response.AddressBookEntries;
            }
        }
    }

// Source: GAP (GAP)_Gap Reference rule with ON Hanger Pooler_2507.cs
// GetShipToAddressCustomData() // returns the CustomData for the first matching address book entry
this.GetShipToAddressCustomData = function(){
        //debugger;
        var payloadShipToAddress = {
            "Skip": 0,
            "Take": 10,
            "WhereClauses": [{
                "FieldName": "Id",
                "FieldValue": consigneeId,
                "Operator": 0
            }]
        };
        
        var data = {
            CompanyId: client.userContext.CompanyId,
            addressBookIds: addrsBookId,
            SearchCriteria: payloadShipToAddress
        };
        //console.log(data);
      
        ShipToAddressBook = client.thinClientAPIRequest('GetAddressBookEntries_AddressBook', data, false).responseJSON;
    
        console.log(ShipToAddressBook.AddressBookEntries[0].CustomData);
        
        return ShipToAddressBook.AddressBookEntries[0].CustomData;
                
    };

// PreShip(shipmentRequest, userParams) // returns nothing
this.PreShip = function(shipmentRequest, userParams) { 
        debugger;
        const st_company = shipmentRequest.PackageDefaults.Consignee.Company.toUpperCase();
        addrsBookId = shipmentRequest.PackageDefaults.Consignee.AddressBookId;
        consigneeId = shipmentRequest.PackageDefaults.Consignee.Id;
        
        if(st_company == "UPS MI" || st_company.includes("MAIL INNOVATIONS"))
        {
            console.log("Mail Innovatoins Shipment");
            console.log(shipmentRequest.PackageDefaults.Consignee.AddressBook);
            console.log(addrsBookId);
            console.log(consigneeId);
                
            addressCustomData = this.GetShipToAddressCustomData();
            mi_flag = client.getValueByKey('Custom1',addressCustomData);
                  
            if(mi_flag !== "MI_TPB"){
               throw new Error("Please contact GAP ShipExec Admin for Mail Innovation Authorization [Ship-Exec-stores-dg@gap.com].");
            }
            else
            {
               shipmentRequest.PackageDefaults.ThirdPartyBilling = true;
               shipmentRequest.PackageDefaults.ThirdPartyBillingAddress = {
                    Company : "UPS Mail Innovations",
                    Contact : "Shipping",
                    Address1 : "13050 Marlay Ave",
                    Address2 : "Suite 100",
                    City : "Fontana",
                    StateProvince : "CA",
                    PostalCode : "92337",
                    Country : "US",
                    Email : "upsusmi01transportation@ups.com",
                    Account : "7500YW"
                };
                      
            }
        }

        if (pooler)
        {
            if (document.getElementById("pooler").checked)
            {
                for (var p of shipmentRequest.Packages)
                {
                    p.MiscReference4 = vm.profile.Shippers[0].Email.replace("@", "").replace(" ", "").replace(".pool", "").replace(".POOL", "").toUpperCase();
                }               
            }
        }

        var misc3reference = "200.30.6035.5102.90059";
        for (var i = 0; i < shipmentRequest.Packages.length; i++) {
            shipmentRequest.Packages[i].MiscReference1 = shipmentRequest.Packages[i].ShipperReference;
            shipmentRequest.Packages[i].ShipperReference = "";
            //Address 1
            if(shipmentRequest.PackageDefaults.Consignee.Address1 == "1112 Turkey Mountain Rd.")
            {
                console.log('found 1/4 address');
                shipmentRequest.Packages[i].MiscReference3 = misc3reference;
                shipmentRequest.Packages[i].ShipperReference = misc3reference;
            }
             //Address 2
            if(shipmentRequest.PackageDefaults.Consignee.Address1 == "102R Demi Rd.")
            {
                                console.log('found 1/4 address');

                shipmentRequest.Packages[i].MiscReference3 = misc3reference;
                                shipmentRequest.Packages[i].ShipperReference = misc3reference;

            }
              //Address 3
            if(shipmentRequest.PackageDefaults.Consignee.Address1 == "17511 S. Susana Rd.")
            {
                                console.log('found 1/4 address');

                shipmentRequest.Packages[i].MiscReference3 = misc3reference;
                                shipmentRequest.Packages[i].ShipperReference = misc3reference;

            }
               //Address 4
            if(shipmentRequest.PackageDefaults.Consignee.Address1 == "200 Terminal St.")
            {
                                console.log('found 1/4 address');

                shipmentRequest.Packages[i].MiscReference3 = misc3reference;
                                shipmentRequest.Packages[i].ShipperReference = misc3reference;
            }
        }
        console.log('PRESHIP');
        console.log(shipmentRequest);

    };

// PreRate(shipmentRequest, userParams) // returns nothing
this.PreRate = function(shipmentRequest, userParams) {

        if (pooler)
        {
            if (document.getElementById("pooler").checked)
            {  
                //alert( shipmentRequest.PackageDefaults.Shipper);
                for (var p of shipmentRequest.Packages)
                {
                    p.MiscReference4 = vm.profile.Shippers[0].Email.replace("@", "").replace(" ", "").replace(".pool", "").replace(".POOL", "");
                    //alert(p.MiscReference4);
                } 
            }
            else
            {
                for (var p of shipmentRequest.Packages)
                {
                    p.MiscReference4 = ""
                }
            }
        }
    };

// Source: GAP (GAP)_Rule with Mail Innov. Billing_1888.cs
// function isSelectedValueInShippersArray() // returns boolean
function isSelectedValueInShippersArray() {
        // Use a more specific selector that targets the ng-model
        var selectElement = document.querySelector('select[ng-model="vm.currentShipment.PackageDefaults.Shipper"]');
        
        // Ensure the element exists
        if (!selectElement) {
          console.warn('Select element not found');
          return false;
        }
        
        // Get the index of the selected option
        var selectedIndex = selectElement.selectedIndex;
        
        // Ensure an option is selected
        if (selectedIndex < 0) {
          console.warn('No option is selected');
          return false;
        }
        
        // Get the selected option element
        var selectedOption = selectElement.options[selectedIndex];
        
        // Get the value of the selected option (and remove the "string:" prefix if present)
        var selectedValue = selectedOption.value.replace('string:', '');
        
        // Check if the selected value is in the array
        return StoreLocationsCanadaArray.includes(selectedValue);
      }

// Source: GAP INC (GAPMailrooms)_DirectToMobileCBR_1918.cs
// GetReturnAddresses() // returns the return address list from the user context-backed service response
function GetReturnAddresses()
	{
		var userMethodParams = { "action": "GetD2MAddressBook"};
		
		var data = { Data: JSON.stringify(userMethodParams), UserContext: client.userContext };
		
		var returnAddressList = undefined;
		
		thinClientAPIRequest('UserMethod', data, false).done(function (response) {
			
			if (response.ErrorCode != 0) 
			{
				alert(response.ErrorMessage);
			} 
			else 
			{
				
				var json = DecodeReturnString(response.Data);
				returnAddressList = json.ReturnAddressList;		
				returnAddressList.forEach(function (returnAddress) {
					var x = returnAddress;
					console.log(x);
				});
			}
		});
		return returnAddressList;
	}

// Source: GAP INC (GAPMailrooms)_Gap Mailrooms BR_70.cs
// new Shipment(shipmentRequest) // returns a Shipment instance used to read user/to custom data
class Shipment {
   constructor(shipmentRequest) {
      this.ShipmentRequest = shipmentRequest;
   }

 
   get UserEmail() { return this.GetEmail(); }

   get UserRef1() { return this.GetCustom("Custom1", "User"); }
   get UserRef2() { return this.GetCustom("Custom2", "User"); }
   get UserRef3() { return this.GetCustom("Custom3", "User"); }
   get UserRef4() { return this.GetCustom("Custom4", "User"); }
   get UserRef5() { return this.GetCustom("Custom5", "User"); }
   get UserRef6() { return this.GetCustom("Custom6", "User"); }
   get UserRef7() { return this.GetCustom("Custom7", "User"); }
   get UserRef8() { return this.GetCustom("Custom8", "User"); }
   get UserRef9() { return this.GetCustom("Custom9", "User"); }
   get UserRef10() { return this.GetCustom("Custom10", "User"); }

   get ToRef1() { return this.GetCustom("Custom1", "To"); }
   get ToRef2() { return this.GetCustom("Custom2", "To"); }
   get ToRef3() { return this.GetCustom("Custom3", "To"); }
   get ToRef4() { return this.GetCustom("Custom4", "To"); }
   get ToRef5() { return this.GetCustom("Custom5", "To"); }
   get ToRef6() { return this.GetCustom("Custom6", "To"); }
   get ToRef7() { return this.GetCustom("Custom7", "To"); }
   get ToRef8() { return this.GetCustom("Custom8", "To"); }
   get ToRef9() { return this.GetCustom("Custom9", "To"); }
   get ToRef10() { return this.GetCustom("Custom10", "To"); }

   GetEmail(){
      var emailText = "";
      var tags = document.getElementsByTagName("a"); 
      Array.from(tags).forEach(function(tag){
         if(tag.innerHTML.includes("Hello")) emailText = tag.innerHTML.replace("Hello", "").replace("!","").trim(); 
      });
      return emailText;
   }
   
   GetCustom(fieldName, customDataType) {
      var returnValue = "";
      var customData = null;
      if (customDataType === "User") customData = this.ShipmentRequest.PackageDefaults.OriginAddress.CustomData;
      if (customDataType === "To") customData = this.ShipmentRequest.PackageDefaults.Consignee.CustomData;

      if (customData) {
         customData.forEach(function (customField) {
            if (fieldName === customField.Key) returnValue = customField.Value;
         })
      }
      return returnValue;
   }
// End Shipment Class    
}

// GetCustom(fieldName, customDataType) // returns the matching custom field value or an empty string
   GetCustom(fieldName, customDataType) {
      var returnValue = "";
      var customData = null;
      if (customDataType === "User") customData = this.ShipmentRequest.PackageDefaults.OriginAddress.CustomData;
      if (customDataType === "To") customData = this.ShipmentRequest.PackageDefaults.Consignee.CustomData;

      if (customData) {
         customData.forEach(function (customField) {
            if (fieldName === customField.Key) returnValue = customField.Value;
         })
      }
      return returnValue;
   }

// Source: Gem Group (GEM)_Gem_1843.cs
// GetUserContext() // returns void; initializes cached userContext from the API
function GetUserContext()
  {
    if (userContext == undefined)
    {
      var userContextUrl = 'api/usercontext/GET';
      var shipExecServiceUrl = client.config.ShipExecServiceUrl;
      if (shipExecServiceUrl.startsWith('http'))
      {
        var url = new URL(shipExecServiceUrl);
        userContextUrl = url.origin + '/' + userContextUrl;
      }
      var authorizationToken = client.getAuthorizationToken();
      userContext = $.get({ url: userContextUrl, headers: authorizationToken, async: false }).responseJSON;
    }
  }

// voidBatchJS(batch) // returns void; sends a user method request including UserContext
function voidBatchJS(batch)
  {
    // realizar operaciones aquí con el parámetro batch
    console.log("El botón fue presionado para el lote: ", batch.name);
    var usermethodParams = { "Action": "VoidBatch", "BatchReference": batch.name };

    var data = { UserContext: userContext, "Data": JSON.stringify(usermethodParams) };
    var response = client.thinClientAPIRequest('UserMethod', data, false).done(function (response)
    {
      if (response.ErrorCode != 0)
      {
        client.alert.Danger(response.ErrorMessage);
      } else
      {
        $("#TrailerSortCode").val(response.Data);
      }
    });
  }

// CallUserMethod(GlobalMSN, Machine = "") // returns void; sends a user method request including UserContext
    function CallUserMethod(GlobalMSN, Machine = ""){
        let usermethodParams = { "Action": "RePrintAMP", "GlobalMSN": GlobalMSN };
        let data = { UserContext: userContext, "Data": JSON.stringify(usermethodParams) };
        console.log(data);
        let response = client.thinClientAPIRequest('UserMethod', data, true, Machine).done(function (response)
        {
            console.log(response);
            const decodedDataString = decodeHtmlEntities(response.Data);
            const dataObject = JSON.parse(decodedDataString);

            console.log(dataObject);
            dataObject.forEach(item => {
              
              if (item.RawData !== undefined && item.RawData.length > 0){
                  printDocuments(item);
              }
                  
              if (item.PdfData !== undefined && item.PdfData.length > 0) {
                  downloadPdf(item);
              }
              
              if (item.ImageData !== undefined && item.ImageData.length > 0) {
                  client.alert.Danger('Image NOT SUPPORTED');
                  /*TODO download image*/
              }
            });
        });
    }

// Source: GlaxoSmithKline LLC (gsk)_Glasko Smith BR_69.cs
// PreSearchHistory(searchCriteria) // returns void
    this.PreSearchHistory = function(searchCriteria) {
        debugger;
        var whereClauses = searchCriteria.WhereClauses;
        whereClauses.push({ FieldName: 'UserId', FieldValue: userContext.UserId, Operator: 5 });
    }

// getUserContext() // returns void
    function getUserContext() {
        if (userContext == undefined) {
            userContext = ajaxGet('api/usercontext/GET');
        }
    }

// ajaxGet(url) // returns result
    function ajaxGet(url) {
        var result;
        $.ajax({
            type: 'GET',
            url: url,
            async: false,
            success: function(response) {
                result = response;
            }
        });
        return result;
    }

// Source: Global Eagle (GE)_DEFAULT_1616.cs
// callUserMethod(x,requestorEmail, requestorNotes, approverNotes) // returns dataReturn
function callUserMethod(x,requestorEmail, requestorNotes, approverNotes) {
        // 1 - UserType
        // 2 = SupervisorEmail
        // 3 = Send Email / SupervisorEmail
        var usermethodParams = { "action": x, "RequestorEmail": requestorEmail, "RequestorNotes": requestorNotes, "ApproverNotes": approverNotes, "usercontext": userContext};
        var dataReturn;
        $.ajax({
            type: "POST",
            url: clientService.config.ShipExecServiceUrl + '/UserMethod',
            async: false,
            cache: false,
            dataType: "json",
			headers: clientService.authorizationToken(),
            data: { UserContext: client.userContext, "Data": JSON.stringify(usermethodParams) },
            success: function (response) {
                var jsonString = JSON.stringify(response).replace('[', '').replace(']', '');
                if (jsonString) {
                    var result = jQuery.parseJSON(jsonString);
                    dataReturn = result.Data;
                }
            },
            error: function (error) {
                dataReturn = error.responseText;
            }
        });
        return dataReturn;
    }

// Source: HARLEY DAVIDSON (HD)_HD CBR 2.3 Fix_1758.cs
// this.PageLoaded(location) // returns void
    this.PageLoaded = function (location) {

        if (location == '/') {
            var w = window.location.href + "batchmanager";
            window.location.replace(w);
        }

        if (location == '/batchmanager') {
            $(document).ready(function () {
                $.globalEval('var userContext;');

                var dataObject = {};
                dataObject.Action = "GetDealerId";
                dataObject.Content = "";
                debugger;
                var data = { Data: JSON.stringify(dataObject), UserContext: client.userContext };
                clientService.thinClientAPIRequest('UserMethod', data, false).done(function (jsonData) {
                    debugger;
                    //console.log(jsonData.Data);
                    $('body').find('a[ng-click="vm.itemPerPageChanged(0)"]').first().trigger('click').promise().done(function (e) {
                        ProcessDealerId(jsonData.Data);
                    });
                });
            }); //end document.ready
        }

        if (location == '/batchdetail') {
            $(document).ready(function () {
                try {
                    $('body').delegate('a[ng-model="vm.itemsPerPage"][ng-click="vm.itemPerPageChanged(0)"]', 'click', function (e) {
                        var waitForTable = setInterval(function () {
                            /* The spanTotalBatchItems element text defaults to a value of 0, need to wait until it changes before we continue. */
                            if (($('#spanTotalBatchItems').length > 0) && ($('#spanTotalBatchItems').text() != "0")) {
                                var currentDate = new Date();
                                var totalBatchItems = parseInt($('#spanTotalBatchItems').text());

                                /* Once this condition is met, we can remove the rows we don't want. */
                                if ($('#tableBatchDetails tr').length >= totalBatchItems) {
                                    clearInterval(waitForTable);
                                    //console.log('table rows condition met.');
                                    /* Loop the table rows. */
                                    $('#tableBatchDetails tr:gt(0)').each(function (x, y) {
                                        var tdDate = new Date($(this).find('td:eq(1)').text());
                                        if (tdDate < currentDate) {
                                            $(this).remove();
                                        }
                                    }).promise().done(function (e) {
                                        $('#tableBatchDetails').show();
                                        var i = $('#tableBatchDetails tr').length;
                                        $('#spanTotalBatchItems').text(i);
                                    });
                                } //end if ($('#tableBatchDetails tr').length >= totalBatchItems) 

                            } //end if (($('#spanTotalBatchItems').length > 0) && ($('#spanTotalBatchItems').text() != "0")) 

                        }, 250);
                    });

                    /* Event handler for the 'View Labels' button. */
                    $('body').delegate('#buttonViewCheckedLabels', 'click', function (e) {
                        //prevent the subsequent listeners from firing
                        e.stopImmediatePropagation();
                        // console.log("Click");
                        ViewCheckedLabels();
                        //   $('#tableBatchDetails').find('input[type=checkbox]:checked').each(function () 
                        //   { 
                        //     ViewCheckedLabels();
                        //   });
                    }); //end button delegate

                    /* Trigger the click of the 'All' button.  Maybe in the future add an id to the element to make the code a bit more readable. */
                    $('a[ng-model="vm.itemsPerPage"][ng-click="vm.itemPerPageChanged(0)"]').trigger('click');


                }
                catch (err) {
                    console.error(err.message);
                }
            }); //end document.ready
        }

    } //end PageLoaded

// function ProcessDealerId(DealerId) // returns void
    function ProcessDealerId(DealerId) {
        var WaitForDealerId;

        var tdDealerId = $('td:contains(' + DealerId + ')').first();

        if (tdDealerId.length == 0) {
            /* Wait 150 milliseconds before trying to find the dealer id in the batch list.  A better way would be to get the number of batch items, then wait for that number of rows to show.  Work on that next. */
            WaitForDealerId = setTimeout(function () {
                ProcessDealerId(DealerId);
            }, 150)
        }
        else {
            //console.log('DealerID Found.');
            clearTimeout(WaitForDealerId);
            $(tdDealerId).find('a').first().trigger('click');
        }

    }

// var client = { userContext: <immediately-invoked function result> } // returns client object
    var client = {
        userContext: function () {
            var userContextUrl = 'api/usercontext/GET';
            var shipExecServiceUrl = clientService.config.ShipExecServiceUrl;
            if (shipExecServiceUrl.startsWith('http')) {
                var url = new URL(shipExecServiceUrl);
                userContextUrl = url.origin + '/' + userContextUrl;
            }
            var authorizationToken = clientService.authorizationToken();
            return $.get({ url: userContextUrl, headers: authorizationToken, async: false }).responseJSON;
        }(),
        dismiss: $('body').delegate('*', 'focus', function () { $('div[role="alert"]:visible').hide(); }),
        alert: (function () {
            var closealert = $('<button class="close" />').append('<span>&times;</span>');
            var uibalert = $('<div hidden class="alert alert-dismissible alert-bottom ng-scope" role="alert" style="z-index: 2000;" />');
            uibalert.append(closealert).append('<span style="padding-right: 10px" />');

            $('body').append(uibalert);
            closealert.on('click', function () { uibalert.hide(); })

            return {
                Info: function (msg) {
                    uibalert.removeClass('alert-success alert-danger');
                    uibalert.find('span:eq(1)').text(msg);
                    uibalert.addClass('alert-info').show();
                },
                Success: function (msg) {
                    uibalert.removeClass('alert-info alert-danger');
                    uibalert.find('span:eq(1)').text(msg);
                    uibalert.addClass('alert-success').show();
                },
                Danger: function (msg) {
                    uibalert.removeClass('alert-info alert-success');
                    uibalert.find('span:eq(1)').text(msg);
                    uibalert.addClass('alert-danger').show();
                }
            }
        })()
    }

// Source: HARLEY DAVIDSON (HD)_Phase 2_2436.cs
// ViewCheckedLabels() // returns undefined
function ViewCheckedLabels() {
        const batchItemArr = [];
        $('#tableBatchDetails').find('input[type=checkbox]:checked').each(function () {
            //debugger;
            console.log("Print");
            var btnPrint = $(this).closest('tr').find('input[type="button"][value="Print"]').first().trigger('click');
            batchItemArr.push($(this).attr('data'))
        });
        var dataObject = {};
        dataObject.Action = "RemoveBatchItem";
        let bLen = batchItemArr.length;
        for (let i = 0; i < bLen; i++) {
            if (i == 0) {
                dataObject.Content = batchItemArr[i];
            } else {
                dataObject.Content += "," + batchItemArr[i];
            }
        }
        var data = { Data: JSON.stringify(dataObject), UserContext: client.userContext };
        //debugger;
        clientService.thinClientAPIRequest('UserMethod', data, false)
    }

// client.userContext() // returns user context object
userContext: function () {
            var userContextUrl = 'api/usercontext/GET';
            var shipExecServiceUrl = clientService.config.ShipExecServiceUrl;
            if (shipExecServiceUrl.startsWith('http')) {
                var url = new URL(shipExecServiceUrl);
                userContextUrl = url.origin + '/' + userContextUrl;
            }
            var authorizationToken = clientService.authorizationToken();
            return $.get({ url: userContextUrl, headers: authorizationToken, async: false }).responseJSON;
        }()

// Source: Hyve Solutions (HYVE)_Hyve_Solutions_CBR_1593.cs
// PostSelectAddressBook(shipmentRequest, nameaddress) // returns undefined
this.PostSelectAddressBook = function (shipmentRequest, nameaddress) {
		//alert("Country " + nameaddress.Country);
		
		var clientContext;
		var resp;

        $.ajax({
            type: "GET",
            url: "api/usercontext/GET",
            async: false,
            cache: false,
            success: function (result) {
                debugger;
                clientContext = result;
            },
            error: function (jqXhr, exception) {
                debugger;
            }
        });
     
        $.ajax({
            type: "POST",
            url: "api/ShippingService/UserMethod",
            async: false,
            cache: false,
            dataType: "json",           
            data: { UserContext: clientContext , "Data": nameaddress.Country},    
            success: function (response) {
                debugger;
				resp = JSON.stringify(response);
				resp = JSON.parse(resp);
				if (resp.Data !== "" && resp.Data !== undefined)
				{
					var IOR = JSON.parse(resp.Data);
					shipmentRequest.Packages[0].ImporterOfRecord = IOR;
				}
				else
				{
					alert('No available Importer of Records');
				}
            },
            error: function (xhr) {
                debugger;
                alert(xhr.responseText);
            }
        });
		
	}

// GetUserContext() // returns undefined
function GetUserContext() {
		if (userContext === undefined) {
            userContext = ajaxGet('api/usercontext/GET');
        }
    }

// ajaxGet(url) // returns result
function ajaxGet(url) {
        var result;
        $.ajax({
            type: "GET",
            url: url,
            async: false,
            success: function (response) {
                result = response;
            }
        });
        return result;
    }

// Source: ICON Clinical Research (PRAInternational)_PRA Intl Business Rules_1574.cs
// this.PreSearchHistory(searchCriteria) // returns undefined
    this.PreSearchHistory = function(searchCriteria) {
        //searchCriteria.WhereClauses.push({ FieldName: 'UserId', FieldValue: userContext.UserId, Operator: 0 });   //CBR FIX 07202022 Set in profiles
    };

// Source: Imagine Print Solutions (IPS)_Imagine Print CBR_1725.cs
// function () // returns user context object
function () {
            debugger;
            var userContextUrl = 'api/usercontext/GET';
            var shipExecServiceUrl = clientService.config.ShipExecServiceUrl;
            if (shipExecServiceUrl.startsWith('http')) {
                var url = new URL(shipExecServiceUrl);
                userContextUrl = url.origin + '/' + userContextUrl;
            }
            var authorizationToken = clientService.authorizationToken();
            return $.get({url: userContextUrl, headers: authorizationToken, async: false}).responseJSON;
        }

// Source: Indiana University (INDIANAU)_IU.Ship.Returns_1548.cs
// PreSearchHistory(searchCriteria) // returns void
this.PreSearchHistory = function (searchCriteria) {
        searchCriteria.WhereClauses.push({ FieldName: 'UserId', FieldValue: client.userContext.UserId, Operator: 0 });
    };

// Source: Inditex (INDITEX)_Client Business Rules_2483.cs
// function () // returns void
this.ManifestDocuments = function () {
        testbutton();

        function testbutton() {
            $('body').delegate('#buttonExportCODFile', 'click', function () {
                debugger;
                var shipper = $(this).attr('shipper');
                var carrier = $(this).attr('carrier');
                var historyId = $(this).attr('historyId');
                var historyShipDateYear = $(this).attr('shipDateYear');
                var historyShipDateMonth = $(this).attr('shipDateMonth');
                var historyShipDateDay = $(this).attr('shipDateDay');
                var isSftpChecked = $('#SftprBtnId').prop('checked');
                var isLocalChecked = $('#LocalrBtnId').prop('checked');
                debugger;

                // Send data to user method on the server side
                var usermethodParams = { "action": 1, "usercontext": client.userContext, "shipper": shipper, "historyId": historyId, "historyShipDateYear": historyShipDateYear, "historyShipDateMonth": historyShipDateMonth, "historyShipDateDay": historyShipDateDay, "carrier": carrier, "sftpSelectrdStatus": isSftpChecked, "localSelectrdStatus": isLocalChecked };
                var codFileString;

                var data = { UserContext: client.userContext, "Data": JSON.stringify(usermethodParams) };
                
                clientService.thinClientAPIRequest('UserMethod', data, false).done(function (response) {
                    debugger;                  

                    //****New code 01.24.2022****
                    var jsonString = $("<textarea/>").html(response.Data).text();

                    if (jsonString && isLocalChecked) {                        
                        codFileString = jsonString;

                        var data = new Blob([codFileString], { type: 'text/plain' });

                        var ie = navigator.userAgent.match(/MSIE\s([\d.]+)/),
                            ie11 = navigator.userAgent.match(/Trident\/7.0/) && navigator.userAgent.match(/rv:11/),
                            ieEdge = navigator.userAgent.match(/Edge/g),
                            ieVer = (ie ? ie[1] : (ie11 ? 11 : (ieEdge ? 12 : -1)));

                        // set the export file name 
                        var exportFileName = "ShipmentRequest" + historyShipDateYear + historyShipDateMonth + historyShipDateDay + "_1_" + shipper + ".txt"

                        if (ieVer > -1) {
                            window.navigator.msSaveBlob(codFileString, exportFileName);

                        } else {
                            var downloadLink = document.createElement("a");
                            downloadLink.download = exportFileName;
                            downloadLink.href = window.URL.createObjectURL(data);
                            downloadLink.style.display = "none";
                            document.body.appendChild(downloadLink);
                            downloadLink.click();
                            document.body.removeChild(link);
                        }
                    }
                });          
            })
        }
    }

// Source: Integrated Marketing Tech (IMT)_shipment_CBR_IMT_PROD_1825.cs
// userContext() // returns the current user context object
function () {
            debugger;
            var userContextUrl = 'api/usercontext/GET';
            var shipExecServiceUrl = clientService.config.ShipExecServiceUrl;
            if (shipExecServiceUrl.startsWith('http')) {
                var url = new URL(shipExecServiceUrl);
                userContextUrl = url.origin + '/' + userContextUrl;
            }
            var authorizationToken = clientService.authorizationToken();
            return $.get({url: userContextUrl, headers: authorizationToken, async: false}).responseJSON;
        }

// ExportManifestData() // returns void
function ExportManifestData()
    {	
		//var d = new Date();
		//var manifestdate = [d.getMonth()+1, d.getDate(), d.getFullYear()].join('/');	
		//debugger;
		var today = new Date();
		var dd = today.getDate();
		var mm = today.getMonth()+1; 
		var yyyy = today.getFullYear();
		
		if(dd<10) 
		{
			dd='0'+dd;
		} 
		
		if(mm<10) 
		{
			mm='0'+mm;
		} 		
		
		today = mm+'/'+dd+'/'+yyyy;
		
		var manifestdate = prompt("Enter the Ship Date to export (MM/DD/YYYY)", today);	
		
		//Check if Cancel Button was selected
		if (manifestdate === null) 
		{
			return; 
		}
		
		// I am adding a prefix to the beginging of the search value to know what code to run on the user method as this method is going to be used for other functions
        //var params = "manifestdataexport|" + manifestdate;
		
        var data = {
            'UserContext': client.userContext,
            'Data' : 'manifestdataexport|' + manifestdate
        }

        $.post({ 
            url: clientService.config.ShipExecServiceUrl + '/UserMethod',
            data,
            headers: clientService.authorizationToken()
        }).done(function (result) {
			//debugger;
			if (result != null && result != 'undefined' && result != '')
            {
                alert(result.Data);
            }
            else
            {
                alert("No data was returned from manifest data export call");
            } 
		}).fail(function (err) {
		    //debugger;
			//alert(e.message);
            alert("The process is taking longer than usual to complete but will continue to run on the server until all records are transferred");
		});	

        showDelayMessage();
    }

// resendShipments() // returns void
function resendShipments() {	
        
		var today = new Date();
		var dd = today.getDate();
		var mm = today.getMonth()+1; 
		var yyyy = today.getFullYear();
		
		if(dd<10) 
		{
			dd='0'+dd;
		} 

		if(mm<10) 
		{
			mm='0'+mm;
		} 		
		
		today = mm+'/'+dd+'/'+yyyy;
		
		var manifestdate = prompt("Enter the Ship Date to export (MM/DD/YYYY)", today);	
		
		//Check if Cancel Button was selected
		if (manifestdate === null) 
		{
			return; 
		}
		
		// I am adding a prefix to the beginging of the search value to know what code to run on the user method as this method is going to be used for other functions
        var data = {
            'UserContext': client.userContext,
            'Data' : 'resendShipmentsExport|' + manifestdate
        }

        $.post({ 
            url: clientService.config.ShipExecServiceUrl + '/UserMethod',
            data,
            headers: clientService.authorizationToken()          
		}).done(function (result) {
			//debugger;
			if (result != null && result != 'undefined' && result != '')
            {
                alert(result.Data);
            }
            else
            {
                alert("No data was returned from manifest data export call");
            } 
		}).fail(function (err) {
		    //debugger;
			//alert(e.message);
            alert("The process is taking longer than usual to complete but will continue to run on the server until all records are transferred");
		});

        showDelayMessage();
    }

// Source: JANIE & JACK (JanieJack)_JanieJack Reference Rule_82.cs
// this.NewShipment(shipmentRequest) // returns void
   this.NewShipment = function (shipmentRequest) {
       var shipment = new Shipment(shipmentRequest);  
       shipmentRequest.Packages[0].ShipperReference = shipment.UserEmail;
   }

// get UserEmail() // returns string
   get UserEmail() { return this.GetEmail(); }

// GetEmail() // returns string
   GetEmail(){
      var emailText = "";
      var tags = document.getElementsByTagName("a"); 
      Array.from(tags).forEach(function(tag){
         if(tag.innerHTML.includes("Hello")) emailText = tag.innerHTML.replace("Hello", "").replace("!","").trim(); 
      });
      return emailText;
   }

// GetCustom(fieldName, customDataType) // returns string
   GetCustom(fieldName, customDataType) {
      var returnValue = "";
      var customData = null;
      if (customDataType === "User") customData = this.ShipmentRequest.PackageDefaults.OriginAddress.CustomData;
      if (customDataType === "To") customData = this.ShipmentRequest.PackageDefaults.Consignee.CustomData;

      if (customData) {
         customData.forEach(function (customField) {
            if (fieldName === customField.Key) returnValue = customField.Value;
         })
      }
      return returnValue;
   }

// Source: Land Home Financial Services (LHFS)_AdminAcceess_1578.cs
// PreSearchHistory(searchCriteria) // returns void
this.PreSearchHistory = function (searchCriteria) {
        if (!userContext)
        {
            GetUserContext();

        }
        //searchCriteria.WhereClauses.push({ FieldName: 'MiscReference19', FieldValue: userContext.UserId, Operator: 5 });
    };

// Source: Land Home Financial Services (LHFS)_LHFS_Bus_Rules_1579.cs
// PreSearchHistory(searchCriteria) // returns void
this.PreSearchHistory = function (searchCriteria) {
        if (!userContext)
        {
            GetUserContext();

        }
        searchCriteria.WhereClauses.push({ FieldName: 'UserId', FieldValue: userContext.UserId, Operator: 5 });
    };

// GetUserContext() // returns void
function GetUserContext() {
    if (userContext === undefined) {
        userContext = ajaxGet('api/usercontext/GET');
    }
}

// Source: LyondellBasell (LYON)_LYB_Company_Client_Side_1712.cs
// PreSearchHistory(searchCriteria) // returns void
this.PreSearchHistory = function(searchCriteria) {
        console.log("postsearch");
        console.log(this.vm.shippers);
        
        var s = this.vm.shippers;
        for (var x=0; x < s.length -1; x++){
            
            var w = {};
            w.FieldName = 'Shipper';
            w.FieldValue = s[x].Symbol;
            w.Operator = 0;
        
            searchCriteria.WhereClauses.push(w);
        }


        
           
            //s = this.vm.shippers[0].Symbol;
            
            //w.FieldValue = s[0];


            //$('input[type=text][ng-model="vm.miscRef9"]').val(dealerNum);
        

        

    }

// Source: M&T Bank (MTBank)_MTBank_1506.cs
// getUserContext() // returns void; populates the cached userContext via API lookup
function getUserContext() {
    if (userContext == undefined) {
        $.get({ url: "api/usercontext/GET", async: false })
            .done(function (data) {
                userContext = data;
            })
    }
}

// client.userContext: function() // returns the current user context object
userContext: function () {
        var userContextUrl = 'api/usercontext/GET';
        var shipExecServiceUrl = clientService.config.ShipExecServiceUrl;
        if (shipExecServiceUrl.startsWith('http')) {
            var url = new URL(shipExecServiceUrl);
            userContextUrl = url.origin + '/' + userContextUrl;
        }
        var authorizationToken = clientService.authorizationToken();
        return $.get({url: userContextUrl, headers: authorizationToken, async: false}).responseJSON;
    }(),

// Source: MarkenLLP (MLLP)_Inbound Returns CBR_CA_TO_US_2497.cs
// getUserContext() // returns the current user context response
        getUserContext: function () {
            var userContextUrl = client.config.ShipExecServiceUrl.replace('ShippingService', 'usercontext/GET');
            var authorizationToken = client.getAuthorizationToken();
            return $.get({ url: userContextUrl, headers: authorizationToken, async: false }).responseJSON;;
        },

// Source: MarkenLLP (MLLP)_Inbound Returns CBR_PR_TO_US_2506.cs
// getUserContext() // returns the user context object from the usercontext service
        getUserContext: function () {
            var userContextUrl = client.config.ShipExecServiceUrl.replace('ShippingService', 'usercontext/GET');
            var authorizationToken = client.getAuthorizationToken();
            return $.get({ url: userContextUrl, headers: authorizationToken, async: false }).responseJSON;;
        }

// thinClientAPIRequest(method, data, isAsync) // returns the jQuery AJAX request for a ShipExec service call
        thinClientAPIRequest: function (method, data, isAsync) {
            var shipExecServiceUrl = client.config.ShipExecServiceUrl;
            var authorizationToken = client.getAuthorizationToken();
            return $.post({ url: shipExecServiceUrl + '/' + method, data: data, async: isAsync, headers: authorizationToken });
        }

// Source: MarkenLLP (MLLP)_Inbound Returns CBR_US_TO_US_2499.cs
// getUserContext() // returns the current user context object from the usercontext service
        getUserContext: function () {
            var userContextUrl = client.config.ShipExecServiceUrl.replace('ShippingService', 'usercontext/GET');
            var authorizationToken = client.getAuthorizationToken();
            return $.get({ url: userContextUrl, headers: authorizationToken, async: false }).responseJSON;;
        },

// Source: MarkenLLP (MLLP)_Marken_Phase2_CBR_2500.cs
// client.userContext() // returns the current user context JSON synchronously
userContext: function () {
            var userContextUrl = 'api/usercontext/GET';
            var shipExecServiceUrl = clientService.config.ShipExecServiceUrl;
            if (shipExecServiceUrl.startsWith('http')) {
                var url = new URL(shipExecServiceUrl);
                userContextUrl = url.origin + '/' + userContextUrl;
            }
            var authorizationToken = clientService.authorizationToken();
            return $.get({
                url: userContextUrl,
                headers: authorizationToken,
                async: false
            }).responseJSON;
        }(),

// clientService1.getUserContext() // returns the current user context JSON synchronously
getUserContext: function () {
            var userContextUrl = clientService1.config.ShipExecServiceUrl.replace('ShippingService', 'usercontext/GET');
            var authorizationToken = clientService1.getAuthorizationToken();
            return $.get({ url: userContextUrl, headers: authorizationToken, async: false }).responseJSON;;
        },

// Source: MarkenLLP (MLLP)_Marken_Phase3_CBR_2501.cs
// getUserContext() // returns the current user context object from the server
getUserContext: function () {
            var userContextUrl = clientService1.config.ShipExecServiceUrl.replace('ShippingService', 'usercontext/GET');
            var authorizationToken = clientService1.getAuthorizationToken();
            return $.get({
                url: userContextUrl,
                headers: authorizationToken,
                async: false
            }).responseJSON;;
        }

// PassWorldeaseTrackingNumber(docboxtrackingNumber) // returns nothing; sends a user-context-aware request and displays results
function PassWorldeaseTrackingNumber(docboxtrackingNumber) {
        debugger;

        var jsonData;

        var dataObject = {
            ConsolidationTrackingNumber: docboxtrackingNumber,
            UserContext: clientService1.userContext
        };

        var payload = JSON.stringify(dataObject);

        debugger;

        var data = {
            Data: payload,
            UserContext: clientService1.userContext
        };

        // Show the spinner
        $("#loadingSpinner").show();

        var ajaxRequest = clientService1.thinclientService1APIRequest('UserMethod', data, true);

        ajaxRequest.done(function (data) {
            jsonData = data.Data;
            $("#loadingSpinner").hide();
            console.log(jsonData);
            setTimeout(function () {
                debugger;
                var dataObj = JSON.parse(jsonData.replaceAll('&quot;', '"'));
                var htmlString = '<div>Total Invoice Amount: '
                    + dataObj.Total
                    + ' '
                    + dataObj.TotalUOM
                    + '<br /><hr />';
                var i = 0, len = dataObj.TrackingNumbers.length;
                for (; i < len; i++) {
                    var currentTracking = dataObj.TrackingNumbers[i];
                    var itemString = currentTracking.TrackingNumber
                    htmlString += itemString + "<br />";
                }
                htmlString += '</div>';
                $('#infoModalLabel').html(htmlString);
                $('#infoModal').modal('show');

            }, 50);
        }).fail(function (xhr, status, error) {
            console.error("Error:", error);
            $("#loadingSpinner").hide();
            alert("Failed to get data.");

        }).always(function () {
            // Hide the spinner regardless of success or failure
            $("#loadingSpinner").hide();
        });
    }

// Source: MarkenLLP2 (MLLP2)_Inbound Returns CBR_CA_TO_US_1915.cs
// GetUserShipper(selectedStudy, shipmentRequest) // returns void
    function GetUserShipper(selectedStudy,shipmentRequest) {
       var jsonData;
       var dataObject = {
                    StudyReferenceCode:selectedStudy, 
                    UserContext: client.userContext
                };
 
                var payload = JSON.stringify(dataObject);
                debugger;
                var data = { Data: payload, UserContext: client.userContext };
 
                var ajaxRequest = client.thinClientAPIRequest('UserMethod', data, true);
            
                ajaxRequest.done(function (data) {
 
             jsonData = data.Data;
                console.log(jsonData);
               $('select[ng-model="vm.currentShipment.PackageDefaults.Shipper"]').val(jsonData);
               shipmentRequest.PackageDefaults.Shipper=$('select[ng-model="vm.currentShipment.PackageDefaults.Shipper"] option:selected').val().split(":")[1];

                });
            
    }

// Source: MarkenLLP2 (MLLP2)_Inbound Returns CBR_EU_TO_UK_1916.cs
// GetUserShipper(selectedStudy, shipmentRequest) // returns undefined
    function GetUserShipper(selectedStudy,shipmentRequest) {
       var jsonData;
       var dataObject = {
                    StudyReferenceCode:selectedStudy, 
                    UserContext: client.userContext
                };
 
                var payload = JSON.stringify(dataObject);
                debugger;
                var data = { Data: payload, UserContext: client.userContext };
 
                var ajaxRequest = client.thinClientAPIRequest('UserMethod', data, true);
            
                ajaxRequest.done(function (data) {
 
             jsonData = data.Data;
                console.log(jsonData);
               $('select[ng-model="vm.currentShipment.PackageDefaults.Shipper"]').val(jsonData);
               shipmentRequest.PackageDefaults.Shipper=$('select[ng-model="vm.currentShipment.PackageDefaults.Shipper"] option:selected').val().split(":")[1];

                });
            
    }

// getUserContext() // returns the responseJSON from the usercontext service request
        getUserContext: function () {
            var userContextUrl = client.config.ShipExecServiceUrl.replace('ShippingService', 'usercontext/GET');
            var authorizationToken = client.getAuthorizationToken();
            return $.get({ url: userContextUrl, headers: authorizationToken, async: false }).responseJSON;;
        },

// Source: McKinsey & Co., Inc (mcKinsey)_McKinsey Reference Rule_64.cs
// this.PreSearchHistory(searchCriteria) // returns void
    this.PreSearchHistory = function(searchCriteria) {
        //searchCriteria.WhereClauses.push({ FieldName: 'UserId', FieldValue: userContext.UserId, Operator: 0 });
    }

// Source: Meta Platforms Facebook (METAPLATFORMSFACEBOOK)_Meta Return Client Rules Phase 2_1857.cs
// updateConsigneeStateProvince(country) // returns void
        function updateConsigneeStateProvince(country) {
            this.consigneeStateProvince = [];
            if (country == 'US') this.consigneeStateProvince = client.shipping.stateProvince.unitedStates;
            else if (country == 'CA') this.consigneeStateProvince = client.shipping.stateProvince.canada;
            this.profile.FieldOptions['Consignee.Phone'].IsRequired = country == 'CA';
        }

// clearNameAddress(shipmentRequest, nameAddress) // returns void
        function clearNameAddress(shipmentRequest, nameAddress) {
            if (nameAddress == 'Consignee') {
                shipmentRequest.PackageDefaults.Consignee = {
                    Company: this.profile.FieldOptions['Consignee.Company'].DefaultValue,
                    Email: client.userName
                }
                shipmentRequest.Packages[this.packageIndex].MiscReference5 = '';
            }
            client.shipping.contact.focus();
        }

// Source: MGMResorts (MGMRESOR)_Inbound Client Rules_1648.cs
// PreSearchHistory(searchCriteria) // returns undefined
    this.PreSearchHistory = function(searchCriteria) {
        //searchCriteria.WhereClauses.push({ FieldName: 'UserId', FieldValue: userContext.UserId, Operator: 0 });   // handled in mgt studio settings
    };

// getUserContext() // returns undefined
    function getUserContext() {
        if (userContext == undefined) {
            userContext = ajaxGet('api/usercontext/GET');
        }
    }

// Source: MGMResorts (MGMRESOR)_Outbound Client Rules_1649.cs
// PreSearchHistory(searchCriteria) // returns undefined
    this.PreSearchHistory = function(searchCriteria) {
        //searchCriteria.WhereClauses.push({ FieldName: 'UserId', FieldValue: userContext.UserId, Operator: 0 });   // handled in mgt studio settings
    };

// Source: Michigan State University (MSU)_MSU Client_1665.cs
// PreSearchHistory(searchCriteria) // returns void
        this.PreSearchHistory = function (searchCriteria) {
            GetUserContext();
            searchCriteria.WhereClauses.push({ FieldName: 'UserId', FieldValue: userContext.UserId, Operator: 0 });
    };

// Source: NAGINA INTERNATIONAL (NAGINA)_ISMEA_FTP_1585.cs
// getUserContext() // returns void; populates userContext for later requests
function getUserContext() {
        debugger;
        if (userContext === undefined) {
            $.get({ url: "api/usercontext/GET", async:false })
            .done(function (data) {
               userContext = data;
            })
        }
    }

// getFTPUploaded() // returns void; sends a request including UserContext
function getFTPUploaded() {
        var selectedShipper = document.getElementById("Shipper").value;
		var shipper = '';
		debugger;
		if (selectedShipper !== "")
		{
			selectedShipper = JSON.parse(selectedShipper);
			shipper = selectedShipper.Name;
		}
        var data = { Action: 'ExportFTP', Shipper: shipper };

        $.post('api/ShippingService/UserMethod',
            {UserContext: userContext, Data: JSON.stringify(data) },
            function(response, status, xhr) {
            }
        ).done(function(response) {
            debugger;
            var resp = JSON.stringify(response);
            resp = JSON.parse(resp);
            debugger;
            if (resp.Data !== "" && resp.Data !== undefined)
            {
                alert('Lastest File: \n' + DecodeReturnString(resp.Data).substring(0,resp.Data.length -1));
            }
            else if (resp.ErrorMessage !== undefined)
            {
                if (resp.ErrorCode == '104')
                {
                    alert('Lastest File: \nNo files found!');
                }
                else
                {
                    alert(resp.ErrorMessage);
                }
            }
        });
    }

// Source: NetApp, Inc (NetApp)_Netapp Reference Business Rule_61.cs
// GetShipment(shipmentRequest) // returns Shipment object with user-context custom field getters
    function GetShipment(shipmentRequest) {

            class Shipment {
               constructor(shipmentRequest) {
                  this.ShipmentRequest = shipmentRequest;
               }
    
               get UserRef1() { return this.GetCustom("Custom1", "User"); }
               get UserRef2() { return this.GetCustom("Custom2", "User"); }
               get UserRef3() { return this.GetCustom("Custom3", "User"); }
               get UserRef4() { return this.GetCustom("Custom4", "User"); }
               get UserRef5() { return this.GetCustom("Custom5", "User"); }
               get UserRef6() { return this.GetCustom("Custom6", "User"); }
               get UserRef7() { return this.GetCustom("Custom7", "User"); }
               get UserRef8() { return this.GetCustom("Custom8", "User"); }
               get UserRef9() { return this.GetCustom("Custom9", "User"); }
               get UserRef10() { return this.GetCustom("Custom10", "User"); }
    
               get ToRef1() { return this.GetCustom("Custom1", "To"); }
               get ToRef2() { return this.GetCustom("Custom2", "To"); }
               get ToRef3() { return this.GetCustom("Custom3", "To"); }
               get ToRef4() { return this.GetCustom("Custom4", "To"); }
               get ToRef5() { return this.GetCustom("Custom5", "To"); }
               get ToRef6() { return this.GetCustom("Custom6", "To"); }
               get ToRef7() { return this.GetCustom("Custom7", "To"); }
               get ToRef8() { return this.GetCustom("Custom8", "To"); }
               get ToRef9() { return this.GetCustom("Custom9", "To"); }
               get ToRef10() { return this.GetCustom("Custom10", "To"); }
    
               GetCustom(fieldName, customDataType) {
                  var returnValue = "";
                  var customData = null;
                  if (customDataType === "User") customData = this.ShipmentRequest.PackageDefaults.OriginAddress.CustomData;
                  if (customDataType === "To") customData = this.ShipmentRequest.PackageDefaults.Consignee.CustomData;
    
                  if (customData) {
                     customData.forEach(function (customField) {
                        if (fieldName === customField.Key) returnValue = customField.Value;
                     })
                  }
                  return returnValue;
               }
            }
    
            return new Shipment(shipmentRequest);
         }

// Source: Nexus (NEXUS)_Nexus CBR 2.3 Fix_1760.cs
// GetAddressBook(addressBookName, context) // returns the address book object or undefined
this.GetAddressBook = function (addressBookName, context) {
        var searchCriteria = { WhereClauses: [{ FieldName: "Name", FieldValue: addressBookName, Operator: 0 }] };
        var data = { SearchCriteria: searchCriteria, CompanyId: context.CompanyId, SiteId: context.SiteId, UserId: context.UserId };
        var response = this.thinClientAPIRequest('GetAddressBooks_Shared', data, false);
        if (response.ErrorCode != 0) {
            client.alert.Danger(response.ErrorMessage);
        } else {
            if (response.TotalRecords == 0) {
                client.alert.Danger('Address Book: "' + addressBookName + '" does not exist');                
            } else {
                return response.AddressBooks[0];
            }
        }
    }

// GetAddressBookEntries(addressBook, context, codeReference) // returns address book entries array or undefined
this.GetAddressBookEntries = function (addressBook, context, codeReference) {
        if (addressBook != undefined) {
            var searchCriteria = { WhereClauses: [{ FieldName: "Code", FieldValue: codeReference, Operator: 0 }] };
            var data = { AddressBookIds: [addressBook.Id], SearchCriteria: searchCriteria, CompanyId: context.CompanyId, SiteId: context.SiteId, UserId: context.UserId };
            var response = this.thinClientAPIRequest('GetAddressBookEntries_Shared', data, false);
            if (response.ErrorCode != 0) {
                client.alert.Danger(response.ErrorMessage);
            } else {
                if (response.TotalRecords == 0) {
                    client.alert.Danger('Address Book Entries: No address found in "' + addressBookName + '" address book');
                } else {
                    return response.AddressBookEntries;
                }
            }
        }
    }

// userContext() // returns the current user context object
userContext: function () {
            var userContextUrl = 'api/usercontext/GET';
            var shipExecServiceUrl = clientService.config.ShipExecServiceUrl;
            if (shipExecServiceUrl.startsWith('http')) {
                var url = new URL(shipExecServiceUrl);
                userContextUrl = url.origin + '/' + userContextUrl;
            }
            var authorizationToken = clientService.authorizationToken();
            return $.get({url: userContextUrl, headers: authorizationToken, async: false}).responseJSON;
        }()

// Source: Nexus (NEXUS)_Nexus CBR Aug 2022_1798.cs
// GetAddressBook(addressBookName, context) // returns the first matching address book or undefined
    this.GetAddressBook = function (addressBookName, context) {
        var searchCriteria = { WhereClauses: [{ FieldName: "Name", FieldValue: addressBookName, Operator: 0 }] };
        var data = { SearchCriteria: searchCriteria, CompanyId: context.CompanyId, SiteId: context.SiteId, UserId: context.UserId };
        var response = this.thinClientAPIRequest('GetAddressBooks', data, false);
        if (response.ErrorCode != 0) {
            client.alert.Danger(response.ErrorMessage);
        } else {
            if (response.TotalRecords == 0) {
                client.alert.Danger('Address Book: "' + addressBookName + '" does not exist');                
            } else {
                return response.AddressBooks[0];
            }
        }
    }

// GetAddressBookEntries(addressBook, context, codeReference) // returns matching address book entries or undefined
    this.GetAddressBookEntries = function (addressBook, context, codeReference) {
        if (addressBook != undefined) {
            var searchCriteria = { WhereClauses: [{ FieldName: "Code", FieldValue: codeReference, Operator: 0 }] };
            var data = { AddressBookIds: [addressBook.Id], SearchCriteria: searchCriteria, CompanyId: context.CompanyId, SiteId: context.SiteId, UserId: context.UserId };
            var response = this.thinClientAPIRequest('GetAddressBookEntries', data, false);
            if (response.ErrorCode != 0) {
                client.alert.Danger(response.ErrorMessage);
            } else {
                if (response.TotalRecords == 0) {
                    client.alert.Danger('Address Book Entries: No address found in "' + addressBookName + '" address book');
                } else {
                    return response.AddressBookEntries;
                }
            }
        }
    }

// Source: NikeRevship (NIKEREVSHIP)_NikeRevshipClientBusinessRule_1786.cs
// function ReplaceAccountNumberWithXXXXXX() // returns void
function ReplaceAccountNumberWithXXXXXX() {
		const observer = new MutationObserver(() => {
			// Select all child tracking numbers
			const trackingNumberElements = document.querySelectorAll('div.ng-binding');
			trackingNumberElements.forEach(element => {
				const text = element.textContent.trim();
				if (text.length === 18) {
					element.textContent = text.substring(0, 3) + 'XXXXXX' + text.substring(9);
				}
			});

			// Select all lead tracking numbers
			const tableCells = document.querySelectorAll('td.ng-binding');
			tableCells.forEach(cell => {
				const text = cell.textContent.trim();
				if (text.length === 18) {
					cell.textContent = text.substring(0, 3) + 'XXXXXX' + text.substring(9);
				}
			});
		});

		// Start observing the body for changes
		observer.observe(document.body, { childList: true, subtree: true });
	}

// Source: Office Depot (ODP)_Sample_CBR_2454.cs
// ValidateCostCenterAccount() // returns boolean
function ValidateCostCenterAccount() {
        
        var customData = vm.profile.UserInformation.Address.CustomData;
        
        var returnValue = true;
        if (customData.length > 0) {
          
            var costCenters = getValueByKey("CustomData1", customData)
            var accounts = getValueByKey("CustomData3", customData);
            var costCenterCount = 0;
            if(costCenters != "")
            { 
                costCenterCount =  costCenters.indexOf(",") > 0 ? costCenters.split(",").length : 1;
            }
            var accountsCount = 0;
            if(accounts != "")
            { 
                accountsCount =  accounts.indexOf(",") > 0 ? accounts.split(",").length : 1;
            }
            
            if (costCenterCount != accountsCount) {
                returnValue =  false;
            }
        }
        return returnValue
    }

// getShippersFromCostCenter() // returns array
function getShippersFromCostCenter() {
        var returnData = [];
        var customData = vm.profile.UserInformation.Address.CustomData;
        var shippers = vm.profile.Shippers;
        returnData = shippers;
        if (customData.length > 0) {
            var customShippers = getValueByKey("CustomData3", customData);
            var customShipperData = customShippers.split(",");
            if (customShipperData.length > 0) {
                returnData = shippers.filter(item => customShipperData.some(cs => cs.toLowerCase()  === item.Symbol.toLowerCase()));
            }
        }

        return returnData;
    }

// getCostCenterByShipper(selectedShipper) // returns string
function getCostCenterByShipper(selectedShipper) {
        
        var customData = vm.profile.UserInformation.Address.CustomData;
       
        var shippers = getValueByKey("CustomData3", customData);
       
        var shipperData = shippers.split(",");
        var index = shipperData.indexOf(selectedShipper);
        var costCenters = getValueByKey("CustomData1", customData);
        var costCenterData = costCenters.split(",");
        var costCenter = ''
        if (costCenterData.length > 0 && costCenterData.length >= index) {
            costCenter = costCenterData[index].trim();
        }
        return costCenter;
    }

// getEmployeeId() // returns string
function getEmployeeId() {
        var customData = vm.profile.UserInformation.Address.CustomData;
        var employeeId = getValueByKey("CustomData2", customData).trim();
        return employeeId;
    }

// checkShipperReleaseAvailable() // returns boolean
function checkShipperReleaseAvailable()
    {
        var profileName = vm.profile.Name;
        var returnValue = false;
        if(profileName == "Warehouse Operations Profile" 
            || profileName == "Private Fleet Profile")
            {
                returnValue = true;
            }
        return returnValue;
    }

// Source: Philip Morris USA (PMUSAInternational)_PMUSAInt_Reference_Rules_55.cs
// GetCustom(fieldName, customDataType) // returns the matching custom field value or an empty string
   GetCustom(fieldName, customDataType) {
      var returnValue = "";
      var customData = null;
      
      if (customDataType === "User")
      {
          if(this.ShipmentRequest.PackageDefaults.OriginAddress)
          {
            customData = this.ShipmentRequest.PackageDefaults.OriginAddress.CustomData;
          }
      }
      
      if (customDataType === "To") 
      {
          if(this.ShipmentRequest.PackageDefaults.Consignee)
          {
            customData = this.ShipmentRequest.PackageDefaults.Consignee.CustomData;
          }
      }
      
      if (customData) {
         customData.forEach(function (customField) {
            if (fieldName === customField.Key) returnValue = customField.Value;
         })
      }
      return returnValue;
   }

// Source: Philip Morris USA Domestic (PMUSADomestic)_PMUSADom_Reference_Rules_56.cs
// function ClientBusinessRules() // returns a ClientBusinessRules constructor/initializer
function ClientBusinessRules() {
  
   this.NewShipment = function (shipmentRequest) {
       var shipment = new Shipment(shipmentRequest);  
       shipmentRequest.Packages[0].ShipperReference = shipment.UserRef1;
       shipmentRequest.Packages[0].ConsigneeReference  = shipment.UserRef2;
       shipmentRequest.Packages[0].MiscReference1 = shipment.UserRef3;
       //shipmentRequest.Packages[0].ShipperReference = shipment.UserEmail;
       // alert(JSON.stringify(shipment)); 
   }
   
   this.AddPackage = function(shipmentRequest, packageIndex) {
       shipmentRequest.Packages[packageIndex].ShipperReference = shipmentRequest.Packages[0].ShipperReference; 
       shipmentRequest.Packages[packageIndex].ConsigneeReference = shipmentRequest.Packages[0].ConsigneeReference;
      // alert("PMUSADom");
   }
   
   
   
   
class Shipment {
   constructor(shipmentRequest) {
      this.ShipmentRequest = shipmentRequest;
   }

 
   get UserEmail() { return this.GetEmail(); }

   get UserRef1() { return this.GetCustom("Custom1", "User"); }
   get UserRef2() { return this.GetCustom("Custom2", "User"); }
   get UserRef3() { return this.GetCustom("Custom3", "User"); }
   get UserRef4() { return this.GetCustom("Custom4", "User"); }
   get UserRef5() { return this.GetCustom("Custom5", "User"); }
   get UserRef6() { return this.GetCustom("Custom6", "User"); }
   get UserRef7() { return this.GetCustom("Custom7", "User"); }
   get UserRef8() { return this.GetCustom("Custom8", "User"); }
   get UserRef9() { return this.GetCustom("Custom9", "User"); }
   get UserRef10() { return this.GetCustom("Custom10", "User"); }

   get ToRef1() { return this.GetCustom("Custom1", "To"); }
   get ToRef2() { return this.GetCustom("Custom2", "To"); }
   get ToRef3() { return this.GetCustom("Custom3", "To"); }
   get ToRef4() { return this.GetCustom("Custom4", "To"); }
   get ToRef5() { return this.GetCustom("Custom5", "To"); }
   get ToRef6() { return this.GetCustom("Custom6", "To"); }
   get ToRef7() { return this.GetCustom("Custom7", "To"); }
   get ToRef8() { return this.GetCustom("Custom8", "To"); }
   get ToRef9() { return this.GetCustom("Custom9", "To"); }
   get ToRef10() { return this.GetCustom("Custom10", "To"); }

   GetEmail(){
      var emailText = "";
      var tags = document.getElementsByTagName("a"); 
      Array.from(tags).forEach(function(tag){
         if(tag.innerHTML.includes("Hello")) emailText = tag.innerHTML.replace("Hello", "").replace("!","").trim(); 
      });
      return emailText;
   }
   
   GetCustom(fieldName, customDataType) {
      var returnValue = "";
      var customData = null;
      if (customDataType === "User") customData = this.ShipmentRequest.PackageDefaults.OriginAddress.CustomData;
      if (customDataType === "To") customData = this.ShipmentRequest.PackageDefaults.Consignee.CustomData;

      if (customData) {
         customData.forEach(function (customField) {
            if (fieldName === customField.Key) returnValue = customField.Value;
         })
      }
      return returnValue;
   }
// End Shipment Class    
}

        
         
}


// Source: Purolator (PURT)_PurolatorUserMethod_1914.cs
// customUserMethod() // returns undefined
    function customUserMethod () {
        debugger;
        var dateSelected = $('#exportDate').val();
        var shipper = $('#gateway :selected').text();
        var carrier = $('#carrier :selected').text();
        
        if (carrier === 'UPS')
        {
            carrier = 'CONNECTSHIP_UPS.UPS';
        }
        else
        {
            carrier = 'CONNECTSHIP_DHLXMLPI.DHL';
        }

        var data = { DateSelected: dateSelected, Shipper: shipper, Carrier: carrier};
        var payload = { UserContext: client.userContext, Data: JSON.stringify(data) };
        var result = client.thinClientAPIRequest('UserMethod', payload, false).responseJSON;
        
        debugger;
        if (result.ErrorCode > 0 || result == undefined) {
            client.alert.Danger(result.ErrorCode + ': ' + result.ErrorMessage);
        } else if (result.Message != undefined || result.Message != "") {
            client.alert.Success(result.Message);
        } else {
            client.alert.Success(result.Message);
        }
    }

// getUserContext() // returns undefined
        getUserContext: function () {
            var userContextUrl = this.config.ShipExecServiceUrl.replace('ShippingService', 'usercontext/GET');
            var authorizationToken = this.getAuthorizationToken();
            this.userContext = $.get({ url: userContextUrl, headers: authorizationToken, async: false }).responseJSON;
        },

// Source: QVC, Inc (QVC)_QVC Reference Rule_37.cs
// GetShipment(shipmentRequest) // returns a Shipment object with custom reference accessors
    function GetShipment(shipmentRequest) {

            class Shipment {
               constructor(shipmentRequest) {
                  this.ShipmentRequest = shipmentRequest;
               }
    
               get UserRef1() { return this.GetCustom("Custom1", "User"); }
               get UserRef2() { return this.GetCustom("Custom2", "User"); }
               get UserRef3() { return this.GetCustom("Custom3", "User"); }
               get UserRef4() { return this.GetCustom("Custom4", "User"); }
               get UserRef5() { return this.GetCustom("Custom5", "User"); }
               get UserRef6() { return this.GetCustom("Custom6", "User"); }
               get UserRef7() { return this.GetCustom("Custom7", "User"); }
               get UserRef8() { return this.GetCustom("Custom8", "User"); }
               get UserRef9() { return this.GetCustom("Custom9", "User"); }
               get UserRef10() { return this.GetCustom("Custom10", "User"); }
    
               get ToRef1() { return this.GetCustom("Custom1", "To"); }
               get ToRef2() { return this.GetCustom("Custom2", "To"); }
               get ToRef3() { return this.GetCustom("Custom3", "To"); }
               get ToRef4() { return this.GetCustom("Custom4", "To"); }
               get ToRef5() { return this.GetCustom("Custom5", "To"); }
               get ToRef6() { return this.GetCustom("Custom6", "To"); }
               get ToRef7() { return this.GetCustom("Custom7", "To"); }
               get ToRef8() { return this.GetCustom("Custom8", "To"); }
               get ToRef9() { return this.GetCustom("Custom9", "To"); }
               get ToRef10() { return this.GetCustom("Custom10", "To"); }
    
               GetCustom(fieldName, customDataType) {
                  var returnValue = "";
                  var customData = null;
                  if (customDataType === "User") customData = this.ShipmentRequest.PackageDefaults.OriginAddress.CustomData;
                  if (customDataType === "To") customData = this.ShipmentRequest.PackageDefaults.Consignee.CustomData;
    
                  if (customData) {
                     customData.forEach(function (customField) {
                        if (fieldName === customField.Key) returnValue = customField.Value;
                     })
                  }
                  return returnValue;
               }
            }
    
            return new Shipment(shipmentRequest);
         }

// Source: R.BCC EXPORTERS PRIVATE LIMITED (R.BCC)_ISMEA_FTP_1716.cs
// getUserContext() // returns void
function getUserContext() {
        debugger;
        if (userContext === undefined) {
            $.get({ url: "api/usercontext/GET", async:false })
            .done(function (data) {
               userContext = data;
            })
        }
    }

// Source: Rainier Arms (RNRARMS)_RainierArms shipping CBR_1640.cs
// function client.userContext() // returns the current user context object
        userContext: function () {
            var userContextUrl = 'api/usercontext/GET';
            var shipExecServiceUrl = clientService.config.ShipExecServiceUrl;
            if (shipExecServiceUrl.startsWith('http')) {
                var url = new URL(shipExecServiceUrl);
                userContextUrl = url.origin + '/' + userContextUrl;
            }
            var authorizationToken = clientService.authorizationToken();
            return $.get({url: userContextUrl, headers: authorizationToken, async: false}).responseJSON;
        }()

// Source: Roadie XD (ROADIEXD)_Company Client Business Rules_2453.cs
// getUserContext() // returns undefined; sets client.userContext from a usercontext API response
        getUserContext: function () {
            var userContextUrl = this.config.ShipExecServiceUrl.replace('ShippingService', 'usercontext/GET');
            var authorizationToken = this.getAuthorizationToken();
            this.userContext = $.get({ url: userContextUrl, headers: authorizationToken, async: false }).responseJSON;
        },

// Source: RoniCo (RONI)_test_1896.cs
// NewShipment(shipmentRequest) // returns void
    this.NewShipment = function(shipmentRequest) { 
        shipmentRequest.PackageDefaults.Consignee = this.vm.profile.UserInformation.Address;
    }

// Source: Russell Investments (RUSSELL)_Russell Investments Client Rules_1872.cs
// NewShipment(shipmentRequest) // returns undefined; sets MiscReference20 to the current client user name
    this.NewShipment = function(shipmentRequest) { 
        this.NewShipment = function(shipmentRequest) { 
        shipmentRequest.Packages[0].MiscReference20 = client.userName;
        }
    }

// Source: SAVERS (SAVERS)_Savers Client Side Rules_1715.cs
// setConsigneeCountries(selectedCountries) // returns void
    function setConsigneeCountries(selectedCountries)
    {
        var countryStringValue = ('string:' + selectedCountries.join('|string:')).split('|');
        var interval = setInterval(function()

        {
            var consigneeCountries = $('name-address[address-property-name="Consignee"] select[name="Country"] option');
            if (consigneeCountries.length)
            {
                clearInterval(interval);
                Array.from(consigneeCountries).forEach(function(opt)

                {
                    if (opt.value != '')
                    {
                        if (!countryStringValue.includes(opt.value))
                        {
                            opt.remove();
                        }
                    }
                });
            }
        }, 50);            
    }

// Source: Sazerac (Sazerac)_Sazerac CBR_1680.cs
// userContext() // returns the current user context JSON object
userContext: function () {
            var userContextUrl = 'api/usercontext/GET';
            var shipExecServiceUrl = clientService.config.ShipExecServiceUrl;
            if (shipExecServiceUrl.startsWith('http')) {
                var url = new URL(shipExecServiceUrl);
                userContextUrl = url.origin + '/' + userContextUrl;
            }
            var authorizationToken = clientService.authorizationToken();
            return $.get({url: userContextUrl, headers: authorizationToken, async: false}).responseJSON;
        }()

// Source: Scheels (SCHE)_Scheels_CBR_Vendors_1892.cs
// this.Shipping() // returns void
this.Shipping = function () {
        
         var userContext = client.userContext;
          this.vm.loadVendorConsignee = function () {
            console.log(this.profile.UserInformation);
           
            this.currentShipment.PackageDefaults.OriginAddress = {
                
                Company: this.profile.UserInformation.Address.Company,
                Contact: this.profile.UserInformation.Address.Contact,
                Address1: this.profile.UserInformation.Address.Address1,
                Address2: this.profile.UserInformation.Address.Address2,
                Address3: this.profile.UserInformation.Address.Address3,
                City: this.profile.UserInformation.Address.City,
                StateProvince: this.profile.UserInformation.Address.StateProvince,
                PostalCode: this.profile.UserInformation.Address.PostalCode,
                Country: this.profile.UserInformation.Address.Country,
                Email: this.profile.UserInformation.Address.Email,
                Phone: this.profile.UserInformation.Address.Phone
            }
            
            var consigneeaddress = consigneeAddress(); 
               
           this.currentShipment.PackageDefaults.Consignee = {

                Company: consigneeaddress[0].Company,
                Contact: consigneeaddress[0].Contact,
                Address1: consigneeaddress[0].Address1,
                Address2: consigneeaddress[0].Address2,
                Address3: consigneeaddress[0].Address3,
                City: consigneeaddress[0].City,
                StateProvince: consigneeaddress[0].StateProvince,
                PostalCode: consigneeaddress[0].PostalCode,
                Country: consigneeaddress[0].Country,
                Email: consigneeaddress[0].Email,
                Phone: consigneeaddress[0].Phone,
                Account : consigneeaddress[0].Account
            }
            
            var shipper =  $('select[ng-model="vm.currentShipment.PackageDefaults.Shipper"]').val();
            shipper = shipper.replace('string:', '');
           debugger;
            if(shipper !== '' && shipper !== 'SCH_FARGO'){
            
               this.currentShipment.PackageDefaults.ThirdPartyBilling = true;
               
               var thirdpartybillingaddress = thirdPartyBillingAddress(); 
               
               this.currentShipment.PackageDefaults.ThirdPartyBillingAddress = {
    
                    Company: thirdpartybillingaddress[0].Company,
                    Contact: thirdpartybillingaddress[0].Contact,
                    Address1: thirdpartybillingaddress[0].Address1,
                    Address2: thirdpartybillingaddress[0].Address2,
                    Address3: thirdpartybillingaddress[0].Address3,
                    City: thirdpartybillingaddress[0].City,
                    StateProvince: thirdpartybillingaddress[0].StateProvince,
                    PostalCode: thirdpartybillingaddress[0].PostalCode,
                    Country: thirdpartybillingaddress[0].Country,
                    Email: thirdpartybillingaddress[0].Email,
                    Phone: thirdpartybillingaddress[0].Phone,
                    Account : thirdpartybillingaddress[0].Account
                }
            }
            else
            {
                this.currentShipment.PackageDefaults.ThirdPartyBilling = false;
            }
            
        }
    }

// var consigneeAddress() // returns address book entries array
var consigneeAddress = function () {
       
        var userContext = client.userContext; 
        var shipper =  $('select[ng-model="vm.currentShipment.PackageDefaults.Shipper"]').val();
        if(shipper != ''){
         shipper = shipper.replace('string:', '');
        }
        else{
         shipper = 'SCH_FARGO';
        }
            
        var address =  GetAddressBookEntries('Return', userContext, shipper);
        return address;
    }

// var thirdPartyBillingAddress() // returns address book entries array
var thirdPartyBillingAddress = function () {
        var userContext = client.userContext; 
        var shipper = 'SCH_FARGO';
        var address =  GetAddressBookEntries('Return', userContext, shipper);
        return address;
    }

// var GetAddressBookEntries(addressBookName, context, shipper) // returns AddressBookEntries array
var GetAddressBookEntries = function (addressBookName, context, shipper) {
        var searchCriteria = { WhereClauses: [{ FieldName: "Name", FieldValue: addressBookName, Operator: 0 }] };
        var data = { CompanyId: context.CompanyId, UserId: context.UserId };
        var response = thinClientAPIRequest('GetAddressBooks', data, false).responseJSON;
        if (response.TotalRecords == 0) {
            throw ({ message: 'Address Book:"' + addressBookName + '" does not exists'});
        } else {
            var addressBookId = response.AddressBooks[0].Id;
            
            searchCriteria = { Skip: 0, WhereClauses: [{FieldName: "Code", FieldValue: shipper, Operator: 0}], OrderByClauses: [] };
            data = { AddressBookIds: [addressBookId], SearchCriteria: searchCriteria, UserContext :{Machine:""} , CompanyId: context.CompanyId, UserId: context.UserId };
            response = thinClientAPIRequest('GetAddressBookEntries', data, false).responseJSON;
            
            if (response.TotalRecords == 0) {
                throw ({ message: 'Address Book:"' + addressBookName + '" does not have any address entry'});
            } else {
                return response.AddressBookEntries;
            }
        }
    }

// Source: SE Demo (SE_DEMO)_CBR_SE_Demo_1818.cs
// GetUserContext() // returns the current user context object or undefined
    this.GetUserContext = function () {
        if (this.vm.userContext) return this.vm.userContext;
        if (this.vm.UserInformation) return this.vm.UserInformation;
    }

// Source: SE Demo APAC (SE_DEMO_APAC)_CBR_SE_Demo_1873.cs
// GetDataManagementContents(apiMethod) // returns undefined
this.GetDataManagementContents = function (apiMethod) {
        var userContext = this.GetUserContext();
        var thinClientAPIRequest = this.thinClientAPIRequest;

        var data = { CompanyId: userContext.CompanyId };
        thinClientAPIRequest(apiMethod, data, true).done(function (result) {
            if (result.ErrorCode == 0) {
                var userObject = { Action: apiMethod, Data: JSON.stringify(result) };
                data = { Data: btoa(JSON.stringify(userObject)), UserContext: userContext };
                thinClientAPIRequest('UserMethod', data, true);//.done(function (result) { debugger; });
            };
        });
    }

// GetUserContext() // returns the current user context object from vm.userContext or vm.UserInformation
this.GetUserContext = function () {
        if (this.vm.userContext) return this.vm.userContext;
        if (this.vm.UserInformation) return this.vm.UserInformation;
    }

// Source: SENTRY INSURANCE-SHO (Sentry)_Sentry Reference Rules_17.cs
// GetCustom(fieldName, customDataType) // returns a string value from user/to custom data or empty string
   GetCustom(fieldName, customDataType) {
      var returnValue = "";
      var customData = null;
      if (customDataType === "User") customData = this.ShipmentRequest.PackageDefaults.OriginAddress.CustomData;
      if (customDataType === "To") customData = this.ShipmentRequest.PackageDefaults.Consignee.CustomData;

      if (customData) {
         customData.forEach(function (customField) {
            if (fieldName === customField.Key) returnValue = customField.Value;
         })
      }
      return returnValue;
   }

// Source: ShipExec Essentials Demo (CF_DEMO)_CBR_SE_Demo_1854.cs
// GetDataManagementContents(apiMethod) // returns void
    this.GetDataManagementContents = function (apiMethod) {
        var userContext = this.GetUserContext();
        var thinClientAPIRequest = this.thinClientAPIRequest;

        var data = { CompanyId: userContext.CompanyId };
        thinClientAPIRequest(apiMethod, data, true).done(function (result) {
            if (result.ErrorCode == 0) {
                var userObject = { Action: apiMethod, Data: JSON.stringify(result) };
                data = { Data: btoa(JSON.stringify(userObject)), UserContext: userContext };
                thinClientAPIRequest('UserMethod', data, true);//.done(function (result) { debugger; });
            };
        });
    }

// GetUserContext() // returns user context object or undefined
    this.GetUserContext = function () {
        if (this.vm.userContext) return this.vm.userContext;
        if (this.vm.UserInformation) return this.vm.UserInformation;
    }

// Source: Soundoff Signal (SOS)_Warehouse Shipping CBR_1803.cs
// GetContainersForFocusedItem(RowId) // returns an array of container indexes or undefined
GetContainersForFocusedItem = (RowId) =>
  {
    const data = this.#AllListItemDataObjects;
    const rows = data.filter(d => d.RowId == RowId)[0];

    //only need the 0 index because the first entry will have the entire container list with it
    if (rows && rows.PackedItems)
    {
      return rows.PackedItems.map(m => m.ContainerIndex);
    }
    return undefined;
  }

// Source: SPEEDTECH (SPEEDTECH)_SpeedTech Client Side Business Rules_1645.cs
// function thinClientAPIRequest(method, data, isAsync) // returns the JSON response from a POST request
function thinClientAPIRequest (method, data, isAsync) {
        var shipExecServiceUrl = clientService.config.ShipExecServiceUrl;
        var authorizationToken = clientService.authorizationToken();
        return $.post({ url: shipExecServiceUrl + '/' + method, data: data, async: isAsync, headers: authorizationToken }).responseJSON
    }

// function authorizationToken() // returns an authorization header object or undefined
authorizationToken: function () {
            if (JSON.parse(window.localStorage.getItem("TCToken")))
                return { 'Authorization': 'Bearer ' + JSON.parse(window.localStorage.getItem("TCToken")).access_token };
        }

// function userContext() // returns the current user context object
userContext: function () {
            var userContextUrl = 'api/usercontext/GET';
            var shipExecServiceUrl = clientService.config.ShipExecServiceUrl;
            if (shipExecServiceUrl.startsWith('http')) {
                var url = new URL(shipExecServiceUrl);
                userContextUrl = url.origin + '/' + userContextUrl;
            }
            var authorizationToken = clientService.authorizationToken();
            return $.get({url: userContextUrl, headers: authorizationToken, async: false}).responseJSON;
        }()

// Source: Standard Industries (STANDARDINDUSTRIES)_GAF_CBR_2457.cs
// function(location) // returns undefined
    this.PageLoaded = function(location) {      
       vm = this.vm;
          debugger;
           if (location === '/shipping')
           {
 $(document).delegate('li.uib-tab.nav-item[heading="Hazmat"] > a', 'click', function (e)
               {
                   debugger;
                       try
                       {
                            var key = vm.profile.UserInformation.Address.CustomData.find(k=> k.Key == 'HAZ_CERT').Key;
                            var value = vm.profile.UserInformation.Address.CustomData.find(k=> k.Key == 'HAZ_CERT').Value;
                            if(key == null || value != 'Y')
                            {
                                alert('Hazardous Shipping Requires User Certification Setup[HAZ_CERT]');
                            }
                        }
                   catch(err)
                   {
                       alert('Hazardous Shipping Requires User Certification Setup[HAZ_CERT]');
                   }
                
                    
                });
           }
    }

// function(loadValue, shipmentRequest, userParams) // returns undefined
    this.PreLoad = function(loadValue, shipmentRequest, userParams) {
        debugger;
          var employeeID;
          var BU;
          var OU;
          var CR;
          var locationCode;
         var CustomData = this.vm.profile.UserInformation.Address.CustomData;
            for (var Custom in CustomData) {
                     if(CustomData[Custom].Key == "Custom1") employeeID = CustomData[Custom].Value;
                      if(CustomData[Custom].Key == "Custom2") BU = CustomData[Custom].Value;
                       if(CustomData[Custom].Key == "Custom3") OU = CustomData[Custom].Value;
                        if(CustomData[Custom].Key == "Custom4") CR = CustomData[Custom].Value;
                         if(CustomData[Custom].Key == "Custom5")  locationCode= CustomData[Custom].Value;
             }
             
         if (CR === undefined || !isFiveDigitNumber(CR)) {
            alert('Invalid Cost Center');
            }
            shipmentRequest.Packages[0].MiscReference3 = employeeID;
        shipmentRequest.Packages[0].ConsigneeReference = CR;
        shipmentRequest.Packages[0].MiscReference2 = CR;
         shipmentRequest.Packages[0].MiscReference4 = BU;
          shipmentRequest.Packages[0].MiscReference5 = locationCode;
    }

// function(shipmentRequest) // returns undefined
    this.NewShipment = function(shipmentRequest) { 
        debugger;
        
         var employeeID;
          var BU;
          var OU;
          var CR;
          var locationCode;
            var CustomData = this.vm.profile.UserInformation.Address.CustomData;
             for (var Custom in CustomData) {
                  if(CustomData[Custom].Key == "Custom1") employeeID = CustomData[Custom].Value;
                      if(CustomData[Custom].Key == "Custom2") BU = CustomData[Custom].Value;
                       if(CustomData[Custom].Key == "Custom3") OU = CustomData[Custom].Value;
                        if(CustomData[Custom].Key == "Custom4") CR = CustomData[Custom].Value;
                         if(CustomData[Custom].Key == "Custom5")  locationCode= CustomData[Custom].Value;
             }
            if (CR === undefined || !isFiveDigitNumber(CR)) {
                  alert('Invalid Cost Center');
            }
                  shipmentRequest.Packages[0].MiscReference3 = employeeID;
        shipmentRequest.Packages[0].ConsigneeReference = CR;
        shipmentRequest.Packages[0].MiscReference2 = CR;
         shipmentRequest.Packages[0].MiscReference4 = BU;
          shipmentRequest.Packages[0].MiscReference5 = locationCode;
 
    }

// Source: STAR INDIA CRAFT (STARINCRFT)_ISMEA_FTP_1583.cs
// getUserContext() // returns void
function getUserContext() {
        debugger;
        if (userContext === undefined) {
            $.get({ url: "api/usercontext/GET", async:false })
            .done(function (data) {
               userContext = data;
            })
        }
    }

// Source: Succinnova Technologies Pvt Ltd (SUCCTECH)_ISMEA_FTP_1635.cs
// getUserContext() // returns void
function getUserContext() {
        debugger;
        if (userContext === undefined) {
            $.get({ url: "api/usercontext/GET", async:false })
            .done(function (data) {
               userContext = data;
            })
        }
    }

// Source: Team D Technologies (TEAMDT)_ISMEA_FTP_1586.cs
// getUserContext() // returns void
function getUserContext() {
        debugger;
        if (userContext === undefined) {
            $.get({ url: "api/usercontext/GET", async:false })
            .done(function (data) {
               userContext = data;
            })
        }
    }

// Source: Tesla (TES)_test 1_1869.cs
// GetPreAlert(element, context) // returns undefined
   function GetPreAlert(element, context){
        debugger;
        var jsonGroup = $(element).attr('group')
        var group = JSON.parse(jsonGroup);
     
        var data = {
            UserContext: client.userContext,
            Data: jsonGroup
        }
        sendPreAlertApi("UserMethod", data , false, context)
        //alert(test.Data);
    }

// this.GroupManager() // returns undefined
    this.GroupManager = function () {
        $('body').delegate('span.glyphicon-bell', 'click', function () {
          this.GetPreAlert(this, client.userContext);
        });
    }

// Source: Texas Roadhouse  (TXRH)_Shipper Reference Mapping_40.cs
// GetCustom(fieldName, customDataType) // returns string
GetCustom(fieldName, customDataType) {
            var returnValue = "";
            var customData = null;
            if (customDataType === "User") customData = this.ShipmentRequest.PackageDefaults.OriginAddress.CustomData;
            if (customDataType === "To") customData = this.ShipmentRequest.PackageDefaults.Consignee.CustomData;

            if (customData) {
                customData.forEach(function (customField) {
                    if (fieldName === customField.Key) returnValue = customField.Value;
                })
            }
            return returnValue;
        }

// get UserRef1() // returns string
get UserRef1() { return this.GetCustom("Custom1", "User"); }

// get UserRef2() // returns string
get UserRef2() { return this.GetCustom("Custom2", "User"); }

// get UserRef3() // returns string
get UserRef3() { return this.GetCustom("Custom3", "User"); }

// get UserRef4() // returns string
get UserRef4() { return this.GetCustom("Custom4", "User"); }

// get UserRef5() // returns string
get UserRef5() { return this.GetCustom("Custom5", "User"); }

// get UserRef6() // returns string
get UserRef6() { return this.GetCustom("Custom6", "User"); }

// get UserRef7() // returns string
get UserRef7() { return this.GetCustom("Custom7", "User"); }

// get UserRef8() // returns string
get UserRef8() { return this.GetCustom("Custom8", "User"); }

// get UserRef9() // returns string
get UserRef9() { return this.GetCustom("Custom9", "User"); }

// get UserRef10() // returns string
get UserRef10() { return this.GetCustom("Custom10", "User"); }

// Source: The Hartford (thehartford)_Hartford Reference Rule_42.cs
// GetShipment(shipmentRequest) // returns a Shipment wrapper used to access user-context custom data
function GetShipment(shipmentRequest) {

        class Shipment {
           constructor(shipmentRequest) {
              this.ShipmentRequest = shipmentRequest;
           }

           get UserRef1() { return this.GetCustom("Custom1", "User"); }
           get UserRef2() { return this.GetCustom("Custom2", "User"); }
           get UserRef3() { return this.GetCustom("Custom3", "User"); }
           get UserRef4() { return this.GetCustom("Custom4", "User"); }
           get UserRef5() { return this.GetCustom("Custom5", "User"); }
           get UserRef6() { return this.GetCustom("Custom6", "User"); }
           get UserRef7() { return this.GetCustom("Custom7", "User"); }
           get UserRef8() { return this.GetCustom("Custom8", "User"); }
           get UserRef9() { return this.GetCustom("Custom9", "User"); }
           get UserRef10() { return this.GetCustom("Custom10", "User"); }

           get ToRef1() { return this.GetCustom("Custom1", "To"); }
           get ToRef2() { return this.GetCustom("Custom2", "To"); }
           get ToRef3() { return this.GetCustom("Custom3", "To"); }
           get ToRef4() { return this.GetCustom("Custom4", "To"); }
           get ToRef5() { return this.GetCustom("Custom5", "To"); }
           get ToRef6() { return this.GetCustom("Custom6", "To"); }
           get ToRef7() { return this.GetCustom("Custom7", "To"); }
           get ToRef8() { return this.GetCustom("Custom8", "To"); }
           get ToRef9() { return this.GetCustom("Custom9", "To"); }
           get ToRef10() { return this.GetCustom("Custom10", "To"); }

           GetCustom(fieldName, customDataType) {
              var returnValue = "";
              var customData = null;
              if (customDataType === "User") customData = this.ShipmentRequest.PackageDefaults.OriginAddress.CustomData;
              if (customDataType === "To") customData = this.ShipmentRequest.PackageDefaults.Consignee.CustomData;

              if (customData) {
                 customData.forEach(function (customField) {
                    if (fieldName === customField.Key) returnValue = customField.Value;
                 })
              }
              return returnValue;
           }
        }

        return new Shipment(shipmentRequest);
     }

// Source: The Home Depot (HOMEDEPOT)_New CBR 2026_2508.cs
// GetUserContext(vm) // returns the user's context object from vm.userContext or vm.UserInformation
    this.GetUserContext = function (vm) {
        if (vm.userContext) return vm.userContext;
        if (vm.UserInformation) return vm.UserInformation;
    }

// PostalCodeLookup(addressName) // returns nothing; performs a user-context-aware postal code lookup request
    this.PostalCodeLookup = function (addressName) {
        var userContext = this.getUserContext(this);
        var postalCode = $('[address-property-name="' + addressName + '"] [name="postalCode"]').val();
        var content = { AddressName: addressName, PostalCode: postalCode };
        var payload = { Action: 'CSZ', Content: JSON.stringify(content) };
        var data = { Data: JSON.stringify(payload), UserContext: userContext };
        var response = this.thinClientAPIRequest('UserMethod', data, false).responseJSON;
        if (response.ErrorCode > 0) {
            client.shipping.alertModalShow(this, 'Postal Code Search', response.ErrorMessage)
        } else {
            var responseData = JSON.parse(client.htmlText(response.Data));
            if (responseData === null) {
                client.shipping.alertModalShow(this, 'Postal Code Search', 'Invalid or Not US Postal Code');
            } else {
                if (responseData.length > 1) {
                    this.addressResults = responseData;
                    client.shipping.validateAddressModalShow();
                } else {
                    this.currentShipment.updateNameAddressCSZ(addressName, responseData[0].City, responseData[0].StateProvince)
                }
            }
        }
    }

// Source: The Ohio State University (OSU)_OSU Reference Configuration Rule_89.cs
// PreSearchHistory(searchCriteria) // returns void
    this.PreSearchHistory = function(searchCriteria) {
        searchCriteria.WhereClauses.push({ FieldName: 'MiscReference20', FieldValue: userContext.UserId, Operator: 5 });
    }

// getUserContext() // returns void
    function getUserContext() {
        if (userContext == undefined) {
            userContext = ajaxGet('api/usercontext/GET');
        }
    }

// Source: Truist (BBANDT)_Truist Admin Business Rules_2463.cs
// this.PreSearchHistory(searchCriteria) // returns void
    this.PreSearchHistory = function (searchCriteria) {
        var selectedValue = $('select[id="reports"]:eq(0)').val();
        var selectedReport = $("select[id='reports'] option[value='" + selectedValue + "'");
        //debugger;
        if(selectedReport[0].innerText !=="All History"){
            searchCriteria.WhereClauses.push({ FieldName: 'UserId', FieldValue: userContext.UserId, Operator: 0 });
        }
    }; 

// Source: Truist (BBANDT)_Truist Business Rules_2464.cs
// this.PreSearchHistory(searchCriteria) // returns undefined
        this.PreSearchHistory = function (searchCriteria) {
        var selectedValue = $('select[id="reports"]:eq(0)').val();
        var selectedReport = $("select[id='reports'] option[value='" + selectedValue + "'");
        //debugger;
        if(selectedReport[0].innerText !=="All History"){
            searchCriteria.WhereClauses.push({ FieldName: 'UserId', FieldValue: userContext.UserId, Operator: 0 });
        }
    }; 

// Source: Volt Lighting (VOLT)_Volt_Business_Rules_1666.cs
// GetUserContext() // returns undefined; loads and caches the current user context
    function GetUserContext() {
        if (userContext == undefined) {
            var userContextUrl = 'api/usercontext/GET';
            var shipExecServiceUrl =  client.config.ShipExecServiceUrl;
            if (shipExecServiceUrl.startsWith('http')) {
                var url = new URL(shipExecServiceUrl);
                userContextUrl = url.origin + '/' + userContextUrl;
            }
            var authorizationToken =  client.getAuthorizationToken();
            userContext = $.get({url: userContextUrl, headers: authorizationToken, async: false}).responseJSON;
        }
    }

// getShippersByProfile() // returns undefined; populates shippers based on the current user context/profile
    function getShippersByProfile(){
	    var usermethodParams = { "action": 5, "orderNumber": "0", "percentageComplete":"0", "trailerNumber":"", "shipper":""};
	    var data = { UserContext: userContext, "Data": JSON.stringify(usermethodParams) };
		var response = client.thinClientAPIRequest('UserMethod', data, false).done(function (response) {
			if (response.ErrorCode != 0) {
				client.alert.Danger(response.ErrorMessage);
			} else {
				var json = DecodeReturnString(response.Data); //$.parseJSON($("<div />").html(response.Data).text());//$.parseJSON(response.Data);
                $.each(json.Shippers, function (i, item) {
                    $('#CVIS_Shippers').append($('<option>', { 
                        value: item["value"],
                        text : item["text"] 
                    }));
                });
			}
		});            
		
		/*$.ajax({
            type: "POST",
            url: "api/ShippingService/UserMethod",
            async: false,
            cache: false,
            dataType: "json",
            data: { UserContext: userContext, "Data": JSON.stringify(usermethodParams) },
            success: function(response) {
                var json = DecodeReturnString(response.Data); //$.parseJSON($("<div />").html(response.Data).text());//$.parseJSON(response.Data);
                $.each(json.Shippers, function (i, item) {
                    $('#CVIS_Shippers').append($('<option>', { 
                        value: item["value"],
                        text : item["text"] 
                    }));
                });
            },
            error: function (error) {
                alert(error);
            }
        });*/
	}

// Source: VSP (VSP)_VSP Reference Rules 2.0_2445.cs
// PageInitialize(viewModel) // returns undefined
    this.PageInitialize = function(viewModel) {
        vm = viewModel;
        //console.log(client.userContext);
        var searchCriteria = {WhereClauses:[{ FieldName:"Name", FieldValue:"Company Address Book", Operator:0 }]};
        var data = { SearchCriteria: searchCriteria, CompanyId: client.userContext.CompanyId, UserId: client.userContext.UserId };
        //var data = { UserContext: client.userContext, SearchCriteria: searchCriteria };
        
		//debugger;
		
        $.when(client.thinClientAPIRequest('GetAddressBooks_Shared', data, false).done(function(response, status, xhr)
        {
            //console.log('response from inside method: ', response);

            if (response.TotalRecords > 0) 
            {
                var addressBookId = response.AddressBooks[0].Id;
    
                data = { UserContext: client.userContext, AddressBookIds: [addressBookId], CompanyId: client.userContext.CompanyId, UserId: client.userContext.UserId };
                //console.log(data);
                
                $.when(client.thinClientAPIRequest('GetAddressBookEntries_Shared', data, false).done(function(response, status, xhr)
                {
                    //console.log(response);
                    if (response.TotalRecords > 0) 
                    {
                        tpbAddressBookEntries = response.AddressBookEntries;
                        vm.currentShipment.xConsigneeCodes = tpbAddressBookEntries;
                    }                    
                    //debugger;
                }));
            }
        }));

    };

// Source: VSP (VSP)_VSP Reference Rules_81.cs
// PageInitialize(viewModel) // returns undefined
    this.PageInitialize = function(viewModel) {
        vm = viewModel;
        //console.log(client.userContext);
        var searchCriteria = {WhereClauses:[{ FieldName:"Name", FieldValue:"Company Address Book", Operator:0 }]};
        var data = { SearchCriteria: searchCriteria, CompanyId: client.userContext.CompanyId, UserId: client.userContext.UserId };
        //var data = { UserContext: client.userContext, SearchCriteria: searchCriteria };
        
		debugger;
		
        $.when(client.thinClientAPIRequest('GetAddressBooks', data, false).done(function(response, status, xhr)
        {
            //console.log('response from inside method: ', response);

            if (response.TotalRecords > 0) 
            {
                var addressBookId = response.AddressBooks[0].Id;
    
                data = { UserContext: client.userContext, AddressBookIds: [addressBookId], CompanyId: client.userContext.CompanyId, UserId: client.userContext.UserId };
                //console.log(data);
                
                $.when(client.thinClientAPIRequest('GetAddressBookEntries', data, false).done(function(response, status, xhr)
                {
                    //console.log(response);
                    if (response.TotalRecords > 0) 
                    {
                        tpbAddressBookEntries = response.AddressBookEntries;
                        vm.currentShipment.xConsigneeCodes = tpbAddressBookEntries;
                    }                    
                    debugger;
                }));
            }
        }));

    };

// getUserContext() // returns the current user context object from the service
        getUserContext: function () {
            var userContextUrl = client.config.ShipExecServiceUrl.replace('ShippingService', 'usercontext/GET');
            var authorizationToken = client.getAuthorizationToken();
            return $.get({url: userContextUrl, headers: authorizationToken, async: false}).responseJSON;;
        }

// Source: VW Group (VW)_VW Client Rules_1513.cs
// PreSearchHistory(searchCriteria) // returns undefined
    this.PreSearchHistory = function (searchCriteria) {
        searchCriteria.WhereClauses.push({ FieldName: 'MiscReference19', FieldValue: userContext.UserId, Operator: 5 });
    };

// PreShip(shipmentRequest, userParams) // returns undefined
    this.PreShip = function (shipmentRequest, userParams) {
        var userId = userContext.UserId;
        for (var i = 0; i < shipmentRequest.Packages.length; i++) {
            shipmentRequest.Packages[i].MiscReference19 = userId;
        }
    };

// Source: Wesbanco Inc (Wesbanco)_Wesbanco Reference Rule_50.cs
// GetCustom(fieldName, customDataType) // returns the matching custom field value or an empty string
   GetCustom(fieldName, customDataType) {
      var returnValue = "";
      var customData = null;
      if (customDataType === "User") customData = this.ShipmentRequest.PackageDefaults.OriginAddress.CustomData;
      if (customDataType === "To") customData = this.ShipmentRequest.PackageDefaults.Consignee.CustomData;

      if (customData) {
         customData.forEach(function (customField) {
            if (fieldName === customField.Key) returnValue = customField.Value;
         })
      }
      return returnValue;
   }

// Source: WILLIAMS-SONOMA (WSintl) (WSintl)_WSIntl Reference Rule_75.cs
// this.PreSearchHistory(searchCriteria) // returns void
    this.PreSearchHistory = function (searchCriteria) {
       // searchCriteria.WhereClauses.push({ FieldName: 'MiscReference19', FieldValue: userContext.UserId, Operator: 0 });
    };

// Source: Worldwide Cargo Services Inc (WORLDWIDECSI)_API TEST_1781.cs
// PostalCodeLookup(addressName) // returns void
this.PostalCodeLookup = function (addressName) {
debugger;
        var userContext = this.getUserContext(this);
        var postalCode = $('[address-property-name="' + addressName + '"] [name="postalCode"]').val();
        var content = { AddressName: addressName, PostalCode: postalCode };
        var payload = { Action: 'CSZ', Content: JSON.stringify(content) };
        var data = { Data: JSON.stringify(payload), UserContext: userContext };
        var response = this.thinClientAPIRequest('UserMethod', data, false).responseJSON;
        if (response.ErrorCode > 0) {
            client.shipping.alertModalShow(this, 'Postal Code Search', response.ErrorMessage)
        } else {
            var responseData = JSON.parse(client.htmlText(response.Data));        
            if (responseData == null) {
                client.shipping.alertModalShow(this, 'Postal Code Search', 'Invalid or Not US Postal Code');
            } else {
                if (responseData.length > 1) {
                    this.addressResults = responseData;
                    client.shipping.validateAddressModalShow();
                } else {
                    this.currentShipment.updateNameAddressCSZ(addressName, responseData[0].City, responseData[0].StateProvince)
                }
            }
        }
    }

// GetUserContext(vm) // returns user context object or undefined
this.GetUserContext = function (vm) {
        if (vm.userContext) return vm.userContext;
        if (vm.UserInformation) return vm.UserInformation;
    }

