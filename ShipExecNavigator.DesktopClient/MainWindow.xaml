<Window x:Class="ShipExecNavigator.DesktopClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ShipExec Navigator"
        Height="520" Width="720"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E2E"
        ResizeMode="CanMinimize">

    <Window.Resources>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="36,13"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#106EBE"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#005A9E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#2A2A3E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3A3A55"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="CaretBrush" Value="White"/>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#13131F" Padding="32,20">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="&#x2693;" FontSize="28" Foreground="#0078D4"
                           VerticalAlignment="Center" Margin="0,0,14,0"/>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="ShipExec Navigator"
                               FontSize="22" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Desktop Client"
                               FontSize="11" Foreground="#7A7A99" Margin="0,3,0,0"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Main Panel -->
        <Border Grid.Row="1" Margin="32" Background="#13131F" CornerRadius="10">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" MinWidth="320">
                <TextBlock Text="Welcome"
                           FontSize="26" FontWeight="SemiBold"
                           Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock Text="Select an action to get started."
                           FontSize="13" Foreground="#7A7A99"
                           HorizontalAlignment="Center" Margin="0,10,0,0"/>
                <Button Style="{StaticResource PrimaryButtonStyle}"
                        Content="Launch Navigator"
                        Click="LaunchNavigator_Click"
                        HorizontalAlignment="Center"
                        Margin="0,28,0,0"/>

                <!-- Alert section -->
                <TextBlock Text="Send Alert to Navigator"
                           FontSize="13" Foreground="#7A7A99"
                           HorizontalAlignment="Center" Margin="0,28,0,8"/>
                <TextBox x:Name="AlertMessageBox"
                         Style="{StaticResource DarkTextBoxStyle}"
                         Width="260"
                         HorizontalAlignment="Center"
                         TextChanged="AlertMessageBox_TextChanged"/>
                <Button x:Name="SendAlertButton"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Content="Send Alert"
                        IsEnabled="False"
                        Click="SendAlert_Click"
                        HorizontalAlignment="Center"
                        Margin="0,12,0,0"/>
            </StackPanel>
        </Border>

        <!-- Footer -->
        <Border Grid.Row="2" Background="#13131F" Padding="32,12">
            <TextBlock Text="ShipExec Navigator &#xB7; Desktop Client"
                       FontSize="11" Foreground="#4A4A66"
                       HorizontalAlignment="Center"/>
        </Border>

    </Grid>
</Window>
