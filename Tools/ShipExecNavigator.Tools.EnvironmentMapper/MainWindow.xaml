<Window x:Class="ShipExecNavigator.Tools.EnvironmentMapper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Environment Mapper — Company Configuration Comparison" Height="650" Width="950"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Source environment file -->
        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Source Environment:" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1" x:Name="File1Path" IsReadOnly="True" Margin="4,0"/>
            <Button Grid.Column="2" Content="Browse..." Width="80" Click="BrowseFile1_Click"/>
        </Grid>

        <!-- Target environment file -->
        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Target Environment:" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1" x:Name="File2Path" IsReadOnly="True" Margin="4,0"/>
            <Button Grid.Column="2" Content="Browse..." Width="80" Click="BrowseFile2_Click"/>
        </Grid>

        <!-- Compare Button -->
        <Button Grid.Row="2" Content="Compare" Width="120" HorizontalAlignment="Left" Margin="0,0,0,8" Click="Compare_Click"/>

        <!-- Console Readout -->
        <TextBox Grid.Row="3"
                 x:Name="ConsoleOutput"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="12"
                 Background="#FF1E1E1E"
                 Foreground="#FFDCDCDC"
                 TextWrapping="NoWrap"/>
    </Grid>
</Window>
